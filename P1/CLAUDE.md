# CLAUDE.md

이 파일은 **P1** 언리얼 엔진 프로젝트에서 작업할 때 Claude Code가 참고하는 가이드입니다.

## 엔진 / 프로젝트 정보

- 엔진 버전: **5.8** (`P1.uproject` 참고)
- 메인 모듈: `P1` (Runtime, Default 로딩 페이즈)
- 빌드 파일: `Source/P1/P1.Build.cs`, `Source/P1.Target.cs`, `Source/P1Editor.Target.cs`
- 활성화된 플러그인: ModelingToolsEditorMode, ModelContextProtocol, Terminal, EditorToolset, GameplayAbilities, ModularGameplay, **MotionWarping** (설치돼 있으나 현재 미사용 — RMB는 결국 `ApplyRootMotionJumpForce`로 구현. `AP1HeroCharacter::MotionWarpingComponent`는 향후 다른 스킬에서 재사용 가능)

## 언리얼 C++ 컨벤션

- Epic의 [Unreal Engine Coding Standard](https://dev.epicgames.com/documentation/en-us/unreal-engine/epic-cplusplus-coding-standard-for-unreal-engine)를 따릅니다.
- 네이밍: 구조체/일반 클래스는 `F`, UObject 파생은 `U`, Actor 파생은 `A`, 인터페이스는 `I`, enum은 `E`, 템플릿은 `T`. bool 변수는 `b` 접두사 사용 (예: `bIsActive`).
- 이 프로젝트에서 직접 만드는 모든 게임플레이 클래스(Character, PlayerState, PlayerController, GameMode, GameState, AttributeSet, AbilitySystemComponent 등)는 위 UE 타입 접두사(`A`/`U`/...) 뒤에 프로젝트 접두사 `P1`을 붙입니다 (예: `AP1HeroCharacter`, `UP1AttributeSet`, `AP1GameMode`). 엔진/플러그인 타입을 그대로 쓰는 경우(`ACharacter`, `UAttributeSet` 등 서드파티 상속 없이 직접 인스턴스화하는 경우)는 제외합니다.
- 서드파티/비-UE 코드와 인터페이스하는 경우가 아니라면 STL보다 언리얼 자체 타입을 우선 사용: `TArray`, `TMap`, `TSet`, `FString`/`FName`/`FText`, `TOptional`, `TSharedPtr`/`TWeakPtr`/`TUniquePtr` 등.
- `UPROPERTY()` / `UFUNCTION()` 매크로에 올바른 스펙시파이어(`BlueprintReadOnly`, `EditAnywhere`, `VisibleAnywhere`, `BlueprintCallable` 등)를 사용. `UObject*` 멤버를 `UPROPERTY()` 없이 raw 포인터로 두지 말 것 — 가비지 컬렉터가 추적하지 못함.
- UObject 포인터 멤버는 raw `T*`보다 `TObjectPtr<T>`를 우선 사용 (UE5 기본값).
- 헤더/소스 분리: 선언은 `.h`, 구현은 `.cpp`. 헤더는 최소화 — 가능하면 `#include` 대신 전방 선언(forward declare) 사용.
- 모든 UCLASS/USTRUCT/UINTERFACE 선언에는 `GENERATED_BODY()`를 사용.
- 로깅은 커스텀 `DECLARE_LOG_CATEGORY_EXTERN` 카테고리를 통해서 하고, 실제 출시 코드 경로에서는 `printf`/`UE_LOG(LogTemp, ...)`를 직접 쓰지 않음.
- 사용 전 포인터 null 체크. `UObject*`는 raw null 체크보다 `IsValid()`를 우선 사용 (pending-kill 상태도 올바르게 처리됨).
- 게임 스레드를 블로킹하지 말 것 — 무거운 작업은 언리얼의 비동기/태스크 시스템(`AsyncTask`, `FRunnable`, Tasks System) 사용.
- 메모리: UObject에 수동 `new`/`delete`를 쓰지 말고 `NewObject<T>()`, `CreateDefaultSubobject<T>()`(생성자 내에서), `SpawnActor<T>()`를 사용.
- 블루프린트에 노출된 API를 수정할 때는 기존 블루프린트/에셋이 의존할 수 있으므로 하위 호환성을 유지.

## 빌드 & 이터레이션

- 이 프로젝트는 Binaries/Intermediate/Saved/DerivedDataCache를 커밋하지 **않습니다** — 빌드 산출물입니다 (`.gitignore` 참고).
- C++ 변경 사항은 보통 에디터에서 테스트하기 전에 IDE나 `UnrealBuildTool`을 통한 재컴파일이 필요합니다. 핫 리로드는 많은 경우 동작하지만 전부는 아닙니다 (새 UCLASS/USTRUCT 추가는 보통 에디터 재시작 필요).
- 빌드는 `Automation_P1.sln`, `P1.sln` 솔루션을 사용. Source 파일이 추가/삭제되면 `.uproject` 우클릭 → "Generate Visual Studio project files"로 프로젝트 파일을 재생성할 수 있습니다.

## 워크플로우 노트

- `Binaries/`, `Intermediate/`, `Saved/`, `DerivedDataCache/`, `.vs/`는 커밋하지 않음 — 로컬/생성 파일입니다.
- 새 C++ 클래스를 추가할 때는 `Source/P1/` 하위의 기존 모듈 구조를 따릅니다.
- **MCP 도구 사용 시 허가 필수:** `mcp__unreal-mcp__*` 도구들(에디터 에셋/오브젝트 조작)은 토큰 사용량이 많으므로, **먼저 사용자의 명시적 허가를 받은 후에만** 실행합니다. 입력 에셋 생성, 블루프린트 구성, 프로퍼티 설정 등을 MCP로 수행하려면 반드시 "이 작업을 MCP로 진행해도 될까요?" 같은 확인 후 진행하십시오.

## GAS (Gameplay Ability System) 컨벤션

GAS는 어빌리티/스탯 시스템의 핵심 토대로 사용할 예정이므로, 부가 기능이 아니라 핵심 아키텍처로 취급합니다:

- 리스폰 캐릭터의 경우, `UAbilitySystemComponent`는 보통 Pawn이 아니라 PlayerState에 두어 Pawn 파괴/소유권 변경에도 어빌리티 상태가 유지되도록 합니다.
- 게임플레이 속성(Attribute)은 `UAttributeSet` 서브클래스로 정의하고, getter/setter는 `GAMEPLAYATTRIBUTE_*` 매크로를 사용하며 `PreAttributeChange`/`PostGameplayEffectExecute`로 클램핑합니다.
- 어빌리티는 `UGameplayAbility` 서브클래스로 구현하고 `GiveAbility`/어빌리티 셋으로 부여합니다. 비용/쿨다운은 직접 타이머를 만들지 말고 `GameplayEffect`로 처리합니다.
- 어빌리티 식별, 차단/취소, 상태 쿼리에는 enum이나 문자열 비교 대신 `FGameplayTag`(`GameplayTagsManager`/`UGameplayTagsManager`)를 사용합니다.
- 모든 어빌리티 발동은 리플리케이션을 고려해야 합니다: 예측 키(prediction keys), 예측되지 않는 효과에는 `ServerRPC`/`ClientRPC`, 게임플레이 상태를 직접 손으로 리플리케이트하지 말고 `ReplicatedGameplayEffects`를 사용합니다.
- **어빌리티 부여(GiveAbility)는 `PossessedBy`에서 호출하는 `AddDefaultAbilities()`에서만 수행한다.** `InitAbilityActorInfo()`는 ASC 참조 초기화만 담당하며 능력 부여 코드를 포함하지 않는다 — 이를 섞으면 `OnRep_PlayerState`와 `PossessedBy` 양쪽에서 호출될 때 중복 부여가 발생한다.
- **`LocalPredicted` 어빌리티의 데미지 판정은 반드시 `CurrentActorInfo->IsNetAuthority()` 체크로 서버에서만 실행한다.** 클라이언트 예측 인스턴스에서도 AnimNotify가 발화되므로 체크 없이는 RTT만큼 늦게 중복 판정된다.
- **홀드 콤보는 `Spec->InputPressed`가 아니라 자체 `bInputHeld` 플래그로 판단한다.** 단, `bInputHeld`가 서버에서 갱신되려면 커스텀 입력 라우팅이 `ServerSetInputPressed/Released()`로 입력을 서버에 복제해야 하고, 어빌리티는 `bReplicateInputDirectly=true`여야 한다. 이게 없으면 서버 `InputReleased()`가 호출되지 않아 콤보가 한 타 더 나간다.
- **지면 조준(ground-targeted) 스킬 패턴**: `UAbilityTask_WaitTargetData(UserConfirmed)` + 커스텀 `AGameplayAbilityTargetActor` 서브클래스. 조준 확정 전까지는 `CommitAbility`를 호출하지 않아 코스트/쿨다운이 안 나가게 하고, 확정 시점에만 커밋한다. 조준 중에는 `State.TargetingAbility` 같은 루즈 태그를 부여해 ASC 입력 라우팅에서 다른 어빌리티 입력을 차단하고 LMB/RMB를 confirm/cancel로 리라우팅한다.
- **루트모션 도약(JumpForce)의 Duration은 몽타주 "전체 길이"가 아니라 "착지 판정 AnimNotify가 실제로 찍힌 시점"에 맞춘다.** `UAbilityTask_ApplyRootMotionJumpForce`의 Duration을 몽타주 총 길이로 주면, 착지 노티파이가 타임라인 중간(예: 60%)에 있을 경우 노티파이가 발동하는 순간 캐릭터가 아직 목표 지점에 도달하지 못한 채 공중에 뜬 상태가 된다. 몽타주의 `Notifies` 배열을 순회해 해당 노티파이의 `GetTriggerTime()`을 Duration으로 써야 물리적 착지와 애니메이션/데미지 판정이 정확히 일치한다.
- **같은 슬롯/겹치는 몽타주가 서로 인터럽트하면 진행 중이던 루트모션 태스크가 조기 종료되어 잔여 속도로 캐릭터가 통제 불가능하게 날아갈 수 있다.** (`ApplyRootMotionJumpForce`가 `VelocityOnFinishMode=MaintainLastRootMotionVelocity`인 상태에서 중간에 강제 종료되면 그 순간 속도가 일반 물리로 그대로 넘어감.) 루트모션 기반 이동 스킬이 활성 중일 때는 다른 어빌리티(특히 몽타주를 쓰는 기본공격)가 끼어들지 못하도록 `ActivationOwnedTags`로 차단 태그(`State.Attacking` 재사용 등)를 부여해야 한다.
- **"다음 기본공격 강화" 류 버프 어빌리티는 DamageGameplayAbility가 아니라 베이스 `UP1GameplayAbility`를 상속한다.** 버프 자체는 데미지를 입히지 않고 GE 하나(지속시간+태그 부여, 필요시 어트리뷰트 모디파이어)를 자신에게 적용하고 끝나며, 실제 강화된 효과는 그 태그를 체크하는 **기본공격(MeleeAttack) 쪽 코드**에서 처리한다. 사거리 증가처럼 기존에 이미 읽고 있는 어트리뷰트를 버프 GE가 잠깐 올려두는 방식이면 소비 어빌리티 쪽 코드 수정이 아예 필요 없다. 버프는 적중 판정 직후 `RemoveActiveEffectsWithGrantedTags()`로 소모(제거)해 "다음 1회만" 적용되게 한다.
- **"어빌리티가 자신에게 GE를 적용" 패턴(버프/디버프/쿨다운 등)은 전용 서브클래스가 아니라 베이스 `UP1GameplayAbility::ApplyEffectToSelf()`로 처리한다.** Damage처럼 여러 어빌리티가 공유하는 계수 프로퍼티가 있는 게 아니라 어빌리티마다 고유한 GE 하나를 참조하는 것뿐이라, `DamageGameplayAbility`식 서브클래스 계층을 만들 필요는 없다 — `MakeOutgoingGameplayEffectSpec`+`ApplyGameplayEffectSpecToOwner`(예측 키 컨텍스트를 올바르게 태움, raw `ASC->ApplyGameplayEffectSpecToSelf`보다 정석적)를 감싼 헬퍼 하나로 충분.
- **"어빌리티 레벨에 따라 값이 달라져야 하는" 수치(Flat 데미지, 커스텀 쿨다운 지속시간 등)는 `float` 대신 `FScalableFloat`(`ScalableFloat.h`)로 선언하고 `GetValueAtLevel(GetAbilityLevel())`로 평가한다.** `FScalableFloat`는 `Value * CurveTable[Level]` 형태로, Curve를 지정하지 않으면 `Value`를 그대로 반환하므로(`IsStatic()`) 기존처럼 고정값으로도 동작한다 — 기존 `float` 프로퍼티를 이 타입으로 바꿔도 `SerializeFromMismatchedTag`가 자동으로 값을 이관해 이미 에디터에서 값을 넣어둔 BP 자산이 깨지지 않는다. 단, **코스트(Cost)는 이 프로젝트에서 SetByCaller를 거치지 않고 `CommitAbilityCost()`가 어빌리티 고유의 `CostGameplayEffectClass`(엔진 내장 프로퍼티)를 그대로 커밋하므로, 코스트 GE 에셋의 Modifier Magnitude를 "Scalable Float"로 설정하면 C++ 변경 없이도 이미 레벨별로 값이 달라진다** — `MakeOutgoingGameplayEffectSpec`이 `GetAbilityLevel()`을 스펙 레벨로 넘기기 때문. 반대로 쿨다운처럼 `Data.CooldownDuration` SetByCaller로 C++이 직접 값을 주입하는 경우(감소 재적용 등 런타임 조정이 필요해서 GE 자체의 Scalable Float 대신 이 방식을 씀, `AssaultTheGates::BaseCooldown` 등)는 GE의 커브 메커니즘을 우회하므로 C++ 프로퍼티 자체를 `FScalableFloat`로 관리해야 레벨 스케일링이 적용된다.
- **버프로 얹는 추가 데미지는 `ApplyDamageToTarget()`의 Bonus 파라미터로 전달한다.** 어빌리티 클래스의 `FlatDamage`/`PhysicalPowerCoefficient`/`MagicalPowerCoefficient`/`TargetMaxHealthPctCoefficient`/`SourceMaxHealthPctCoefficient`는 그 어빌리티 자체의 평소 계수이므로, "평소엔 0, 버프 활성 시에만 추가" 패턴은 호출 시점에 Bonus 인자를 얹어 해결한다 (클래스 프로퍼티를 런타임에 직접 수정하지 않음).
- **"캐릭터를 따라다니며 지속적으로 주변을 판정하는" 스킬(Q의 회오리 등)은 표준 Periodic GameplayEffect가 아니라 어빌리티가 직접 소유하는 반복 타이머로 구현한다.** Periodic GE는 캐스트 시점에 고정된 대상에게만 반복 적용되므로, "캐릭터가 이동하며 매 틱 주변을 새로 스캔"하는 요구를 표현할 수 없다. 이런 스킬도 데미지 채널은 그대로 공유하므로 `UP1DamageGameplayAbility`는 상속하되, 전용 중간 상속 클래스는 실제로 2개 이상의 스킬에서 동일한 형태(반복 스캔+효과)가 확인되기 전까지는 만들지 않는다 (버프 헬퍼를 뽑을 때와 같은 원칙 — 추측성 추상화 금지).
- **지속시간 종료 후에야 쿨다운이 시작되는 스킬은 코스트/쿨다운 커밋을 분리한다.** 캐스트 시점엔 `CommitAbilityCost()`만 호출하고, 지속시간이 끝나는 시점(마지막 틱 등)에 `ApplyEffectToSelf(CooldownGE)`로 쿨다운만 따로 적용한다. 어빌리티가 중간에 취소/중단되어도 코스트는 이미 나갔으므로 `EndAbility` 오버라이드에서 쿨다운 미적용 시 폴백으로 적용해준다 (플래그로 중복 적용 방지).
- **여러 데미지 어빌리티가 겹치는 "주변 오버랩+팀/높이 필터" 로직은 `UP1DamageGameplayAbility::GetEnemiesInRadius()` 공용 헬퍼로 처리한다.** 전방 반원처럼 방향성 필터가 추가로 필요하면(MeleeAttack) 헬퍼가 반환한 목록에 호출자가 덧씌운다 — 헬퍼 자체에 방향 필터를 넣지 않아 360도 판정(Make Way)과 반원 판정(MeleeAttack) 양쪽에 재사용 가능하다.
- **무적/스테이시스류(피격 완전 무시) 스킬은 GE로 `State.Invulnerable` 태그만 부여하면 끝난다 — 별도의 "데미지 취소" 로직을 어빌리티마다 만들지 않는다.** `UP1AttributeSet::PostGameplayEffectExecute`가 Damage 메타 어트리뷰트를 Health로 변환하기 직전 이 태그를 한 번 체크해 전 어빌리티 공용으로 처리하므로, 어떤 스킬이든 이 Duration GE 하나만 자신에게 적용하면 그 지속시간 동안 자동으로 무적이 된다(Stone Forged Soul의 스테이시스가 첫 사용처).
- **목표 지점이 없는 고정 왕복 이동(제자리에서 떴다가 같은 자리로 돌아오는 등)은 `ApplyRootMotionJumpForce` 같은 코드 기반 궤적 생성이 필요 없다 — 원본 애니메이션의 루트모션을 그대로 쓰는 게 더 간단하고 정확하다.** RMB(Assault The Gates)가 코드로 포물선을 만든 이유는 지면 조준으로 "매번 달라지는 가변 목표 지점"까지 도달해야 했기 때문이다. 목표 지점이 항상 "원래 서있던 자리"로 고정된 경우(R의 상승→하강 등)는 애니메이터가 구워둔 루트모션이 정확히 그 왕복을 표현하므로, 원본 소스 애니메이션에 실제 수직 이동이 담겨 있는지만 확인하면 된다 — 있으면 그대로 재생, 없으면(에셋에 흔한 문제) 코드 기반 이동을 다시 설계해야 한다. 공중에 뜬 동안은 중력이 루트모션과 싸우지 않도록 `MovementMode`를 `MOVE_Flying`으로 일시 전환했다가 착지 시점(AnimNotify)에 `MOVE_Walking`으로 되돌린다.
- **"맞았을 때 반응하는" 쿨다운 있는 리액티브 프록(디플렉트, 스펠실드 등)은 어빌리티가 아니라 `UP1AttributeSet::PostGameplayEffectExecute`에서 판정하고, 어빌리티는 그 판정 결과를 통보받아 쿨다운만 커밋하는 얇은 껍데기로 만든다.** 데미지를 실제로 취소할 수 있는 유일한 지점이 AttributeSet이기 때문에 판정 자체를 거기서 하는 게 자연스럽고(무적 판정과 같은 이유), "이 데미지가 어떤 어빌리티에서 왔는지"는 `Data.EffectSpec.CapturedSourceTags`로 확인한다(위 `ApplyDamageToTarget`의 Asset Tags 전달 항목 참고). "지금 사용 가능한지"는 네이티브 쿨다운 태그(`Cooldown.Ability.X`)의 부재로 판단하면 충분 — 새로 부여된 어빌리티는 쿨다운 태그 없이 시작하므로 별도의 "준비완료" 상태를 만들 필요가 없다. 다만 AttributeSet은 어빌리티 클래스를 참조하면 안 되므로(범용성 유지), "이 캐릭터가 애초에 이 어빌리티를 갖고 있는지"는 그 어빌리티가 `OnGiveAbility`에서 자기 Asset Tag를 루즈 태그로 ASC에 심어두는 것으로 판별하고, 발동(쿨다운 커밋)은 AttributeSet이 GameplayEvent를 보내 `AbilityTriggers`로 그 어빌리티를 트리거하는 방식으로 위임한다(Stoicism 디플렉트가 첫 사용처). 이런 반응형 패시브는 다른 어빌리티 사용 중에도 항상 발동해야 하므로 생성자에서 베이스가 공통으로 거는 `State.Attacking` 상호배타 규칙을 `RemoveTag`로 명시적으로 빼야 한다.
- **코스메틱(비-게임플레이) 이펙트를 다른 클라이언트(시뮬레이티드 프록시)에도 보이게 하려면 상황에 맞는 메커니즘을 고른다. GAS 어빌리티 C++ 코드에서 직접 `SetMaterial`/파티클 스폰을 하면 로컬 클라이언트+서버에서만 보이고 다른 플레이어 화면엔 안 보인다(LocalPredicted 어빌리티는 시뮬레이티드 프록시에서 실행되지 않음) — 반드시 아래 중 하나를 거쳐야 한다:**
  1. **AnimNotify(State)** — 몽타주의 고정된 타임라인 구간에 이펙트를 묶을 때 (예: 스윙 궤적, 착지 파티클). 몽타주가 리플리케이트되므로 모든 클라이언트가 재생.
  2. **`AP1CharacterBase`의 Multicast RPC** — 이펙트 수명이 GE의 활성~제거(조기 소모 포함)에 대응해야 하거나, 몽타주에 고정 배치할 수 없는 **조건부/가변 길이** 이펙트일 때. 머티리얼/파티클을 **직접 프로퍼티로 참조**하고 서버에서 이 RPC를 호출하면 끝 — 별도 Cue Notify 에셋이나 스캔 경로 등록이 필요 없어 이 프로젝트에서는 이 방식을 기본으로 채택. 두 갈래가 있다: (a) **1회성** — `MulticastPlayParticleEffect`, "쐈다가 알아서 끝남"에 적합(`SpawnEmitterAttached(..., bAutoDestroy=true)`); 매 스윙마다 다시 트리거해야 하는 이펙트(적중 여부 무관하게 반복 재생)도 이 방식을 매번 호출하는 식으로 처리한다(Sacred Oath 무기 궤적). (b) **지속(버프 생명주기, 상태 자체가 켜져있음을 표현)** — `MulticastSetMaterialOverride` 또는 `MulticastSetAttachedParticleEffect`+`MulticastStopAttachedParticleEffect`(파티클처럼 컴포넌트를 직접 붙였다 떼야 하는 경우, `bAutoDestroy=false`로 스폰 후 반환된 `UParticleSystemComponent*`를 `AttachedParticleEffectComponent`에 보관했다가 `DeactivateSystem()`+`DestroyComponent()`로 정리), "태그가 있는 동안 계속 유지되다 태그가 사라지면 꺼짐"에 적합(Sacred Oath 검 발광). **(a)와 (b)의 선택 기준**: 이펙트가 "그 순간 상태(버프 활성)를 계속 나타내야 하는가"(b) vs "특정 이벤트(스윙, 히트)가 발생할 때마다 다시 트리거되어야 하는가"(a) — 검 발광엔 (b), 무기 궤적엔 (a)를 쓴다(처음엔 궤적도 (b)로 만들었다가 "매 스윙마다 재생"이라는 실제 요구와 안 맞아 (a)로 되돌렸다). (b) 패턴은 어빌리티가 직접 호출하지 않고 아래 `UP1BuffCosmeticEffectComponent`가 담당한다.
  3. **GameplayCue** — `UP1GameplayCueNotify_MaterialOverride`/`_ParticleEffect`가 범용 유틸리티로 존재하지만, 폴더 스캔(`GameplayCueNotifyPaths`)+태그매칭 방식이 에디터 세션 중 갱신이 잘 안 되는 등 다루기 번거로워서 Sacred Oath는 결국 2번(Multicast)으로 전환했다. 팀 규모가 커져 어빌리티 코드와 이펙트 에셋을 완전히 분리해야 할 필요가 생기면 재검토.
- **"버프 상태에 따라 같은 입력이 다른 동작을 해야 하는" 경우("강화된 기본공격" 등)는 같은 InputTag에 두 어빌리티를 경쟁시키지 않고, 그 시너지가 있는 영웅이 아예 다른(상속) 어빌리티 클래스를 자신의 유일한 해당 슬롯 어빌리티로 등록한다.** 판정(버프 태그 유무)은 액티베이션 시점이 아니라 실제 히트/효과 적용 시점에 어빌리티 내부에서 한다 — 이 프로젝트는 처음에 `ActivationRequiredTags`/`ActivationBlockedTags`로 같은 InputTag에 평소 버전과 변종을 동시에 등록해 상호 배타적으로 게이팅하는 방식을 시도했으나, **액티베이션 시점의 태그 스냅샷은 `LocalPredicted` 어빌리티의 클라이언트 예측 GE가 서버 authoritative 버전과 조율(reconcile)되는 타이밍에 취약해서(예측된 버프 태그가 실제보다 훨씬 일찍 사라지는 것처럼 보이는 경우 발생) 폐기했다.** 또한 "먼저 시도되어 성공한 어빌리티가 `State.Attacking`을 걸어 같은 입력에 매칭되는 나머지 후보를 오염시킬 수 있다"는 구조적 위험도 있었다. 대신: 시너지가 없는 영웅은 베이스 클래스(`UP1GameplayAbility_MeleeAttack`)를, 있는 영웅은 서브클래스(`UP1GameplayAbility_MeleeAttack_SacredOath`)를 DefaultAbilities에 등록 — 한 영웅당 해당 슬롯 어빌리티는 항상 하나뿐이라 경쟁 자체가 없다. 콤보/몽타주처럼 복잡한 공유 로직은 베이스에 남기고, 서브클래스가 상속받아 재사용 — 데미지 계산처럼 실제로 달라지는 부분만 `protected virtual` 훅(`ApplyComboHitDamage` 등)으로 뽑아 오버라이드하고, 그 안에서 버프 태그를 체크해 없으면 `Super::`로 폴백한다. 베이스 어빌리티 클래스는 특정 버프의 존재를 전혀 몰라도 된다.
- **"버프 GE가 걸어둔 태그가 있는 동안 코스메틱 이펙트를 켜고, 사라지면(조기 소모든 자연 만료든 원인 무관하게) 되돌려야 하는" 경우는 타이머로 추측하지 말고 `ASC->RegisterGameplayTagEvent(Tag, EGameplayTagEventType::NewOrRemoved)`로 태그 카운트 변경 이벤트를 직접 구독한다.** Sacred Oath 검 발광이 초기엔 "자연 만료 시점에 맞춘 타이머"(조기 소모 시 별도로 즉시 원복)라는 이중 경로였는데, 이건 정확히 "GE 자체 만료"와 "타이머 발화"가 거의 동시에 태그를 건드리면서 경쟁 상태를 만들었던 지점이다. 태그 이벤트 구독은 원인(조기 소모/자연 만료)과 무관하게 카운트가 0↔양수로 바뀌는 정확한 시점에 단 한 번만 반응하므로 이런 경쟁이 애초에 발생하지 않는다.
- **"버프 태그 하나에 반응해 코스메틱을 켜고 끄는" 로직은 어빌리티나 영웅 공용 캐릭터 클래스에 하드코딩하지 않고 `UP1BuffCosmeticEffectComponent`(범용 PawnComponent, 아래 항목 참고)에 데이터로 넣는다.** 처음엔 이 리스너를 `AP1HeroCharacter`(모든 영웅의 공용 베이스)에 직접 구현하고 태그/슬롯 이름을 그 클래스의 멤버로 하드코딩했었는데, 이러면 Sacred Oath 시너지가 없는 다른 영웅도 전혀 안 쓰는 프로퍼티/리스너를 상속받게 되어 `MeleeAttack`이 한때 겪었던 것과 같은 종류의 "공용 클래스 오염"이 캐릭터 레벨에서 재발한다. 컴포넌트로 옮기면 이 시너지가 필요한 영웅의 BP에만 컴포넌트를 추가하면 되고, `AP1HeroCharacter`는 "내가 이 컴포넌트를 갖고 있다면 ASC를 넘겨준다"는 범용 훅 한 줄(`FindComponentByClass`)만 알면 된다 — 특정 스킬 이름은 전혀 몰라도 됨.
- **AnimBP 슬롯은 몽타주 성격에 맞게 골라야 한다.** Layered Blend Per Bone으로 블렌딩되는 `UpperBody` 슬롯은 하체를 Locomotion State Machine에 맡기고 상체만 덮어씌운다 — 이동 중에도 자연스러운 기본공격류에 적합. 반면 도약/궁극기처럼 전신을 새로 정의해야 하는 스킬은 Locomotion+UpperBody 블렌드 이후에 위치한 **FullBody 전용 Slot**(Output Pose 직전, 전신을 100% 오버라이드)을 써야 한다. 잘못된 슬롯을 쓰면 GAS/AnimInstance 레벨에서는 몽타주가 정상 재생(로그·노티파이 전부 발동)되는데도 AnimGraph에 연결이 안 돼 있어 화면에는 전혀 반영되지 않는다 — 재생 여부는 `AnimInstance::Montage_IsPlaying`/`Montage_GetPosition`으로 검증하고, 안 보이면 슬롯 배선부터 의심할 것.

## UI 아키텍처 컨벤션

- **역할 분리**: C++는 델리게이트 바인딩·위젯 컨트롤러 연결·데이터 계산 로직만 담당. 색상·폰트·레이아웃·애니메이션은 WBP 디자이너에서 설정한다.
- **계층 구조**: `UP1UserWidget` → 각 위젯 C++ 클래스 → WBP (Blueprint). WBP의 이벤트 그래프는 최소화하고 C++에서 제어한다.
- **위젯 컨트롤러 패턴 (D1/Aura 방식)**: `AP1HUD`가 화면별 컨트롤러를 레지스트리로 보유. `FWidgetControllerParams`(PC, PS, ASC, AS)로 초기화. ASC 델리게이트 → 컨트롤러 브로드캐스트 → 위젯 업데이트.
- **FloatingStatusWidgetController는 per-character**: AP1HUD가 아닌 `AP1HeroCharacter::InitAbilityActorInfo()`에서 캐릭터마다 생성.
- **BindWidget / BindWidgetOptional**: C++ 멤버 변수 이름과 WBP 위젯 이름이 정확히 일치해야 바인딩된다.
- **NativePaint 사용 기준**: 동적 계산이 필요한 커스텀 렌더링(세그먼트 구분선 등)에만 사용. 단순 색상/스타일은 WBP에서 처리.

## 멀티플레이어 / 네트워킹 컨벤션

- 목표 토폴로지: 데디케이티드 서버 권위(authoritative), 소규모 매치 (단일 아레나 배틀 아레나 모드, 리그오브레전드 "칼바람 나락"/ARAM과 유사).
- 기본적으로 서버 권위 로직을 따릅니다: 게임플레이 판정은 서버에서 확정하고, 클라이언트는 GAS 예측이나 무브먼트 예측(Character Movement Component)이 지원하는 범위에서만 예측합니다.
- 리플리케이트되는 프로퍼티는 `UPROPERTY(Replicated)`로 표시하고 `GetLifetimeReplicatedProps`를 구현합니다. 상태 변경에 대한 클라이언트 반응은 `RepNotify` 콜백을 사용합니다.
- 언리얼 RPC(`Server`, `Client`, `NetMulticast`)는 신중하게 사용합니다 — `Server` RPC는 클라이언트→서버로 신뢰 경계를 넘으므로 `WithValidation`으로 검증합니다.
- 경쟁형 멀티플레이어 게임에 맞게 리플리케이션 범위를 적절히 제한합니다(`bNetUseOwnerRelevancy`, relevancy, `OnlyRelevantToOwner` 등) — 대역폭을 최소화하고, 코스메틱 전용 상태를 과도하게 리플리케이트하지 않습니다.
- 카메라/조작 방식: 백뷰(3인칭, 어깨너머 시점) 캐릭터, 논타겟("non-target") 스킬샷 스타일 어빌리티 캐스팅 — 클릭-투-타겟이 아니라 커서/조준 방향 기반의 라인트레이스/투사체/AOE 타게팅으로 설계합니다.

---

## 프로젝트 설명

**장르:** 멀티플레이어 배틀 아레나 — *Paragon*(Epic의 단종된 MOBA) 에셋을 기반으로 한 단일 아레나(콜로세움) 전투 모드. 리그오브레전드의 "칼바람 나락"(ARAM)처럼, 정식 3라인 MOBA가 아니라 좌우 대칭형 단일 라인 아레나에서 곧바로 교전이 벌어지는 구조.

**프로젝트 목표 (중요, 의사결정의 최우선 기준):** 약 2개월 내에 (1) 비주얼적으로 그럴듯한 완성도와 (2) 언리얼 멀티플레이어 네트워킹 역량을 어필할 수 있는 포트폴리오를 만드는 것이 목표입니다. 콘텐츠 볼륨(맵 크기, 라인/정글 수, 캐릭터 수 등)을 키우는 것보다 적은 콘텐츠를 더 다듬고, 서버 권위 로직/GAS 리플리케이션/예측 같은 네트워킹 코드의 완성도를 높이는 쪽으로 시간을 배분합니다. 작업 범위를 결정할 때 항상 이 목표를 기준으로 판단합니다.

**진행 단계:** 초기 프로토타입. 어빌리티, 전투, UI 등 핵심 게임플레이 시스템의 기반을 갖춘 상태이며, 현재는 스킬 어빌리티 구현 단계입니다.

**코드베이스 출처:** 스타터 템플릿에서 파생되지 않고 완전히 처음부터(from scratch) 구축합니다. 다만 Epic의 **Lyra** 샘플의 구조적 패턴(예: 모듈화된 GAS 구성, Game Feature 플러그인 방식, 경험/데이터 드리븐 설계)을 필요한 부분에서 선택적으로 참고/모방하려는 시도를 할 수 있습니다.

**아트/콘텐츠 에셋:** 오리지널 *Paragon* 에셋 팩과 기타 구매한 마켓플레이스 에셋을 사용합니다 (직접 제작한 아트가 아님) — `Content/ParagonProps`, `Content/KiteDemo` 등에서 확인 가능합니다. 콜로세움/아레나 맵은 `Content/Maps/InGame.umap`(경로 `/Game/Maps/InGame`)으로 임포트+구성 완료 — `AP1ArenaGameMode`가 실제로 구동되는 매치 맵이자 `Scripts/Package_Client.bat`/`Package_Server.bat`이 쿡하는 맵. (한때 존재했던 `Content/Maps/Arena.umap`은 더 이상 쓰지 않는 구 맵이니 혼동 주의.) 코드, 게임플레이 시스템, 게임 고유 로직은 전부 커스텀입니다.

**핵심 시스템:**
- **멀티플레이어 모델:** 데디케이티드 서버, 서버 권위적. 소규모 매치(예: 3v3~5v5) 기준의 단일 아레나.
- **맵 구조:** 좌우 대칭형 콜로세움/아레나 1개. 정식 MOBA의 3라인·정글·다중 타워 구조는 사용하지 않음 (ARAM/칼바람 나락 스타일).
- **어빌리티/전투 시스템:** 언리얼의 **Gameplay Ability System(GAS)** 기반 — 속성(Attribute), 게임플레이 이펙트, 게임플레이 태그, 어빌리티 셋.
- **카메라/조작 방식:** 백뷰(3인칭, MOBA/액션RPG 스타일 추적 카메라), **논타겟**("non-targeted") 어빌리티 캐스팅 — 즉 클릭-투-유닛-타겟 방식이 아니라 방향/커서/AOE 기반의 스킬샷 스타일 조준.

**Claude 작업 시 시사점:** 게임플레이에 영향을 주는 모든 코드에서 서버 권위적 설계, GAS에 맞는 구현(임시방편 상태 관리 대신 태그/이펙트/어빌리티 활용), 리플리케이션 정확성을 우선시합니다. 구조에 대해 확신이 없을 때는 Lyra의 패턴(모듈화된 게임플레이, 데이터 드리븐 경험, GAS 구성)을 합리적인 참고 기준으로 삼을 수 있지만, Lyra와 정확히 일치시킬 필요는 없습니다. 작업 범위나 우선순위가 애매할 때는 "2개월 내 비주얼+네트워킹 포트폴리오"라는 목표를 기준으로 판단합니다.

---

## 구현 현황

> 소스코드 기준 현재 상태. 새 시스템을 추가하거나 완료되면 이 섹션을 업데이트할 것.

### GameMode / GameState (`Source/P1/GameModes/`)

| 클래스 | 상태 | 비고 |
|---|---|---|
| `AP1GameMode` | 완료 | 베이스 GameMode (쉘) |
| `AP1ArenaGameMode` | 완료 | DefaultPawnClass=P1HeroCharacter, GameStateClass=P1GameState 지정. **`ChoosePlayerStart_Implementation`**(신규 일반화, 2026-07-27) — 팀 미배정(255) 플레이어에게 `NextTeamIndex % NumTeams`로 팀을 교대 배정(리스폰 시엔 기존 팀 유지)하고, `PlayerStartTag`가 `"Team0"`~`"Team{NumTeams-1}"`인 `APlayerStart` 중 하나를 `FMath::RandHelper`로 랜덤 선택해 반환 — 같은 태그의 PlayerStart를 팀당 여러 개 배치해두면 팀원 여러 명이 한 자리에 겹쳐 스폰되는 걸 막을 수 있다(예전엔 태그 일치하는 첫 번째 하나만 무조건 반환했음). `NumTeams`(EditDefaultsOnly, 기본 3)는 레벨에 배치한 PlayerStart 태그 개수와 반드시 일치해야 한다. `BeginPlay()`가 `GameState->SetMatchStartTime()`(매치 경과 시간 기준점)와 `GameState->InitializeTeamScores(NumTeams)`(팀 킬스코어 배열 크기 맞춤)를 함께 호출. **매치 종료(2026-08-12)**: `KillScoreToWin`(EditDefaultsOnly, 기본 30)/`ResultScreenDurationSeconds`(기본 6초)/`LobbyMapPath`(기본 `/Game/Maps/PreGame`, 실제 로비 레벨 경로와 일치) 프로퍼티 + `OnTeamKillScored(TeamId)`(킬마다 `UP1AttributeSet::HandleKillRewards()`가 호출 — `GameState->AddTeamKill()` 후 임계치 도달 시 `EndMatch()`) + `EndMatch(WinningTeamId)`(`GameState->SetMatchEnded()` 후 `GameState->PlayerArray`를 순회해 각 `AP1PlayerController::ClientShowMatchResult()` Client RPC 호출 — Duration/맵 경로를 RPC 파라미터로 함께 실어 보내 PlayerController가 별도로 같은 값을 중복 보유하지 않게 함). **캐릭터 선택 스폰(신규, 2026-08-18)**: `InitNewPlayer` 오버라이드(반환형이 `FString`인 엔진 시그니처를 그대로 따름 — `void`로 잘못 선언하면 covariant 오류로 컴파일 자체가 깨짐) — `Super` 호출 후 `UGameplayStatics::ParseOption(Options, TEXT("HeroId"))`로 접속 URL의 `?HeroId=` 값을 읽어 `HeroTable`(신규 `EditDefaultsOnly UDataTable*`, RowStruct=`FP1HeroDefinition`, `Characters/P1HeroTypes.h`)에서 `FindRow` 조회 → 성공 시 `Cast<AP1PlayerController>(NewPlayerController)->SelectedCharacterClass`에 대입, 실패(옵션 없음/테이블에 없는 값)하면 경고 로그만 남기고 조용히 통과 — **HeroTable에 실제로 존재하는 HeroId만 유효하므로 이 테이블 자체가 클라이언트 URL 조작에 대한 화이트리스트 역할을 겸한다.** `InitNewPlayer`는 `ChoosePlayerStart`/`RestartPlayer`(그 안에서 아래 `GetDefaultPawnClassForController_Implementation` 호출)보다 항상 먼저 실행되므로 타이밍 문제 없음. **`GetDefaultPawnClassForController_Implementation`**(기존, 이제 이 값을 실제로 소비) — `SelectedCharacterClass`가 채워져 있으면 그 클래스를, 비어있으면 `DefaultPawnClass`로 폴백 |
| `AP1GameState` | 완료 | `MatchStartServerTime`(복제, 센티널 -1)+`SetMatchStartTime()`+`GetElapsedMatchTime()` — 매치 경과 시간 기준점, HUD가 매 프레임 폴링으로 읽음(델리게이트 없음). **팀 킬스코어/매치 종료 상태(2026-08-12)**: `TeamKillScores`(복제 `TArray<int32>`, 인덱스=TeamId)+`EP1MatchState MatchState`(`ReplicatedUsing=OnRep_MatchState`, InProgress/Ended)+`WinningTeamId`(복제, 기본 -1) — 전부 순수 데이터 저장소일 뿐, 승리 임계치 판정/매치 종료 오케스트레이션은 `AP1ArenaGameMode`가 전담(`SetMatchStartTime()`과 같은 "GameMode가 오케스트레이션, GameState가 저장" 원칙). `InitializeTeamScores()`/`AddTeamKill()`/`SetMatchEnded()` 전부 서버 전용 뮤테이터 |
| `AP1LobbyGameMode` | 완료 | **PreGame(로비) 전용, 데디케이티드 서버 없는 완전 로컬/싱글플레이 레벨**. `DefaultPawnClass=nullptr`(스폰할 Pawn 없음), `PlayerStateClass`를 베이스 `AP1GameMode`가 물려주는 `AP1PlayerState`(ASC 보유, 로비엔 불필요)에서 plain `APlayerState`로 되돌림, `PlayerControllerClass=AP1LobbyPlayerController`. 로그인/매칭 대기열은 전부 `UP1BackendSubsystem`이 웹 백엔드와 직접 통신하고, 매칭 성사 시 `ClientTravel`로 이미 떠 있는 Arena 데디케이티드 서버에 접속 — 이 GameMode 자체는 껍데기에 가깝다 |

### Player (`Source/P1/Player/`)

| 클래스 | 상태 | 비고 |
|---|---|---|
| `AP1PlayerState` | 완료 | ASC + AttributeSet 호스팅, Mixed 리플리케이션, `IGenericTeamAgentInterface` (TeamId 리플리케이션). **레벨/전투기록**: `CharacterLevel`(1~18, `SetCharacterLevel()`), `SkillPoints`(시작값 1 — 1레벨에 Q/E/RMB 중 하나를 바로 투자할 수 있어야 해서 LoL처럼 캐릭터 1레벨 자체에 포인트 1개가 딸려옴, 이후 레벨업마다 +1), `Kills`/`Deaths`/`Assists`/`KillStreak`(사망 시 0으로 리셋), `LastDeathTime`(비복제, 서버 전용 — "얼마나 오래 안 죽었는지" 현상금 계산용). **`StunEndServerTime`**(`ReplicatedUsing=OnRep_StunEndServerTime`, 전원 복제)+`OnStunTimeChangedNative` 델리게이트 — 머리 위 스턴바 카운트다운을 프록시(적을 보는 클라)에서도 정확히 그리기 위한 값. 서버가 스턴 적용 시 `AP1HeroCharacter::OnStunTagChanged`에서 `GetWorld()->GetTimeSeconds() + GE남은시간`을 `SetStunEndServerTime()`으로 기록하고, 클라는 `이 값 − GameState->GetServerWorldTimeSeconds()`로 남은시간을 계산(동기화된 서버 시계라 로컬 시계 차이 문제 회피). Mixed 모드에서 스턴 GE 자체는 프록시에 안 가지만 이 복제 프로퍼티는 전원에게 감(자세한 배경은 아래 알려진 버그 참고). `SpendSkillPoint()`는 `AddSkillPoint()`와 대칭(0 이하로 안 내려가게 방어) — 실제 소비 검증(포인트 있는지, 어빌리티가 최대 레벨 아닌지, R처럼 캐릭터 레벨 게이트 통과하는지)은 `UP1AbilitySystemComponent::ServerInvestSkillPoint()`가 전담. `GetXPRequiredForNextLevel()`은 `XPToNextLevelTable`(Row="XPToNextLevel", Time=CharacterLevel)을 `FindCurve`+`Eval`로 조회, 테이블 미설정 시 0(레벨업 불가)으로 안전 폴백. `CharacterLevel`/`SkillPoints`/`Kills`/`Deaths`/`Assists`는 `ReplicatedUsing`(OnRep)에 더해 **네이티브 멀티캐스트 델리게이트**(`OnCharacterLevelChangedNative`/`OnSkillPointsChangedNative`/`OnKDAChangedNative`)를 값이 바뀌는 지점(서버 Setter)과 OnRep(클라이언트) **양쪽에서** 브로드캐스트한다 — GAS Attribute의 `Set()`이 서버/클라 양쪽에서 델리게이트를 쏘는 것과 동일한 이유(리슨서버가 호스트를 겸하면 자기 자신에게는 OnRep이 발동하지 않으므로, Setter 쪽 즉시 호출이 없으면 호스트 자신의 HUD만 갱신 안 됨 — 이 프로젝트는 데디케이티드 서버 토폴로지라 실전에선 안 겪지만 방어적으로 양쪽 다 호출). Kills/Deaths/Assists는 KDA로 항상 함께 표시되므로 델리게이트 하나로 3값을 묶어 브로드캐스트(개별 델리게이트 3개로 안 쪼갬). **왜 GameState가 아니라 PlayerState인가**: `DOREPLIFETIME`가 OwnerOnly 조건 없이 전체 브로드캐스트라 이미 다른 클라이언트도 `GameState->PlayerArray` 순회로 남의 K/D/A를 읽을 수 있음 — 스코어보드 만들 때도 GameState에 별도 복제 불필요, 그때그때 PlayerArray 순회로 충분 |
| `AP1PlayerController` | 완료 | Enhanced Input (Move/Look/Jump + AbilityInputActions TMap), `AP1PlayerCameraManager` 지정. `HandleAbilityInputPressed(InputTag)`에서 Ctrl(Left/Right)이 눌려있으면 어빌리티 발동 대신 `ASC->ServerInvestSkillPoint(InputTag)`를 호출하고 리턴 — 스킬 포인트 투자는 클릭이 아니라 Ctrl+스킬키로 처리한다(마우스는 게임 중 캡처돼 있어 UI 버튼 클릭이 불가능하고, 상점을 열 때만 UI 모드로 전환할 예정이라 그때까진 키보드로만 처리). **`BeginPlay()`에서 로컬 컨트롤러면 `SetInputMode(FInputModeGameOnly())`+`bShowMouseCursor=false`로 명시적으로 되돌린다** — PreGame 로비(`AP1LobbyPlayerController`)가 `FInputModeUIOnly`로 바꿔둔 마우스 캡처 모드가 `GameViewportClient` 레벨 상태라 `ClientTravel`로 완전히 새 PlayerController가 생겨도 안 풀리고 남아있어서, Arena 입장 직후 키보드/마우스 게임 입력이 전혀 안 먹던 버그의 원인이었다(아래 알려진 버그 참고). `HandleMove()`는 `State.Stunned` 또는 `State.Rooted`(캐스팅 중 이동 차단용 범용 루즈 태그, 사용처는 Dekker 섹션 참고) 보유 시 입력을 무시한다. **`HandleJumpStarted()`**: 캐릭터가 공중(`CharacterMovement->IsFalling()`)이면 일반 `Character->Jump()` 대신 `ASC->AbilityInputTagPressed(InputTag.Ability.Passive)`를 호출 — Dekker의 Rocket Boots(공중 재점프 패시브)가 이 경로로 트리거된다(Dekker 섹션 참고). 이 태그를 가진 어빌리티가 없는 영웅은 아무 일도 안 일어나 안전(원래도 `JumpMaxCount=1`이라 공중 재점프는 무효였음). **`ClientShowDamageNumber(WorldLocation, DamageAmount)`**(Client RPC) — `UP1AttributeSet::NotifyDamageDealt()`가 가해자 컨트롤러에만 호출, `DamageNumberActorClass`(EditDefaultsOnly)로 `AP1DamageNumberActor`를 로컬 전용으로 스폰(자세한 내용은 UI 섹션 참고). **점수판(신규)**: `ScoreboardAction`(Tab 홀드) — `JumpAction`과 동일하게 `Started`/`Completed`(+`Canceled`) 페어로 바인딩, `HandleScoreboardShow()`가 `ScoreboardWidgetInstance`를 처음 누를 때만 생성(`CreateWidget`+`AddToViewport`)하고 이후로는 Visibility만 토글(`SelfHitTestInvisible`↔`Collapsed`) + 매번 `RefreshScoreboard()`로 최신 스냅샷 다시 그림, `HandleScoreboardHide()`는 숨기기만. **매치 결과/로비 복귀(신규, 2026-08-12)**: `ClientShowMatchResult(WinningTeamId, ResultScreenDurationSeconds, LobbyMapPath)`(Client RPC) — `AP1ArenaGameMode::EndMatch()`가 전원에게 호출. `MatchResultWidgetInstance`를 최초 1회 생성(`Scoreboard`와 동일하게 `UP1ScoreboardWidgetController`를 재사용해 `SetWidgetController()`) 후 `SetWinningTeamId()`+`RefreshResult()`로 채우고 `SetInputMode(FInputModeUIOnly())`+`bShowMouseCursor=true`(매치가 끝났으니 게임 입력 불필요, 상점의 `GameAndUI`와 다르게 완전 UI 전용)로 전환, `FTimerHandle`로 `ResultScreenDurationSeconds` 후 `ReturnToLobby()` 예약. `ReturnToLobby(LobbyMapPath)`는 `ClientTravel(LobbyMapPath, TRAVEL_Absolute)` — `LobbyMapPath`에 IP:Port가 없는 순수 로컬 맵 경로만 들어있으므로(데디케이티드 서버 없는 `AP1LobbyGameMode` 레벨), 각 클라이언트가 Arena 서버 접속을 끊고 독립적으로 로컬 레벨을 로드한다 |
| `AP1LobbyPlayerController` | 완료 | PreGame 전용. Enhanced Input/GAS 관련 로직 전혀 없음 — `BeginPlay()`에서 `PreGameHUDWidgetClass`(EditDefaultsOnly)로 `UP1PreGameHUDWidget`을 생성+뷰포트 추가하고 `FInputModeUIOnly`+`bShowMouseCursor=true`로 전환하는 게 전부. **헤드리스 부하테스트 봇(신규, 2026-08-20)**: 생성자에 `UP1BotLobbyComponent`를 항상 붙여두고, `BeginPlay()`는 커맨드라인에 `-BotId=N`이 있으면(`FParse::Value`, `=`가 붙는 값 옵션이라 `FParse::Param`으론 감지 안 됨 — 반드시 `Value` 사용) `UP1PreGameHUDWidget` 생성만 건너뛴다(화면 없는 nullrhi 봇이라 UI 불필요) — 이 한 줄이 이 클래스에 남는 유일한 봇 관련 코드. 실제 로그인→히어로 선택(Greystone 고정)→매칭 큐 참가 로직은 전부 `UP1BotLobbyComponent`(`Player/P1BotLobbyComponent.h`) 안에만 있음 — `UP1BackendSubsystem`의 위젯과 분리된 공개 API(`Login`/`Signup`/`SetSelectedHeroId`/`JoinQueue`+델리게이트)를 UI 없이 그대로 재사용. `AP1PlayerController`도 동일 패턴으로 `UP1BotArenaComponent`(`Player/P1BotArenaComponent.h`)를 붙여 Arena 진입 후 정글 캠프 고정 순번 순찰+Q/기본공격 무조건 반복(쿨다운 판단은 `AbilityInputTagPressed` 내부의 `TryActivateAbility`가 자동 처리)을 담당 — 봇 행동을 전부 결정론적으로 고정한 이유는 NetSerialize/Push Model/Replication Graph 최적화 작업의 Before/After를 같은 조건에서 비교하기 위함. 런처는 `Scripts/Run_Bots.bat`(패키지드 클라이언트를 `-nullrhi -unattended -nosound -BotId=N`으로 N개 실행, Arena IP를 몰라도 됨 — 백엔드가 매칭 성사 시 알려줌) |

### Online (`Source/P1/Online/`)

| 클래스 | 상태 | 비고 |
|---|---|---|
| `UP1BackendSubsystem` | 완료 | `UGameInstanceSubsystem` — PreGame 레벨에서 웹 백엔드(`Backend/`, Spring Boot)와 통신하는 유일한 창구. GameInstance 생존주기 동안 유지되므로 `ClientTravel`로 Arena 서버에 접속한 뒤에도 살아있음(향후 매치 결과 보고 등 재사용 가능). `Signup`/`Login`(성공 시 JWT를 내부 `AuthToken`에 저장)/`JoinQueue`/`LeaveQueue` HTTP 호출(`FHttpModule`+수동 Json 직렬화, `HTTP`/`Json`/`JsonUtilities` 모듈 `P1.Build.cs`에 추가 필요)과 매칭 상태 폴링(`FTimerHandle`, 2초 간격 `GET /api/match/status`, `MatchStatusPollIntervalSeconds`)을 전담. `BackendBaseUrl`은 `UPROPERTY(Config)`(`UCLASS(Config=Game)`)라 `DefaultGame.ini`의 `[/Script/P1.P1BackendSubsystem]` 섹션에서 재컴파일 없이 주소 변경 가능. `OnSignupComplete`/`OnLoginComplete`/`OnQueueJoined`/`OnMatchFound`/`OnQueueLeft`(dynamic multicast, BlueprintAssignable) 델리게이트로 위젯에 결과 전달 — `OnMatchFound`를 받으면 `HandleMatchStatusPayload()`가 알아서 폴링 타이머를 멈춘다. `LeaveQueue()`의 응답도 `HandleMatchStatusPayload()`로 먼저 해석하는데, 이탈 요청이 서버에 도달하기 직전에 매칭이 이미 성사된 레이스 상황이면 백엔드가 이탈을 거부하고 MATCHED를 그대로 반환하기 때문 — 이 경우 `OnQueueLeft`가 아니라 `OnMatchFound`가 대신 브로드캐스트된다(curl로 이 레이스 케이스까지 검증됨). **`SelectedHeroId`(신규, 2026-08-18)**: `FName`, 기본값 `"Greystone"`이라 "미선택" 상태 자체가 없음 — `SetSelectedHeroId`/`GetSelectedHeroId`(둘 다 BlueprintCallable)로 `UP1HeroPickerWidget`이 읽고 쓴다. 순수 로컬 상태라 백엔드가 다시 돌려주지 않는다(클라가 이미 자기 선택을 알고 있으므로). 두 곳에 쓰임: (1) `JoinQueue()`가 이 값을 `{"heroId": ...}` JSON 바디로 매칭 신청에 실어 보냄(백엔드 기록용, 매칭 로직은 안 씀), (2) `UP1PreGameHUDWidget::HandleMatchFound()`가 `ClientTravel` URL에 `?HeroId=` 옵션으로 붙임(**실제로 Arena 스폰 클래스를 결정하는 건 이 경로뿐** — `AP1ArenaGameMode::InitNewPlayer` 참고) |

### Characters (`Source/P1/Characters/`)

| 클래스 | 상태 | 비고 |
|---|---|---|
| `AP1CharacterBase` | 완료 | Abstract 베이스, `IAbilitySystemInterface` + `IGenericTeamAgentInterface`, CachedASC 포인터, `IsSameTeam()` 헬퍼, `CharacterType` 태그(기본 Hero) + `IsHero()`. **`Character.Type.Boss`/`Character.Type.Minion` 태그는 안 쓰기로 해서 삭제됨**(보스/라인미니언 둘 다 최종 설계에 없음) — `IsHeroOrBoss()`도 이때 `IsHero()`로 단순화(호출부는 `UP1GameplayAbility_AssaultTheGates`뿐) |
| `AP1HeroCharacter` | 완료 | `PossessedBy`(서버)/`OnRep_PlayerState`(클라)가 부르는 `HandleAbilitySystemReady()`가 오케스트레이터 — GAS 와이어링(`InitAbilityActorInfo(PS, ASC)`, 이름 그대로 좁게 유지), 사망/코스메틱 이벤트 구독, 권위 전용 베이스스탯 적용, 무빙스피드 바인딩, 로컬 전용 UI(머리위 위젯+메인 HUD) 생성을 순서대로 실행한다. `AddDefaultAbilities()`(서버 전용, PossessedBy에서 호출)는 별도 분리 유지 — **투자 가능한 어빌리티(RMB/Q/E/R, `MaxAbilityLevel>1`)는 Level 0(미투자)으로 부여**하고 좌클릭/패시브만 기본 Level 1 유지(아래 GAS 섹션 `UP1GameplayAbility` 참고). `MovementSpeed` 어트리뷰트 바인딩. 특정 스킬/버프를 아는 코드는 없음 — `UP1BuffCosmeticEffectComponent`가 붙어있으면 ASC를 넘겨주는 범용 훅만 보유. **레벨업/스폰/리스폰 스탯**: `ApplyBaseStatsForLevel(Level, bFullHeal)`이 `DefaultAttributesEffect`를 실제 `CharacterLevel`로 재적용 후, `bFullHeal=true`(스폰/리스폰)면 신규 MaxHealth/MaxMana까지 완전 회복, `bFullHeal=false`(레벨업)면 딱 증가분(NewMax-OldMax)만큼만 현재 체력/마나를 올린다 — **레벨업이 공짜 풀힐이 되면 안 된다는 요구사항** 때문에 두 경우를 분리. `CheckLevelUp()`은 `UP1AttributeSet`이 Experience 증가를 감지해 호출 — `PS->GetXPRequiredForNextLevel()` 임계치를 넘는 동안 while로 반복 레벨업(한 번에 여러 레벨 상승 가능), 매 레벨 `AddSkillPoint()` + `ApplyBaseStatsForLevel(..., false)`. **킬/어시스트 보상**: `GrantKillReward(VictimKillStreak, VictimTimeSinceLastDeath, VictimLevel)`은 `GoldRewardKillEffectClass`(MMC `UP1MMC_GoldKillBounty` 기반 현상금)+`ChampionKillXPTable`(Row 1개짜리 CurveTable, `GetChampionXPReward()`가 `GetRowMap()`의 첫 커브를 `Eval(VictimLevel)`) 조합으로 골드/경험치 지급, `GrantAssistReward(VictimLevel)`은 `GoldRewardFlatEffectClass`(고정 `AssistGoldAmount`=50)+`ChampionAssistXPTable` 조합 — 둘 다 `UP1AttributeSet::HandleKillRewards()`에서 호출됨. **`GrantMonsterKillReward(GoldAmount, ExperienceAmount)`(신규, 2026-08-16)**: 정글 몬스터 처치 전용 — 현상금 MMC나 피해자 레벨 커브 조회 없이(몬스터 쪽 `AP1JungleMonsterCharacter::GetKillReward()`가 이미 최종값을 계산해서 넘겨줌) `GoldRewardFlatEffectClass`/`ExperienceRewardEffectClass`를 그대로 재사용하는 얇은 래퍼(어시스트와 동일한 "플랫 지급" 경로) — `UP1AttributeSet::HandleMonsterKillRewards()`에서 호출. `ApplyFlatRestoreEffect(EffectClass, SetByCallerTag, Magnitude)`가 "SetByCaller 태그 하나짜리 Instant GE를 자신에게 적용"하는 공용 헬퍼(회복/마나/골드/경험치 전부 공유). **스턴 시 진행 중 어빌리티 취소**: `OnStunTagChanged()`가 `State.Stunned` 카운트 0→양수 전이(서버)를 감지하면 `CancelActiveAbilitiesOnStun()`을 호출 — `ASC->GetActivatableAbilities()`를 순회해 `Spec.IsActive() && Ability->OwnsActivationTag(State.Attacking)`인 스펙만 `CancelAbilityHandle()`로 취소한다(`CancelAbilities()`는 AssetTag만 매칭하고 `State.Attacking`은 `ActivationOwnedTags`라 안 맞으므로 직접 순회 필요 — `UP1GameplayAbility::OwnsActivationTag()`가 이 목적의 얇은 접근자). StoicismDeflect/Vitality처럼 생성자에서 `State.Attacking`을 명시적으로 뺀 반응형 패시브는 이 루프에서 자연히 제외됨. **이동 불가 CC 범용 디스패치(신규, 2026-08-17)**: `OnImmobilizedTagChanged()`는 `OnStunTagChanged()`와 별개 리스너(관심사 분리 — 스턴 전용 연출/취소 로직과 안 섞임)로, `State.Immobilized`(스턴/에어본/속박 등을 아우르는 범용 CC 카테고리 태그, CC GE가 자기 고유 태그와 함께 이 태그도 부여해야 함) 카운트 0→양수 전이(서버) 시 `Event.Character.Immobilized`를 자신의 ASC로 보낸다 — `Event.Character.BasicAttackHitDealt`와 동일한 "발신자는 어떤 아이템이 반응하는지 모른다" 원칙(사면(아이템) "용기"가 첫 사용처, `UP1GameplayAbility_Item_Bravery` 참고). 지금은 스턴 GE만 `State.Immobilized`를 부여하지만, 나중에 에어본/속박이 추가돼도 그 GE들에 이 태그만 얹으면 되고 이 코드는 전혀 안 바뀐다 |
| `AP1MinionCharacter` | 미구현 (설계상 영구 스킵) | 라인 미니언 없는 게임으로 최종 설계 변경돼 앞으로도 안 만듦 — `Character.Type.Minion` 태그는 이 때문에 여전히 미사용(고아) 상태로 남아있음 |
| `AP1JungleMonsterCharacter` (`Characters/`) | **C++ 뼈대 완료 (에셋/밸런싱 대기)** | 정글 몬스터(중립 캠프). `AP1CharacterBase`를 직접 상속하고 ASC/AttributeSet을 **PlayerState가 아니라 Pawn 자신**이 호스팅(`AP1CharacterBase.h`가 예고해둔 패턴 그대로) — `SetReplicationMode(Full)`(사람이 조종하지 않아 "소유 클라" 개념이 없으므로 Mixed 대신 전원에 GE까지 복제, 안 그러면 스턴바와 같은 종류의 프록시 버그가 재현됨). `TeamId`는 베이스 기본값 255(NoTeam) 그대로 둬서 `IsSameTeam()`이 항상 false → 별도 설정 없이 전 영웅에게 자동 적대. `CharacterType=Character.Type.Monster`(신규 태그). `HomeLocation`(BeginPlay 시점 스폰 위치, `SetHomeLocation()`으로 나중에 캠프 스포너가 덮어쓸 수 있음)/`LeashRadius`. **`EnterLeashRecovery()`/`ExitLeashRecovery()`** — 리시 복귀 시작/종료: `ResetInvulnerabilityEffectClass`(선택, 미설정 시 무적 없이 회복만 진행 — 조용한 폴백)로 `State.Invulnerable` 부여 + `Tick()`에서 `LeashRecoveryHealPercentPerSecond`(초당 최대체력 비율)만큼 램프 회복, 종료 시 무적 해제+즉시 풀피 확정. **`RequestMeleeAttack()`** — `Event.Monster.MeleeAttack` GameplayEvent 발신만 담당(쿨다운 판정은 어빌리티 쪽 위임). **`OnHitReactEventReceived()`** — `Event.Character.HitReact`(신규로 Instigator 필드가 채워짐, 아래 AttributeSet 참고) 구독, Instigator가 `AP1PlayerState`면 `GetPawn()`으로 한 번 더 풀고(히어로가 가한 데미지의 표준 패턴) `AP1JungleMonsterAIController::NotifyAggro()` 호출. 머리 위 HP 바는 히어로와 동일 초기화지만 `IsLocallyControlled()` 숨김 분기 없음(항상 노출). **사망 몽타주(신규)**: `OnDiedEventReceived()`가 먼저 컨트롤러의 `StopMovement()`+`BrainComponent->StopLogic()`+`CharacterMovementComponent->DisableMovement()`로 BT/이동을 멈춰 시체가 계속 공격/추적하지 않게 한 뒤, `DeathMontage` 설정 시 `MulticastPlayDeathMontage()`(NetMulticast — 이 캐릭터는 소유 클라이언트가 없어 raw `Montage_Play`가 서버 화면에만 보이므로 Multicast 필수)로 전 클라이언트에 재생하고 `SetLifeSpan(DeathDestroyDelay)`(기본 2초)로 파괴를 늦춘다(미설정 시 몽타주 없이 즉시 `Destroy()`). `OnMonsterDied` 델리게이트는 파괴 시점이 아니라 **죽는 즉시** 브로드캐스트(리스폰 대기시간은 죽는 순간부터 카운트되는 게 자연스러움) — 히어로처럼 ragdoll은 하지 않음(필요해지면 `Event.Montage.Death.Impact` 노티파이 패턴을 그대로 재사용 가능, 아래 알려진 버그의 덱커 사망 래그돌 항목 참고). **처치 보상(신규, 2026-08-16)**: `RewardDataTable`(`Data/DT_MonsterGoldXP.json` 임포트, Row Struct=`FP1MonsterRewardData`, 새 헤더 `Characters/P1JungleMonsterTypes.h`)+`MonsterTypeName`(조회할 Row 이름, 예: `"Wolves"`)+`RewardMultiplierTable`(`Data/CT_MonsterRewardMultiplier.json` 임포트, Row="MonsterRewardMultiplier", Time=MonsterLevel) — `GetKillReward(OutGold, OutExperience)`가 `BaseGold/BaseExperience × 배율(MonsterLevel)`로 최종 지급량을 계산해 반환한다(둘 다 셋 중 하나라도 미설정/Row 없음이면 0 반환, 경고 로그만 남기고 조용히 폴백). `UP1AttributeSet::HandleMonsterKillRewards()`가 사망 시점에 이 값을 조회해 킬러에게 지급 |
| `UP1BuffCosmeticEffectComponent` | 완료 | 자세한 내용은 Ability System 섹션 참고(GAS 태그 이벤트에 강하게 의존) — "버프 태그 활성 동안 코스메틱 유지, 소멸 시 원복"을 데이터로 구성하는 범용 `UPawnComponent`, 필요한 영웅에만 추가 |

### Ability System (`Source/P1/AbilitySystem/`)

**`Abilities/` 하위 폴더 구조**: 여러 영웅이 공유하는 베이스(`P1DamageGameplayAbility`, `P1GameplayAbility_MeleeAttack`)는 `Abilities/` 바로 밑에 두고, 특정 영웅 전용 어빌리티(현재는 그레이스톤의 RMB/Q/E/R/기본공격 변종/패시브 전부)는 `Abilities/Greystone/`처럼 영웅별 하위 폴더로 분리한다. **이 파일 이동은 블루프린트에 전혀 영향을 주지 않는다** — 블루프린트의 부모 클래스 참조는 `Source/` 안의 파일 경로가 아니라 컴파일된 리플렉션 경로(`/Script/모듈이름.클래스이름`)로 결정되므로, 클래스 이름과 모듈이 그대로면 파일을 어디로 옮기든 안전하다(클래스 "이름"을 바꾸는 것과는 다른 얘기 — 그건 Core Redirects가 필요함). 파일 이동 시 실제로 해야 하는 건: (1) 그 파일을 include하는 다른 `.cpp`의 include 경로 갱신, (2) `.uproject` 우클릭 → Generate Visual Studio project files로 프로젝트 파일 재생성.

| 클래스 | 상태 | 비고 |
|---|---|---|
| `UP1AbilitySystemComponent` | 완료 | InputTag → Ability press/release 라우팅. `bReplicateInputDirectly` 서버 전파. 조준 중(`State.TargetingAbility`) LMB=confirm/RMB=cancel 리라우팅 + 나머지 차단. **`ServerInvestSkillPoint(AbilityInputTag)`**(Server RPC, WithValidation) — `AP1PlayerController::HandleAbilityInputPressed`의 Ctrl+스킬키 경로가 호출. `GetActivatableAbilities()`에서 `DynamicSpecSourceTags`로 스펙을 찾아 (1) `PS->GetSkillPoints()>0`, (2) `Spec.Level < MaxAbilityLevel`, (3) `PS->GetCharacterLevel() >= Ability->GetRequiredCharacterLevelForNextRank(Spec.Level)`(R의 6/11/15 레벨 게이트) 셋 다 서버에서 재검증 후 `Spec.Level += 1` + `MarkAbilitySpecDirty` + `PS->SpendSkillPoint()` — 클라이언트 UI(투자 버튼 표시 여부)는 힌트일 뿐 신뢰하지 않는다. **`ReduceCooldownByInputTag(InputTag, Percent)`(신규, 2026-08-16)**: 특정 InputTag(Q/E/RMB 등)에 바인딩된 어빌리티의 남은 쿨다운을 퍼센트만큼 줄이는 범용 유틸 — `AssaultTheGates::ReduceCooldown()`(자기 자신 전용)의 "남은시간 재계산 후 제거+재적용" 방식을 여러 어빌리티에 재사용 가능하게 일반화했다. `GetActivatableAbilities()`에서 InputTag로 스펙을 찾고 `Spec.Ability->GetCooldownTags()`(그 어빌리티의 실제 쿨다운 태그, 엔진이 이미 `CooldownGameplayEffectClass`의 CDO에서 추출해주는 표준 API)로 `GetActiveEffectsTimeRemaining()` 조회 → 쿨다운 중이 아니면 조용히 무시 → 쿨다운 중이면 `RemoveActiveEffectsWithGrantedTags()`로 기존 것 제거 후, **그 어빌리티 전용 쿨다운 GE를 재적용하는 대신** 공용 `GenericCooldownReductionEffectClass`(EditDefaultsOnly, Duration=SetByCaller `Data.CooldownDuration`, 고정 Granted Tags 없음)에 `SpecHandle.Data->DynamicGrantedTags`로 그 어빌리티의 쿨다운 태그를 런타임에 실어 적용 — 이 덕분에 Q/E/RMB 각각의 쿨다운 GE 에셋을 전혀 안 건드리고도(SetByCaller로 바꿀 필요 없이) 어떤 어빌리티든 쿨다운 감소가 가능하다. 애쉬브링어(아이템) "크로노 스트라이크"가 첫 사용처였으나(2026-08-17 원작 툴팁 재확인으로 애쉬브링어에서는 제거됨, 아래 `UP1GameplayAbility_Item_ChronoStrike` 행 참고) 이 유틸 자체는 지금 **괴사(아이템)의 AbilityHaste/UltimateHaste 쿨다운 감소 시스템**(`UP1GameplayAbility::ApplyAbilityHasteToCommittedCooldown`)이 실사용 중 |
| `UP1AttributeSet` | 완료 | 아래 어트리뷰트 전체 구현 — 리플리케이션, 클램핑 포함. `PostGameplayEffectExecute`가 Damage 메타 어트리뷰트를 Health로 변환하기 직전 두 가지를 순서대로 체크: (1) `State.Invulnerable` 태그가 있으면 데미지를 완전히 무시(전 어빌리티 공유, Stone Forged Soul 스테이시스가 첫 사용처), (2) 이 데미지가 기본공격에서 왔고(`Data.EffectSpec.CapturedSourceTags`에서 `Ability.BasicAttack` 확인) 캐릭터가 Stoicism 디플렉트를 갖고 있으며(`Ability.StoicismDeflect` 루즈 태그) 쿨다운 중이 아니면(`Cooldown.Ability.StoicismDeflect` 부재) 데미지를 무효화하고 `Event.StoicismDeflect.Consumed` 이벤트를 보내 해당 어빌리티가 자기 쿨다운을 커밋하게 함 — **AttributeSet은 Stoicism 어빌리티 클래스를 전혀 참조하지 않고 태그만으로 전부 판별**. **킬/어시스트 감지**: `RecordDamageContribution()`이 데미지를 준 시점마다 `TMap<TWeakObjectPtr<AP1PlayerState>, float>`(기여자→마지막 기여 시각)에 기록해두고, `HandleKillRewards()`가 Health<=0 감지 시 `Data.EffectSpec.GetEffectContext().GetInstigator()`로 킬러를, 이 맵에서 10초(`AssistWindowSeconds`) 이내 기여자 전원을 어시스터로 판별해 각각 `AP1HeroCharacter::GrantKillReward()`/`GrantAssistReward()`를 호출한다(피해자의 죽기 직전 KillStreak/생존시간/CharacterLevel을 `VictimPS->AddDeath()` 호출 **이전에** 먼저 캡처 — 리셋되기 전 값이 필요). AttributeSet은 이때도 `AP1HeroCharacter`를 직접 캐스트해 호출하지만, 이건 "어빌리티 클래스 참조 금지" 원칙과 무관한 별개 카테고리(Character 클래스 참조는 허용, Ability 클래스 참조만 금지) — 킬 보상은 전 영웅 공통 로직이라 어차피 per-ability 유연성이 필요 없기도 함. `Experience` 어트리뷰트 변경 시 `Hero->CheckLevelUp()`을 호출해 레벨업 체크는 캐릭터 클래스에 위임(AttributeSet은 XP 커브테이블을 몰라도 됨). **데미지 팝업(신규)**: 데미지가 실제로 적용된 직후(무적/디플렉트 무효화 분기 통과) `NotifyDamageDealt(Data, LocalDamage)` 호출 — `Data.EffectSpec.GetEffectContext().GetInstigator()`(=가해자 `AP1PlayerState`, `RecordDamageContribution`과 동일한 조회) → `Cast<AP1PlayerController>(InstigatorPS->GetOwner())`(PlayerState의 Owner=소유 컨트롤러라는 표준 UE 소유권 규약)로 가해자의 PlayerController를 찾아 `ClientShowDamageNumber()` Client RPC 호출. **정글 몬스터 어그로 훅(신규)**: 생존 시 보내는 `Event.Character.HitReact`에 `Data.EffectSpec.GetEffectContext().GetInstigator()`를 `HitReactEventData.Instigator`로 실어 보내도록 한 줄 추가 — 이 이벤트를 구독하는 `AP1JungleMonsterCharacter::OnHitReactEventReceived()`가 이 값으로 공격자를 resolve해 AIController에 어그로를 통지한다(자세한 내용은 위 Characters 섹션 및 아래 AI 섹션 참고). **정글 몬스터 킬 보상(신규, 2026-08-16)**: `HandleKillRewards()`는 `VictimPS`(피해자의 ASC OwnerActor를 `AP1PlayerState`로 캐스트)가 null이면 조용히 리턴만 하고 있었는데 — 정글 몬스터는 ASC를 PlayerState가 아니라 Pawn 자신이 들고 있어 항상 이 캐스트가 실패해서, 몬스터를 죽여도 킬/어시스트 집계뿐 아니라 **골드/경험치 지급까지 통째로 누락되고 있었다**(실제로 겪은 버그). `VictimPS`가 null이면 이제 `HandleMonsterKillRewards(Data, VictimOwner)`로 위임 — `Cast<AP1JungleMonsterCharacter>(VictimOwner)`가 성공하면 `VictimMonster->GetKillReward()`(아래 Characters 섹션 참고)로 계산된 골드/경험치를 킬러의 `AP1HeroCharacter::GrantMonsterKillReward()`에 그대로 전달한다. 킬/데스/어시스트/팀킬스코어 집계는 원래도 몬스터 킬에 안 섞여야 하는 게 맞아 그대로 유지(이 함수는 별도 경로라 `HandleKillRewards()`의 KDA 로직을 전혀 안 건드림). **온-히트 아이템 어빌리티 범용 디스패치(2026-08-17 리팩터링)**: 처음엔 크로노 스트라이크/매너스 둘 다 `HandleChronoStrikeProc`/`HandleMenaceProc`라는 아이템별 전용 메서드로 AttributeSet에 직접 구현했었는데, ~30개 아이템을 계획 중인 상황에서 이 방식은 아이템이 늘어날수록 AttributeSet이 계속 커지는 클린코드 위반이라는 지적을 받아 리팩터링했다. 지금은 `DispatchBasicAttackHitDealt(Data, bWasCritical)` 하나만 있다 — 데미지 적용 직후(무적/디플렉트 통과, 생사와 무관하게 항상) 호출되어, 이 데미지가 기본 공격에서 왔으면(`CapturedSourceTags`의 `Ability.BasicAttack`) 가해자(Instigator)의 ASC로 **`Event.Character.BasicAttackHitDealt`** GameplayEvent를 보낸다(`FGameplayEventData::Target`=피격자 액터, `EventMagnitude`=크리티컬 여부 1.0/0.0). **AttributeSet은 이 이벤트를 누가 구독하는지 전혀 모른다** — 어떤 아이템이 반응할지는 그 아이템 구매 시 부여되는 `UP1GameplayAbility_OnHitItemAbility` 파생 어빌리티가 `AbilityTriggers`로 알아서 구독한다(아래 표 참고). 아이템 보유 여부는 "존재 확인용 GE+루즈 태그" 트릭 대신 **어빌리티 부여 자체**로 표현한다(`ASC->GiveAbility()`/`ClearAbility()`, `AP1PlayerState::ServerBuyItem`/`ServerSellItem` 참고) — 어빌리티가 `GetActivatableAbilities()`에 있다는 사실 자체가 "이 아이템을 갖고 있다"는 신호다. `bWasCritical`은 `CriticalHitFlag` 메타 어트리뷰트(아래 참고)를 `Damage`와 동일한 "즉시 읽고 리셋" 패턴으로 읽어서 얻는다 |
| `UP1MMC_GoldKillBounty` | 완료 | `GE_GoldReward_Kill`의 Gold Modifier 계산 클래스(`UGameplayModMagnitudeCalculation` 상속). `Data.KillStreak`/`Data.TimeSinceLastDeath`를 `Spec.GetSetByCallerMagnitude(Tag, bWarnIfNotFound=false, Default=0.f)`로 읽어 `BaseKillGold`(기본 300) + `KillStreakBonusTable`(CurveTable, Row="KillStreakBonusGold") 조회값 + `GoldPerSecondAlive`(초당 2, `MaxTimeAliveBonus`=200에서 캡) 선형 가산을 합산해 최종 골드를 반환 — 오래 살아있었거나 연속킬 중인 상대를 잡으면 더 많은 골드를 주는 현상금 시스템. **GE의 Custom Calculation Class는 런타임에 해당 클래스의 CDO를 그대로 쓰므로, `KillStreakBonusTable` 같은 에셋 참조 프로퍼티는 이 네이티브 C++ 클래스가 아니라 이를 상속하는 블루프린트(예: `MMC_GoldKillBounty`)의 Class Defaults에서 지정해야 한다** — GE 에셋 자체에는 SetByCaller 관련 설정이 전혀 없고, 값은 전부 `AP1HeroCharacter::GrantKillReward()`가 스펙에 미리 심어둔다 |
| `UP1MMC_StackCountMagnitude` / `UP1MMC_StackThresholdMagnitude` (신규, 2026-08-17) | 완료 | 스택형 버프 GE의 Modifier Magnitude를 `FGameplayEffectSpec::GetStackCount()` 기반으로 계산하는 범용 MMC 두 종 — 더스트 데빌(아이템) "매너스"가 첫 사용처지만 특정 아이템에 종속되지 않은 범용 이름으로 만들어 다른 스택형 아이템에도 재사용 가능. **`StackCountMagnitude`**(선형): `MagnitudePerStack × 현재 스택 수`(예: 공격속도 스택 3%×N). **`StackThresholdMagnitude`**(임계값): `스택 수 >= RequiredStackCount`면 `BonusMagnitude`, 아니면 `BelowThresholdMagnitude`를 반환(예: 6스택 도달 시에만 이동속도 보너스) — **`BelowThresholdMagnitude`는 Modifier Op에 맞춰 반드시 지정해야 한다: Additive면 0.0(기본값), Multiply면 반드시 1.0**(0으로 두면 곱셈이라 스탯 전체가 0으로 사라지는 버그 — 실제로 설계 중 발견해서 고침). 둘 다 다른 MMC와 동일하게 네이티브 클래스 CDO에 값을 못 넣으므로 BP 서브클래스(예: `MMC_DustDevil_AttackSpeedPerStack`)에서 값을 지정해야 한다 |
| `UP1GameplayAbility_OnHitItemAbility` (`Abilities/`, 신규 2026-08-17) | 완료 | 온-히트 반응형 아이템 어빌리티 전부의 공용 abstract 베이스 — `Event.Character.BasicAttackHitDealt`(`AbilityTriggers`)로만 트리거되고 입력 태그는 없다. 생성자는 StoicismDeflect와 동일한 이유로 `State.Attacking` 상호배타 규칙을 `RemoveTag`로 빼고(다른 스킬 사용 중에도 항상 반응해야 함) `NetExecutionPolicy=ServerOnly`(예측할 소유 클라이언트 없음). `ActivateAbility()`가 `TriggerEventData->Target`(피격자)/`EventMagnitude`(크리티컬 여부)를 풀어 순수 가상 `OnBasicAttackHitDealt(AActor* Target, bool bWasCritical)`를 호출한 뒤 즉시 `EndAbility` — 실제 반응 로직은 이 훅 하나만 오버라이드하면 된다. `Abilities/Items/` 폴더에 이 클래스를 상속하는 구체 아이템 어빌리티들을 둔다(Greystone/Dekker 영웅별 폴더와 동일한 구조 원칙). **베이스가 `UP1GameplayAbility`에서 `UP1DamageGameplayAbility`로 바뀜(2026-08-17, 진실의 일격 추가 시)** — 온-히트 반응이 버프/쿨감뿐 아니라 "추가 데미지"인 경우(주문검류)도 대비해 `ApplyDamageToTarget()`을 공용으로 물려받게 함. 크로노 스트라이크/매너스는 이 기능을 안 쓰므로 영향 없음(순수 추가, 하위 호환) |
| `UP1GameplayAbility_Item_ChronoStrike` (`Abilities/Items/`, 신규 2026-08-17) | 완료 | 애쉬브링어 고유효과 "크로노 스트라이크" — `HeroCDRPercent`(기본 0.08)/`MonsterCDRPercent`(기본 0.04, 둘 다 EditDefaultsOnly)를 대상 유형(영웅/정글 몬스터, `Cast<AP1PlayerState>(Target)` 성공 여부로 판별 — `HandleKillRewards`/`HandleMonsterKillRewards`와 동일한 판별 방식)에 따라 골라 `UP1AbilitySystemComponent::ReduceCooldownByInputTag()`를 Q/E/RMB 세 번 호출 |
| `UP1GameplayAbility_Item_Menace` (`Abilities/Items/`, 신규 2026-08-17) | 완료 | 더스트 데빌 고유효과 "매너스" — 대상이 **영웅일 때만**(정글 몬스터 제외, `Cast<AP1PlayerState>(Target)`) 발동해 `MenaceBuffEffectClass`(EditDefaultsOnly, 스택형 버프 GE, `UP1MMC_StackCountMagnitude`/`UP1MMC_StackThresholdMagnitude` 참고)를 자신에게 적용. `bWasCritical`이면 `FGameplayEffectSpec::SetStackCount(2)`로 2스택 요청(평소 1스택) — GAS가 기존 활성 스택에 자동으로 더해준다(Stack Limit에서 클램프) |
| `UP1GameplayAbility_Item_TrueStrike` (`Abilities/Items/`, 신규 2026-08-17) | 완료 | 증강 고유효과 "진실의 일격" — 롤 "주문검"류(Sheen/Spellblade) 2단 메커니즘, 완전히 다른 두 트리거를 한 클래스에서 처리. **"부여"**: `OnGiveAbility()`가 `ASC->AbilityActivatedCallbacks`를 구독(RocketBoots의 "다른 스킬 사용 시 쿨감"과 동일 패턴) — `OnAnyAbilityActivated()`가 활성화된 어빌리티의 `InputTag`가 Q/E/RMB/R 중 하나인지 확인(기본공격/패시브/InputTag 없는 아이템 반응형 어빌리티는 자연히 제외)하고, 자기 쿨다운(`GetCooldownTags()`+`ASC->HasAnyMatchingGameplayTags()`로 수동 확인 — 이 경로는 `ActivateAbility`를 안 거쳐 엔진 자동 게이팅을 못 받음)이 비어있으면 `EmpowerBuffEffectClass`(4초 마커 버프, `Buff.TrueStrike.Empowered`)를 적용하고 자기 쿨다운(1.5초)을 커밋. 이 경로는 정식 활성화 생명주기를 전혀 안 거치는 순수 사이드이펙트. **"소모"**: 베이스 `UP1GameplayAbility_OnHitItemAbility`의 기존 `Event.Character.BasicAttackHitDealt` 트리거를 그대로 재사용 — `OnBasicAttackHitDealt()`가 `Buff.TrueStrike.Empowered` 태그 확인 후 `ApplyDamageToTarget()`으로 고정 피해(`bIsTrueDamage=true`, `PhysicalPowerCoefficient=0.90`)를 추가로 입히고 `RemoveActiveEffectsWithGrantedTags`로 버프 제거. **`EmpowerEffect`/`ConsumeEffect`(신규, 2026-08-17, 수정 2026-08-18)**: 서로 독립된 프로퍼티라 하나만 설정해도 되고 둘 다 생략해도 됨. `ConsumeEffect`(적중 순간 1회성 버스트)는 그대로 `MulticastPlayNiagaraEffect()`. **`EmpowerEffect`(양손 발광)는 1회성이 아니라 지속형** — `OnGiveAbility()`가 `ASC->AbilityActivatedCallbacks` 구독과 별개로 `Buff.TrueStrike.Empowered` 태그를 `RegisterGameplayTagEvent(NewOrRemoved)`로 구독해두고, `OnEmpoweredTagChanged(NewCount)`가 0→양수면 양손 소켓 각각 `MulticastSetAttachedNiagaraEffect()`로 시작, 양수→0이면 `MulticastStopAttachedNiagaraEffect()`로 중지한다. **애초에 `MulticastPlayNiagaraEffect()`(1회성)로 만들었다가 이펙트가 버프 종료 후에도 영구히 안 사라지는 버그가 있었음** — 원인: 지속 버프용 손 발광 Niagara 에셋은 보통 Looping으로 만들어지는데, 1회성 함수는 `bAutoDestroy=true`로 스폰해 시스템이 "완료" 상태에 도달해야 자동 파괴되지만 Looping 시스템은 스스로 절대 완료되지 않아 트리거(소모)든 자연만료든 상관없이 계속 남아있었다. 태그 이벤트 구독으로 바꾸면 "소모"(`RemoveActiveEffectsWithGrantedTags`)든 GE의 4초 자연만료든 결국 같은 태그 소멸로 귀결되므로 원인과 무관하게 한 곳에서 정확히 한 번 반응(Sacred Oath 검 발광과 동일한 근거, 아래 알려진 버그 표 "코스메틱 효과 되돌리기를 태그 존재 여부로 판단" 항목 참고) |
| `UP1GameplayAbility_Item_Bravery` (`Abilities/Items/`, 신규 2026-08-17) | 완료 | 사면(아이템) 고유효과 "용기" — 기존 온-히트 아이템들(`UP1GameplayAbility_OnHitItemAbility` 파생)과 다른 완전히 새로운 반응 축: **이동 불가 CC에 걸리는 순간** 발동. `Event.Character.Immobilized`(`AbilityTriggers`, `AP1HeroCharacter::OnImmobilizedTagChanged` 발신)로 트리거되고, `BuffEffectClass`(EditDefaultsOnly, 3초짜리 `DamageReduction`+`MovementSpeed` 버프 GE)를 자신에게 적용한 뒤 `CooldownGameplayEffectClass`(45초, SetByCaller 불필요 — 캐릭터 레벨 스케일링 없는 고정값)를 커밋. **생성자에서 `ActivationBlockedTags.RemoveTag(TAG_State_Stunned)`가 필수** — 베이스 `UP1GameplayAbility`가 기본으로 스턴 중 전 어빌리티 발동을 막는데, 이 어빌리티는 정확히 "스턴에 걸리는 순간" 발동해야 하므로 이 예외 처리가 없으면 정작 필요한 순간에 막혀서 절대 발동 안 함(다른 반응형 패시브들이 `State.Attacking`만 빼는 것과 달리, 이건 `State.Stunned`까지 빼야 하는 유일한 케이스). "지금 사용 가능한지"는 발신자가 체크하지 않고(무조건 이벤트 발신) 이 어빌리티 자신의 쿨다운 태그를 엔진의 표준 `CanActivateAbility()`가 자동으로 걸러준다 — StoicismDeflect(발신자가 미리 쿨다운 체크)와 반대 방향. **`ShieldEffect`(신규, 2026-08-17)**: 버프 적용 직후 `AP1CharacterBase::MulticastPlayNiagaraEffect()`로 1회 재생(미설정 시 생략) |
| `UP1GameplayTags` | 완료 | Native 태그: `InputTag.Ability.BasicAttack/RMB/E`, `Ability.BasicAttack/AssaultTheGates/SacredOath`, `State.Attacking`, `Event.Montage.*`, `Data.DamageMultiplier`, `Data.Damage.Flat/PhysicalPower/TargetMaxHealthPct`, `Data.DebuffMagnitude`(범용 디버프 크기 채널), `Character.Type.Hero/Monster`(`Boss`/`Minion`은 미사용이라 삭제됨) |
| `UP1GameplayAbility` (베이스) | 완료 | `InstancedPerActor` + `LocalPredicted` + `bReplicateInputDirectly=true` 기본값, `GetP1Character/PlayerController` 헬퍼, `ApplyEffectToSelf(EffectClass, SetByCallerTag, Magnitude)` — "자신에게 GE 적용" 공통 헬퍼 (버프/디버프/쿨다운 등 전 어빌리티 공유). **`ActivationOwnedTags`/`ActivationBlockedTags`에 `State.Attacking`을 전 어빌리티 공통으로 부여/차단 — 게임 전역 규칙("스킬 사용 중엔 다른 어빌리티 발동 불가", WASD 이동은 어빌리티가 아니라 예외)을 베이스 하나로 처리, 개별 어빌리티마다 반복 설정 불필요**. `bActivateOnGranted`(기본 false) — true면 `AP1HeroCharacter::AddDefaultAbilities()`가 `GiveAbility` 대신 `GiveAbilityAndActivateOnce`로 부여해 그 자리에서 1회 활성화한다. **입력도 트리거 이벤트도 없이 부여되는 즉시 스스로 계속 돌아야 하는 상시 패시브에 필수**(`GiveAbility`만으로는 `ActivateAbility`가 절대 호출되지 않아 영원히 잠들어있게 됨 — Stoicism Vitality에서 처음 발견된 문제, `GA_Lightbringer`처럼 입력 바인딩이 있거나 `GA_StoicismDeflect`처럼 GameplayEvent로 트리거되는 어빌리티는 이 플래그가 필요 없음). **스킬 포인트 투자 시스템(신규)**: `MaxAbilityLevel`(기본 1=투자 불가 — 좌클릭/패시브는 이 기본값 그대로, RMB/Q/E는 5, R은 3으로 BP에서 설정)과 `RequiredCharacterLevelPerRank`(TArray, 인덱스 i=스펙레벨을 i+1로 올리는 데 필요한 최소 캐릭터 레벨 — RMB/Q/E는 빈 배열(제한없음), R은 `{6,11,15}`) + `GetRequiredCharacterLevelForNextRank()`. **`CanActivateAbility()` 오버라이드** — `MaxAbilityLevel>1`인데 `Spec.Level<=0`(미투자)이면 발동 자체를 막아 "1레벨엔 좌클릭/패시브만 쓸 수 있고 나머지는 첫 포인트를 투자해야 열림"을 구현. 좌클릭/패시브는 이 체크를 완전히 건너뛰어 영향 없음. Q/E/RMB 중 "하나만 먼저 선택"하는 요구사항은 별도 배타 로직 없이 자연히 해결됨 — 1레벨엔 포인트가 1개뿐이라 셋 중 하나에만 투자할 수 있는 게 그 자체로 택1이기 때문. **`ApplyCooldown()` 오버라이드 + `ApplyEffectToSelf()` 훅(신규, 2026-08-17)** — AbilityHaste/UltimateHaste 쿨다운 감소를 전 어빌리티에 자동 적용(표준 커밋 경로와 이 프로젝트의 수동 커밋 관례 양쪽 다 커버). 자세한 내용은 아래 어트리뷰트 목록 밑 "AbilityHaste/UltimateHaste 쿨다운 감소 시스템" 설명 참고 |
| `UP1DamageGameplayAbility` | 완료 | 데미지 어빌리티 공통 베이스 — `DamageEffectClass` + `FlatDamage`(`FScalableFloat`, 레벨 스케일링)/`PhysicalPowerCoefficient`/`MagicalPowerCoefficient`/`TargetMaxHealthPctCoefficient`/`SourceMaxHealthPctCoefficient` + `ApplyDamageToTarget()` (계수 채널 + 호출 시점 Bonus 파라미터로 버프성 추가 데미지 지원) + `GetEnemiesInRadius(Center, Radius, HalfHeight)` (구 오버랩+팀+높이 필터 공용 헬퍼, 방향성 필터는 호출자가 추가). **`ApplyDamageToTarget`은 raw `ASC->MakeOutgoingSpec`을 쓰기 때문에(어빌리티 레벨 `MakeOutgoingGameplayEffectSpec`과 달리) 스펙에 어빌리티 Asset Tags가 자동으로 안 실린다 — `SpecHandle.Data->CapturedSourceTags.GetSpecTags().AppendTags(GetAssetTags())`로 수동으로 채워준다.** "이 데미지가 어떤 어빌리티에서 왔는지"를 타겟 쪽(AttributeSet 등)에서 판별해야 하는 모든 경우(Stoicism 디플렉트가 첫 사용처)에 필요. **`bIsTrueDamage`(신규, 2026-08-17)**: true면 `ApplyDamageToTarget`이 `Data.DamageType.Physical`/`Magical` 대신 `Data.DamageType.True`를 부여 — `P1ExecCalc_Damage`가 이 태그를 보고 방어력 감산을 완전히 건너뛴다(관통과 무관한 별개 축, "고정 피해"). 증강(아이템) "진실의 일격"이 첫 사용처 |
| `UP1ExecCalc_Damage` | 완료 | 물리/마법 혼합 데미지 ExecutionCalculation. 계수 **채널 합산**: `Raw=(Flat + PhysCoeff*Source.PhysicalPower + MagCoeff*Source.MagicalPower + TargetMaxHPCoeff*Target.MaxHealth + SourceMaxHPCoeff*Source.MaxHealth)`, `Pre=Raw*Mult` × 방어 감산(`Armor/(Armor+100)`, Penetration flat 차감) → `Damage` 메타. Source.MaxHealth와 Target.MaxHealth는 같은 어트리뷰트를 서로 다른 Capture(Source/Target)로 캡처해야 해서, `DEFINE_ATTRIBUTE_CAPTUREDEF` 매크로 대신 `SourceMaxHealthDef`는 `FGameplayEffectAttributeCaptureDefinition` 생성자를 직접 호출해 구성(매크로는 `GET_MEMBER_NAME_CHECKED`로 프로퍼티 실명을 요구해 같은 이름을 두 캡처에 재사용 불가). **크리티컬 판정 결과도 `CriticalHitFlag` 메타 어트리뷰트로 함께 출력한다(신규, 2026-08-17)** — `Damage`는 Additive(여러 히트 누적)지만 이건 Override(이번 히트 값 그대로)로 출력, 온-히트 크리티컬 반응 아이템(더스트 데빌 등)이 소비. **`DamageReduction`도 함께 소비한다(신규, 2026-08-17)** — Target에서 캡처(non-snapshot, 최신값), 방어 감산(`PassThrough`)까지 끝난 값에 `×(1-DamageReduction)`로 최종 승산(관통으로 무효화 안 되는 별개 축, 사면(아이템) "용기"가 첫 사용처). **`Data.DamageType.True`도 읽는다(신규, 2026-08-17)** — `CapturedSourceTags`에 이 태그가 있으면 `PassThrough=1.0`으로 고정해 방어 감산 자체를 건너뛴다("고정 피해", `UP1DamageGameplayAbility::bIsTrueDamage=true`인 어빌리티가 부여 — 증강(아이템) "진실의 일격"이 첫 사용처). `DamageReduction`은 이 경우에도 그대로 적용됨(방어력만 무시, 전체 방어 시스템 무시 아님). 미래: damage-type 태그로 물리/마법 방어(PhysicalArmor vs MagicalArmor) 선택 |
| `UP1GameplayAbility_JungleMonsterMeleeAttack` (`Abilities/`) | **완료** | 정글 몬스터 근접 공격 — `UP1DamageGameplayAbility` 직속 상속, 입력 없이 `Event.Monster.MeleeAttack`(신규 태그) GameplayEvent로만 트리거(`AbilityTriggers`, StoicismDeflect와 동일한 이벤트-트리거 패턴). 콤보/몽타주 없이 `ActivateAbility`에서 즉시 `GetEnemiesInRadius`+`ApplyDamageToTarget` 판정 후 종료. 재사용 대기시간은 새 매커니즘 없이 베이스 `CooldownGameplayEffectClass`(에디터 설정) 표준 커밋 그대로 사용 — 쿨다운 중엔 이벤트가 와도 `CanActivateAbility`가 자동으로 막아준다. `SetAssetTags`에 `Ability.BasicAttack`을 의도적으로 재사용(MeleeAttack/RangedAttack과 동일) — Stoicism 디플렉트가 몬스터 공격도 인식해서 막을 수 있어야 하므로. `NetExecutionPolicy=ServerOnly`(예측할 소유 클라이언트가 없음). **`AttackMontage`(신규, 선택)**: 설정 시 판정과 동시에 `ASC->PlayMontage(this, ActivationInfo, AttackMontage, 1.0f)`로 재생 — raw `AnimInstance->Montage_Play`가 아니라 반드시 이 API를 써야 하는 이유는 `ServerOnly`라 클라 예측 경로가 없어서 몽타주 복제가 전적으로 ASC의 `RepAnimMontageInfo`에 의존하기 때문(raw 호출은 서버 화면에만 보임). 콤보 없이 풀바디 몽타주 하나면 충분 — 히어로의 UpperBody 슬롯 분리는 "이동하며 동시에 스킬"이 필요해서였는데, 몬스터는 BT가 멈춰서 공격하는 구조라 해당 없음 |
| `UP1GameplayAbility_MeleeAttack` | 완료 | 콤보 기본공격. AnimNotify → `BasicAttackHit` 이벤트 → `OnHitEventReceived()`(protected virtual, 적중 여부와 무관하게 스윙마다 호출)에서 `GetEnemiesInRadius()` + 전방 반원 필터 → `ApplyComboHitDamage()`(protected virtual)로 데미지 적용. 콤보 큐는 `InputPressed()`에서만 처리. Sacred Oath 관련 코드는 전혀 없음(서브클래스로 분리) — 이 클래스는 특정 버프의 존재를 모르며, 시너지가 있는 영웅은 아예 이 클래스 대신 `MeleeAttack_SacredOath`를 등록한다(같은 InputTag 경쟁 없음, 상세 배경은 GAS 컨벤션 참고) |
| `UP1GameplayAbility_MeleeAttack_SacredOath` | 완료 | `MeleeAttack` 상속, 콤보/몽타주 로직은 그대로 재사용하고 `ApplyComboHitDamage()` + `OnHitEventReceived()` 둘 다 오버라이드. Sacred Oath 시너지가 있는 영웅(그레이스톤)의 **유일한** BasicAttack 어빌리티로 등록 — 평소 버전과 경쟁하지 않는다. `OnHitEventReceived`(적중 여부와 무관하게 스윙 자체가 발생하는 시점, `Super`가 `ApplyComboHitDamage`를 호출하기 *전*)에서 `Buff.SacredOath.Active` 태그가 아직 있으면 `TrailParticleTemplate`을 1회 재생하고 `Super::OnHitEventReceived()`를 호출한다 — 허공에 헛스윙해도(버프가 소모되지 않으므로) 매 스윙마다 트레일이 다시 나오는 이유. `ApplyComboHitDamage()`에서는 같은 태그로 분기: 없으면 `Super::ApplyComboHitDamage()`(평소 로직)로 폴백, 있으면 강화 로직(Cleave 무시(전원 100%)+추가데미지(`BonusFlatDamage`는 `FScalableFloat`, 레벨 스케일링)+슬로우) 적용 후 `RemoveActiveEffectsWithGrantedTags`로 버프 소모. 콤보가 여러 스텝 이어지는 동안 같은 인스턴스가 재사용되므로 매 히트마다 재확인 — 첫 스윙에서 소모되면 이후 스텝은 자동 폴백돼 "다음 1회만" 강화를 보장(단, 트레일은 태그가 있는 한 헛스윙에도 계속 나옴 — 강화 데미지와는 다른 생명주기). 검 발광은 이 클래스도, 다른 어떤 어빌리티도 전혀 관여하지 않음 — `UP1BuffCosmeticEffectComponent`가 태그 자체를 구독해 자동 처리. `bShowDebugSacredOath`로 강화 히트 대상에 금색 디버그 구체+보너스데미지 텍스트 표시 가능 |
| `UP1GameplayAbility_SacredOath` (E) | 완료 | 순수 버프 어빌리티(Damage 상속 아님). 커밋 후 자신에게 `BuffEffectClass`(5초 지속, `Buff.SacredOath.Active` 태그+`AttackRange` 보너스 모디파이어)를 적용하고, `CastMontage` 재생 후 종료(몽타주 없으면 즉시 종료) — **그게 전부**. 검 발광은 물론 어떤 코스메틱 Multicast도 이 어빌리티가 직접 호출하지 않는다(`UP1BuffCosmeticEffectComponent`가 태그 부여/소멸에 반응해 알아서 처리하므로 어빌리티는 GAS 로직만 신경 쓰면 됨). 무기 궤적(트레일)은 "버프 지속 동안 매 스윙마다 재생"이라는 요구 때문에 태그 상태가 아니라 스윙 이벤트에 반응해야 해서 `MeleeAttack_SacredOath::OnHitEventReceived`가 담당. 실제 데미지/슬로우/궤적은 `MeleeAttack_SacredOath` 쪽에서 처리 |
| `UP1BuffCosmeticEffectComponent` (`Characters/`) | 완료 | "버프 태그가 활성인 동안 코스메틱 이펙트(머티리얼/지속 파티클)를 유지하고, 태그가 사라지면 자동 원복"하는 범용 `UPawnComponent`. `Effects`(`TArray<FP1BuffCosmeticEffectEntry>`) 배열로 태그↔머티리얼 슬롯/파티클을 데이터로 매핑 — 특정 스킬 이름을 전혀 몰라도 되므로 이 시너지가 필요한 영웅의 BP에만 컴포넌트를 추가하면 되고, 다른 영웅은 영향받지 않는다. `BindToAbilitySystemComponent(ASC)`를 소유 캐릭터가 ASC 준비 시점(`AP1HeroCharacter::InitAbilityActorInfo`)에 호출해줘야 하며, 각 Entry의 태그마다 `RegisterGameplayTagEvent(NewOrRemoved)`를 구독해 카운트>0이면 켜고 0이면 끈다(`AddUObject`의 페이로드 파라미터로 Entry 데이터를 콜백에 함께 전달). 서버(`HasAuthority()`)에서만 Multicast를 호출. 그레이스톤 BP에 이 컴포넌트를 추가하고 Sacred Oath 검 발광 항목(`Buff.SacredOath.Active` → `MaterialSlotName`+`OverrideMaterial`) 하나를 등록해 사용 |
| `UP1GameplayAbility_MakeWay` (Q) | 완료 | 캐릭터를 따라다니는 화염 회오리. 어빌리티가 직접 반복 타이머(1초 간격 4틱=1,2,3,4초) 소유 → 매 틱 `GetEnemiesInRadius()`로 현재 위치 기준 재스캔 → 데미지+방어력감소 디버프(중첩, `Data.DebuffMagnitude`). 코스트는 캐스트 시 `CommitAbilityCost()`, 쿨다운은 4번째 틱 직후(지속시간 종료 시점)에 적용 — 표준 GE Periodicity 대신 C++ 타이머를 쓴 이유는 "캐릭터가 이동하며 매번 재판정"이 고정 타겟 기반 Periodic GE로는 불가능하기 때문. `WhirlwindParticleTemplate`/`WhirlwindSocketName`으로 캐릭터를 따라다니는 회오리 지속 파티클을 `ActivateAbility`에서 `MulticastSetAttachedParticleEffect`로 시작하고, `EndAbility`(정상 종료/조기 취소 공통 경로)에서 `MulticastStopAttachedParticleEffect`로 정리 — 버프 태그 기반이 아니라 어빌리티 자신의 타이머 생명주기에 직접 맞물려 있다는 점이 Sacred Oath 패턴과 다름(별도 GE 태그 없이 어빌리티가 이미 시작/종료 시점을 정확히 알고 있으므로) |
| `UP1AnimNotifyState_MaterialOverride` | 완료 | 범용 머티리얼 오버라이드 AnimNotifyState. 구간 동안 지정 슬롯을 `OverrideMaterial`로 교체, 끝나면 `SetMaterial(Index, nullptr)`로 스켈레탈메시 기본 머티리얼 복귀. 몽타주 기반이라 모든 클라이언트에 리플리케이트됨(GAS 코드로 직접 SetMaterial하면 로컬+서버에서만 보이는 것과 대비). **몽타주의 "고정된 타임라인 구간"에만 종속** — GE 수명(조기 소모 등 가변 길이)에 정확히 대응해야 하면 아래 GameplayCue를 대신 사용 |
| `UP1GameplayCueNotify_MaterialOverride` / `_ParticleEffect` | 완료, **현재 미사용** | GameplayCue 기반 코스메틱 효과(자세한 설명은 GAS 컨벤션 참고). Sacred Oath는 결국 이 방식 대신 아래 Multicast 패턴으로 전환했음 — 폴더 스캔(`GameplayCueNotifyPaths`)+태그매칭이 에디터 세션 중 갱신이 안 되는 등 다루기 번거로워서. 범용 유틸리티 클래스라 삭제하지 않고 남겨둠 — 다른 어빌리티에서 GameplayCue 방식이 더 적합하면 재사용 가능 |
| `AP1CharacterBase::MulticastSetMaterialOverride` / `MulticastPlayParticleEffect`(1회성) / `MulticastSetAttachedParticleEffect`+`MulticastStopAttachedParticleEffect`(부착형 지속) | 완료 | 코스메틱 Multicast 배관(범용 인프라, 특정 스킬을 모름). GameplayCue의 스캔 경로/태그 매칭 없이, 서버에서 호출하면 Multicast RPC로 모든 클라이언트(시뮬레이티드 프록시 포함)에 자동 복제됨 — 별도 Cue Notify 에셋 불필요. 호출부는 `UP1BuffCosmeticEffectComponent`(태그 기반 지속, 예: Sacred Oath 검 발광), 개별 어빌리티의 1회성 호출(예: Sacred Oath 무기 궤적), 또는 어빌리티가 자기 타이머 생명주기에 맞춰 직접 시작/정지(예: Make Way 회오리)까지 세 갈래. `MulticastSetAttachedParticleEffect`는 소켓이 비어있거나 존재하지 않으면 루트 컴포넌트(액터 전체)에 부착하는 폴백이 있음 — 특정 본이 아니라 캐릭터 전체를 중심으로 도는 이펙트(회오리 등)에 적합. **`MulticastPlayNiagaraEffect`(신규, 2026-08-17)** — 위 목록은 전부 레거시 Cascade(`UParticleSystem`)라 현대 Niagara(`UNiagaraSystem`)는 타입이 달라 겸용 불가, 그래서 `MulticastPlayParticleEffect`와 완전히 같은 구조(소켓 부착 1회성, 소켓 없으면 캐릭터 위치에 재생)로 별도 함수를 만듦(`Niagara` 모듈은 이미 `P1.Build.cs`에 있어 추가 의존성 설정 불필요) — 사면(아이템) "용기"/증강(아이템) "진실의 일격"이 첫 사용처, 신규 이펙트는 가능하면 이쪽 사용 권장. **`MulticastSetAttachedNiagaraEffect`+`MulticastStopAttachedNiagaraEffect`(신규, 2026-08-18)** — `MulticastSetAttachedParticleEffect`/`Stop`의 Niagara판이지만, 슬롯 하나만 관리하는 원본과 달리 `TMap<FName, UNiagaraComponent*>`로 **SocketName을 키**로 삼아 여러 소켓에 동시에 독립적인 지속 이펙트를 유지할 수 있다(증강 "진실의 일격"의 양손 동시 발광이 첫 사용처 — 손마다 소켓이 다르므로 슬롯이 하나뿐이면 한쪽만 유지 가능). 소켓 부착만 지원(원본의 "소켓 없으면 루트 폴백"은 없음 — 지금까지 부착 없는 지속 이펙트 요구가 없었음). **버프 지속시간에 종속된 이펙트(특히 Looping 에셋)는 반드시 이 지속형 함수를 쓸 것 — 1회성 `MulticastPlayNiagaraEffect`로 대체하면 Looping 시스템이 "완료" 상태에 영영 도달하지 못해 버프가 끝나도 이펙트가 영구히 안 사라지는 버그가 생긴다**(진실의 일격에서 실제로 겪은 버그, 위 표 참고) |
| `AP1HeroCharacter::InitAbilityActorInfo` → `UP1BuffCosmeticEffectComponent` 바인딩 훅 | 완료 | ASC 준비 시점에 `FindComponentByClass<UP1BuffCosmeticEffectComponent>()`로 컴포넌트를 찾아 `BindToAbilitySystemComponent(ASC)`를 호출하는 한 줄짜리 범용 훅 — 컴포넌트가 없는 영웅(대부분)은 그냥 스킵된다. `AP1HeroCharacter` 자체엔 특정 버프 태그/슬롯 이름이 전혀 남아있지 않음(예전엔 `WeaponGlowMaterialSlotName`/`BindSacredOathBuffTagListener`가 여기 하드코딩돼 있었으나 컴포넌트로 이관) |
| `UP1GameplayAbility_AssaultTheGates` (RMB) | **완료 (풀 테스트 통과)** | 2단계 지면조준 도약. 조준(WaitTargetData+GroundDecal, 미커밋) → LMB 확정/RMB 취소 → 확정 시 코스트 소모 + 서버 쿨다운(SetByCaller 지속시간) → `ApplyRootMotionJumpForce`(Duration=Land 노티파이 트리거 시점) 포물선 도약 → 착지 AOE 데미지 → 영웅 적중 시 이속버프+쿨다운 35%↓. 도약 내내 `State.Attacking` 소유해 LMB 등 다른 어빌리티 인터럽트 차단 |
| `AP1TargetActor_GroundDecal_Deferred` | 완료 | 원형 지면 조준 타겟액터. 카메라 트레이스+사거리 클램프, 확정 위치를 LocationInfo 타겟데이터로 서버 복제. 시각 표시는 `UDecalComponent`(Deferred Decal) — 초기엔 UE5.8 Substrate 데칼 렌더링이 불안정해 `UStaticMeshComponent`(평면 메시) 폴백으로 시작했으나(`AP1TargetActor_GroundDecal`), 이후 Deferred Decal이 정상 렌더링됨을 확인하고 전 원형 조준 어빌리티(AssaultTheGates/ContainmentFence/IonStrike)를 이 클래스로 통일 — **StaticMesh 버전은 삭제됨**. 직사각형 조준(PhotonDisruptor)도 동일한 이유로 StaticMesh 버전(`AP1TargetActor_GroundDecal_Rectangle`)을 삭제하고 `AP1TargetActor_GroundDecal_Rectangle_Deferred`만 유지 |
| `UP1GameplayAbility_StoneForgedSoul` (R, 궁극기) | 완료 (카메라 연출만 보류) | 조준 없이 제자리에서 발동. 캐스팅+상승+공중대기+하강+착지가 전부 담긴 몽타주 하나만 재생 — **RMB와 달리 `ApplyRootMotionJumpForce` 등 코드 기반 궤적 생성이 없음**: 목표 지점이 항상 "원위치"로 고정된 왕복 이동이라 원본 애니메이션에 실제 수직 루트모션이 있으면 그걸 그대로 쓰는 게 더 정확하고 간단함(RMB가 코드 궤적을 쓴 이유는 지면 조준으로 매번 달라지는 가변 목표 지점 때문이었는데, 여긴 그 조건 자체가 없음). 몽타주 재생 시작 시 `MovementMode`를 `MOVE_Flying`으로 전환(중력이 루트모션과 안 싸우게), 착지 AnimNotify(`Event.Montage.StoneForgedSoul.Crash`, RMB의 Land 이벤트와 동일 패턴)에서 `MOVE_Walking`으로 복귀 + 그 자리 기준 범위 데미지(`UP1DamageGameplayAbility`의 `FlatDamage`/`PhysicalPowerCoefficient` 재사용). 발동 즉시(스테이시스 시작) 3가지 병행: (1) 잃은 체력 비율로 최대 `MaxHealAmplification`배까지 증폭되는 회복 — 공식이 단순 계수 곱셈이 아니라 C++에서 최종값을 계산해 `Data.Heal.Flat` SetByCaller로 전달, (2) `State.Invulnerable` 태그를 부여하는 무적 GE(전 어빌리티 공용 태그, 아래 참고), (3) `Make Way`와 동일한 패턴의 반복 재스캔 타이머로 근접도 비례 슬로우(거리 0=`MaxSlowPercent`, 반경 끝=0%, 선형 감쇠) 계속 갱신 적용. **착지 데미지는 `UP1AttributeSet::GetUltimateDamagePercent()`를 타격 시점에 직접 읽어 `1.0f+값`을 `ApplyDamageToTarget`의 `DamageMultiplier`로 전달**(신규, 2026-08-17 — 괴사(아이템) "물질분열기"가 첫 사용처, 하드코딩 `1.0f`이었던 걸 교체) |
| `UP1AnimNotify_SendGameplayEvent` | 완료 | 범용 GameplayEvent 발신 AnimNotify — `EventTag` 프로퍼티로 태그 지정. 콤보 히트/스킬 착지 이벤트 등 재사용 |
| `UP1GameplayAbility_StoicismDeflect` (패시브 절반 1/2) | 완료 | "다음 기본공격 무효화, N초 쿨다운" — 실제 무효화 판정은 이 어빌리티가 아니라 `UP1AttributeSet::PostGameplayEffectExecute`에서 처리(위 참고). 이 클래스의 역할은 `OnGiveAbility`에서 자신의 Asset Tag(`Ability.StoicismDeflect`)를 루즈 태그로 ASC에 심어 "존재 판별"을 가능케 하는 것과, `Event.StoicismDeflect.Consumed` GameplayEvent(`AbilityTriggers`)로 트리거되면 `CommitAbilityCooldown()`만 커밋하고 즉시 종료하는 것뿐 — "지금 사용 가능한지"는 네이티브 쿨다운 태그 부재로 이미 판별되므로 별도 "준비완료" 상태 관리가 필요 없다. 다른 어빌리티 사용 중에도 항상 반응해야 해서 베이스의 `State.Attacking` 상호배타 규칙에서 제외(`ActivationBlockedTags`/`ActivationOwnedTags`에서 Remove). `NetExecutionPolicy=ServerOnly`. 쿨다운(15→4.8초 등, 영웅 레벨별)은 `CooldownGameplayEffectClass`의 Duration Modifier를 Scalable Float로 설정하는 것만으로 충분(Cost와 동일한 이유로 C++에 값을 안 둠) |
| `UP1GameplayAbility_StoicismVitality` (패시브 절반 2/2) | 완료 | "초당 최대체력 비례 회복 + 고정 방어력, 둘 다 잃은 체력 비율에 비례 증폭 + 체력 50% 미만이면 회복만 2배" — 상시 켜져있는 패시브라 코스트/쿨다운 없이 `ActivateAbility`에서 반복 타이머(기본 1초 간격, 캐릭터 생존 내내 반복)를 시작하고 즉시 1틱 선실행. 매 틱 현재 Health/MaxHealth를 읽어 회복량(즉시 회복 GE, `Data.Heal.Flat` SetByCaller)과 방어력 보너스(버프 GE, `Data.ArmorBonus.Flat` SetByCaller)를 매번 새로 계산해 재적용 — 둘 다 "현재 체력"이라는 계속 바뀌는 값에 의존해 한 번 적용하고 끝나는 Infinite GE로는 표현 불가. 방어력 버프 GE는 Stacking Type=AggregateByTarget/Stack Limit=1로 설정해 누적이 아니라 항상 최신 값으로 덮어써야 함(Stone Forged Soul 슬로우 디버프와 동일 패턴). Deflect와 마찬가지로 `State.Attacking` 제외 + `NetExecutionPolicy=ServerOnly`. **`bActivateOnGranted=true`** — 이게 없으면 `ActivateAbility`가 절대 안 불려서 타이머 자체가 시작을 안 함(실제로 겪은 버그, 아래 알려진 버그 표 참고) |
**`Abilities/Dekker/`** — 두 번째 영웅. 좌클릭/기본공격은 아직 그레이스톤 것을 그대로 씀(전용 기본공격 미구현), RMB/Q/E/R/패시브 5개 스킬 전부 완료(에디터 작업 포함, 2026-08-17 확인).

| 클래스 | 상태 | 비고 |
|---|---|---|
| `UP1GameplayAbility_StasisBomb` (RMB) | 완료 | "우클릭 홀드=조준 유지, 릴리즈=발사"하는 홀드 조준 스킬(AssaultTheGates의 WaitTargetData 방식과 다름) — 확정은 `InputReleased()` 오버라이드, 취소는 `ASC->GenericLocalCancelCallbacks`에 직접 구독(`OnAimCancelled`). `AimMontage`(홀드 중 루프 재생, `AnimInstance->Montage_Stop`으로 명시적 정지)/`FireMontage`(발사 1회성). `AP1Projectile`을 `MaxBounces=1`로 스폰해 첫 충돌이 적이면 즉시, 지형이면 1회 튕긴 뒤 다음 충돌에서 `OnBombExplode()` 폭발 판정(`GetEnemiesInRadius`+`ApplyDamageToTarget`). **거리 비례 스턴**: `ActiveBomb->GetDistanceTraveled()`를 `MaxStunDistance`(3500)로 나눈 Alpha를 `MinStunDuration`(0.8, 고정)~`MaxStunDuration`(FScalableFloat, 레벨별)로 `FMath::Lerp` — 멀리 날아갈수록 스턴이 길어진다. 스턴 GE는 `Data.StunDuration` SetByCaller로 이 값을 주입 |
| `UP1GameplayAbility_PhotonDisruptor` (Q) | 완료 | "드론이 하늘로 날아가 사거리 끝까지 이동하며 지나간 지표면에 주기적으로 블래스트"하는 스킬 — WaitTargetData(직사각형 인디케이터, `AP1TargetActor_DirectionalGroundTargetBase` 계열)로 조준→확정 시 `GroundStart`(서버 기준 캐릭터 위치)~`GroundEnd`(확정 지점) 사이를 `SweepDuration` 동안 선형 보간하며 `BlastTickInterval` 간격으로 `GetEnemiesInRadius`+데미지+슬로우 판정(MakeWay와 동일한 "어빌리티 소유 반복 타이머" 패턴, 중심점이 캐릭터가 아니라 시간에 따라 이동하는 좌표라는 점만 다름). 드론/빔 비주얼(`SkyDronePodEffect`)은 `MulticastPlayMovingParticleEffect`(Start/End/Duration이 결정적이라 각 클라가 로컬 타이머만으로 독립 재생, 위치 리플리케이션 불필요)로 재생. **`State.Rooted`**: 조준 단계(WaitTargetData 활성 중)는 자유 이동 허용, 확정 후 실제 스윕 구간에서만 `SetRootedState(true)`로 이 루즈 태그를 부여해 `AP1PlayerController::HandleMove()`가 이동 입력을 무시하게 만든다 — `ActivationOwnedTags`(어빌리티 전체 수명)가 아니라 수동 스코프 관리인 이유는 조준 중엔 캐릭터가 움직일 수 있어야 하기 때문(AssaultTheGates/ContainmentFence와 동일 원칙). **쿨다운은 확정 즉시 시작(2026-08-17)**: 스킬샷 계열(SweepDuration 1.6초, 채널링이 아님)이라 MakeWay/IonStrike의 "지속시간 끝나야 쿨다운 시작" 패턴과 달리 표준 `CommitAbility()`로 확정 시점에 코스트+쿨다운을 함께 커밋한다. **한때 실수로 `CommitAbility()` 대신 `CommitAbilityCost()`+`OnBlastTick()`/`EndAbility()`에서 수동 쿨다운 재적용을 섞어 쓴 적이 있었는데, 그러면 한 캐스트에 쿨다운 GE가 두 번 걸려 스킬 아이콘 쿨다운이 카운트다운 도중 리셋되는 버그가 났었다**(AbilityHaste 감소 로그가 한 캐스트에 두 번 찍히는 걸로 발견됨) — 지금은 확정 시 한 번만 커밋하는 것으로 정리됨 |
| `UP1GameplayAbility_ContainmentFence` (E) | 완료 | "원형 장판을 배치해 지속시간 동안 적 영웅의 장판 통과를 막는" 순수 CC — 데미지가 전혀 없어 `UP1DamageGameplayAbility`가 아니라 베이스 `UP1GameplayAbility` 상속. WaitTargetData(원형 인디케이터)로 조준→확정 시 `AP1ContainmentFence`(`Hazards/`, GAS 비관여 순수 `AActor`) 스폰 후 `InitializeFence(Radius, Duration, Instigator)` 호출 — 이 어빌리티는 "언제/어디에 배치할지"만 결정하고, 실제 통과 차단 판정은 스폰된 액터가 전담 |
| `UP1GameplayAbility_IonStrike` (R, 궁극기) | 완료 | 원래 참고했던 Predecessor 툴팁(단발 블래스트+이중 반경 스턴/슬로우)이 실제 보유 중인 구버전 Paragon 에셋(하늘에서 화살비가 떨어지는 연출)과 안 맞아 **지속 데미지+마법방어력 감소(누적)로 재설계**됨. WaitTargetData(원형)로 조준→확정 시 `AimMontage` 정지+`FireMontage` 재생+화살비 파티클(`ArrowRainEffectTemplate`, `MulticastSetPersistentParticleEffectAtLocation`으로 확정 위치에 고정 재생) 후, MakeWay와 완전히 동일한 "어빌리티 소유 반복 타이머" 패턴(`TickPeriod`=0.5초 고정, `TotalTicks`=`Duration.GetValueAtLevel()/TickPeriod`로 캐스트마다 계산 — R은 3랭크 컨벤션이라 `Duration`/`MagicalArmorShredPerTick` 커브는 레벨 1~3용)로 확정 좌표(고정, 캐릭터 위치 아님) 기준 재스캔해 매 틱 데미지+마법방어력 감소 디버프(스택형) 적용. **코스트/쿨다운 분리**(MakeWay 컨벤션): 확정 시 `CommitAbilityCost()`만, 마지막 틱에서 쿨다운 적용, 중도 취소 시 `bCommitted` 플래그로 "조준만 하다 취소"와 구분해 폴백 쿨다운 오적용 방지(PhotonDisruptor와 동일 패턴). **매 틱 데미지도 `UP1AttributeSet::GetUltimateDamagePercent()`를 읽어 `1.0f+값`을 `DamageMultiplier`로 전달**(신규, 2026-08-17, StoneForgedSoul과 동일) |
| `UP1GameplayAbility_RocketBoots` (패시브) | 완료 | "공중에서 점프 입력을 다시 누르면 위로 추진, 다른 스킬 사용 시 쿨다운 3초 감소" — 데미지/버프 없는 순수 이동이라 베이스 `UP1GameplayAbility` 상속. **발동 트리거가 특이**: 전용 InputTag 대신 UI 슬롯 공유용 `InputTag.Ability.Passive`를 재사용 — `AP1PlayerController::HandleJumpStarted()`가 캐릭터 공중 상태에서 이 태그로 `AbilityInputTagPressed`를 직접 호출해 실제 발동 트리거로 이중 사용한다(다른 영웅 패시브는 이 태그를 안 쓰므로 충돌 없음). `ActivateAbility()`가 `CharacterMovement->IsFalling()`을 재검증(아니면 무소모 조용히 종료) 후 `Character->LaunchCharacter(FVector(0,0,LaunchVelocityZ), false, true)`(Z만 덮어써 낙하속도 무관 일정 추진력)로 즉시 완결. **쿨다운은 캐릭터 레벨(1~18) 기준**(`BaseCooldown.GetValueAtLevel(GetP1CharacterLevel())`, StoicismDeflect와 동일 패턴) — `Data.CooldownDuration` SetByCaller로 직접 주입. **"다른 스킬 사용 시 3초 감소"**: `OnGiveAbility()`에서 서버측 ASC에만(`ActorInfo->IsNetAuthority()` 게이트) `ASC->AbilityActivatedCallbacks`를 구독(캐릭터 생존 내내 유지, 어빌리티 자체의 짧은 활성 수명과 무관) — 자기 자신 발동은 제외하고, 남은 쿨다운을 조회해 3초 앞당겨 GE 재적용(AssaultTheGates의 `ReduceCooldown()`과 같은 "제거 후 재적용" 패턴, 퍼센트 대신 플랫 초 단위) |
| `AP1TargetActor_DirectionalGroundTargetBase` (`TargetActors/`) | 완료 | "캐릭터 위치에서 조준 방향으로 고정 길이만큼 뻗어나가는 복도"를 조준하는 액터들의 공통 추상 베이스(PhotonDisruptor가 사용) — 조준 방향 계산/지면 스냅/확정 처리(공통 순수 로직)는 베이스가 담당하고, 실제 비주얼 컴포넌트 생성+매 틱 위치/회전 갱신만 서브클래스(`AP1TargetActor_GroundDecal_Rectangle_Deferred`)가 `UpdateIndicatorTransform()` 훅으로 구현. 원형 타겟액터(`AP1TargetActor_GroundDecal_Deferred`, 확정 지점="중심")와 달리 확정 시 "먼 쪽 끝점"을 반환 |
| `AP1Hazards/AP1ContainmentFence` | 완료 | ContainmentFence(E)가 스폰하는 지속형 지면 장애물 — GAS 비관여 순수 `AActor`(`bReplicates=true`). `InitializeFence(Radius, Duration, Instigator)`가 스피어 오버랩 컴포넌트/파티클 스케일 설정 후 `SetLifeSpan(Duration)`, 초기 오버랩 스캔으로 "안에서 시작=밖으로 못 나감" / 새로 접근하는 적은 "밖에서 시작=안으로 못 들어옴"으로 분류(`TrackedEnemies` 맵). 서버 전용 `Tick()`이 매 프레임 추적 대상의 2D 거리를 재계산해 잘못된 쪽으로 넘어간 적을 경계선(±5유닛 버퍼)으로 `SetActorLocation` 클램프 — 새 콜리전 채널 없이 순수 오버랩+동적 팀 체크+수동 위치 클램핑으로 구현(`IsBlockableEnemy()`가 `Character.Type.Hero`+상대팀 여부 판별) |
| `AP1Projectile` 바운스 물리 | 완료 | StasisBomb이 쓰는 `MaxBounces`/`BounceRestitution` — `OnProjectileBounce` 델리게이트(엔진이 자체 바운스 속도 계산 직후, 정지 임계값 체크 직전에 호출)에서 `ImpactVelocity`를 `ImpactNormal`로 반사시켜 `BounceRestitution` 배율로 감쇠, 레벨 지오메트리의 PhysicalMaterial과 무관하게 결정적인 바운스를 보장. **바인딩은 `BeginPlay()`가 아니라 `InitializeProjectile()`에서** — `SpawnActor<T>()`(non-deferred)가 `BeginPlay()`를 동기 실행 후 포인터를 반환하므로, 호출자가 `SpawnActor` 반환 후 `MaxBounces`를 대입하는 시점은 이미 `BeginPlay()`가 끝난 뒤라 `BeginPlay()`에서 `MaxBounces` 체크하면 항상 스폰 직후의 기본값(0)만 보게 되는 버그가 있었음(아래 알려진 버그 표 참고) |
| GameplayEffect 데이터 에셋 (Dekker) | 진행 중 | 각 어빌리티 표의 "에셋 대기" 항목 참고 — 스턴/슬로우/마법방어력감소 디버프 GE, 각 스킬 쿨다운/코스트 GE, ContainmentFence는 GE 자체가 필요 없음(액터가 판정 전담) |
| GameplayEffect 데이터 에셋 (Greystone) | 진행 중 | 기본공격: `GE_BasicAttackDamage`. RMB: `GE_Damage`(공용, `P1ExecCalc_Damage` Execution 등록 — 모든 물리 데미지 어빌리티가 공유), `GE_Cooldown_AssaultTheGates`(Duration=SetByCaller `Data.CooldownDuration`, Target Tags로 `Cooldown.Ability.AssaultTheGates` 부여), `GE_Cost_Assault`, `GE_Buff_MoveSpeed`. `DefaultAttributesEffect`(`GE_GreystoneDefaultAttributes`) 완료. **전투 보상/레벨업**: `GE_ExperienceReward_Flat`(Instant, `Experience` Modifier=Additive, Magnitude=SetByCaller `Data.Experience.Flat` — 킬/어시스트 공용, 액수만 호출부에서 다르게 주입) 완료·PIE 실전 검증됨(`[AttributeSet] Experience 획득` 로그로 확인). `GE_GoldReward_Kill`(Gold Modifier=Custom Calculation Class=`MMC_GoldKillBounty`)/`GE_GoldReward_Flat`(Gold Modifier=Additive, SetByCaller `Data.Gold.Flat`) 완료 |

**AttributeSet 어트리뷰트 목록:**

| 카테고리 | 어트리뷰트 |
|---|---|
| Vital | `Health`, `MaxHealth`, `Mana`, `MaxMana`, `HealthRegen`, `ManaRegen` |
| Combat | `PhysicalPower`, `MagicalPower`, `AttackSpeed`, `BasicAttackTime`, `AttackRange`, `Cleave` |
| Combat | `PhysicalArmor`, `MagicalArmor`, `PhysicalPenetration`, `MagicalPenetration` |
| Combat | `LifeSteal`, `Tenacity`, `DamageReduction`(신규 2026-08-17, [0,1] 클램프, 방어력과 별개의 최종 승산 감쇄 레이어 — 관통으로 무효화 안 됨, `P1ExecCalc_Damage`가 방어 감산 이후 소비, 사면(아이템) "용기"가 첫 사용처), `AbilityHaste`(2026-08-17까지는 UI 표시만 되던 죽은 스탯 — 아래 별도 설명 참고 이제 실제 쿨다운에 연결됨), `UltimateHaste`(신규 2026-08-17, R에만 추가로 붙는 가속치), `UltimateDamagePercent`(신규 2026-08-17, R 데미지 배율 보너스) |
| Combat | `CriticalChance`(2026-08-07 추가, [0,1] 클램프, 기본 0), `CriticalDamage`(2026-08-07 추가, 1.0 미만 불가, 기본 1.5=150%) |
| Movement | `MovementSpeed` |
| Progression | `Gold`, `Experience` — 전투 보상 시스템용. GE Modifier 타겟이 될 수 있어야 해서(MMC 현상금 계산 등) PlayerState의 plain int가 아니라 정식 GAS Attribute로 구현 |
| Meta (transient) | `Damage` — ExecCalc_Damage 출력 누적, PostGameplayEffectExecute에서 Health로 변환 (비복제). `CriticalHitFlag`(신규, 2026-08-17) — ExecCalc가 크리티컬 판정 결과(0.0/1.0)를 Override로 출력, `Damage`와 동일하게 PostGameplayEffectExecute가 즉시 읽고 리셋(온-히트 크리티컬 반응 아이템의 판별용, 더스트 데빌 "매너스"가 첫 사용처) |

**⚠️ `AttackSpeed`는 퍼센트 스케일(100=기준/100%)이다 — `LifeSteal`/`Tenacity`/`CriticalChance`/`CriticalDamage`처럼 1.0=100%인 분수 스케일이 아니다.** `P1GameplayAbility_MeleeAttack`/`P1GameplayAbility_RangedAttack`의 `PlayRate = (몽타주 길이/BasicAttackTime) × (AttackSpeed/100.0f)` 계산이 이 스케일을 그대로 나눠 쓰기 때문에 어트리뷰트 값 자체를 바꿀 수 없다(기본값도 `InitAttackSpeed(100.0f)`, 예전엔 실수로 `1.0f`였다가 상점 UI가 `×100`으로 표시해서 120→12000%로 보이는 버그가 났었음 — 2026-08-07에 발견/수정). 새로 AttackSpeed를 다루는 GE/UI/코드를 추가할 땐 **항상 100 기준**으로 값을 넣을 것(예: "+20% 공격속도" 아이템 Modifier는 `+20`, `1.2`가 아님). `UP1ShopStatsWidget::BindStat()`의 `bAlreadyPercent` 파라미터가 이 예외를 처리한다(AttackSpeed만 `true`, 나머지 %스탯은 `false`).

**`AbilityHaste`/`UltimateHaste` 쿨다운 감소 시스템(신규, 2026-08-17)** — 처음엔 `AbilityHaste`가 상점 스탯 패널에만 표시되고 실제 쿨다운엔 전혀 영향을 안 주는 죽은 스탯이었다("괴사" 아이템으로 처음 실제 효과를 붙이면서 발견됨). 계산 로직은 공용 `private` 헬퍼 `UP1GameplayAbility::ApplyAbilityHasteToCommittedCooldown()`에 있다 — `AttrSet->GetAbilityHaste()`(+궁극기면 `GetUltimateHaste()`까지 합산)를 LoL식 공식(`감소율=Haste/(Haste+100)`, 예: Haste 25 → 20% 감소)으로 변환해 방금 건 쿨다운을 `UP1AbilitySystemComponent::ReduceCooldownByInputTag()`로 즉시 줄인다. **두 지점에서 호출된다 — 이게 이 시스템의 핵심 함정이었다**: (1) `ApplyCooldown()` 오버라이드가 `Super::ApplyCooldown()`(엔진 표준 `CommitAbilityCooldown()` 경로) 직후 호출, (2) `ApplyEffectToSelf()`가 지금 적용 중인 `EffectClass`가 이 어빌리티 자신의 쿨다운 GE(`GetCooldownGameplayEffect()->GetClass()`)와 같을 때 호출. **(2)가 필요한 이유**: 이 프로젝트 대부분의 실제 스킬(MakeWay/IonStrike/PhotonDisruptor/AssaultTheGates/StoicismDeflect/RocketBoots — "코스트/쿨다운 분리" 컨벤션, 위 GAS 섹션 참고)은 표준 `CommitAbilityCooldown()`을 아예 안 쓰고 `ApplyEffectToSelf(CooldownGE->GetClass())`로 수동 커밋하기 때문에, (1)만 있으면 이 스킬들엔 헤이스트가 전혀 안 먹는다(실제로 겪은 버그 — "스킬가속 10인데 12초 쿨다운이 그대로"라는 증상으로 발견됨). 기존 스킬 코드는 전혀 안 고쳤다 — 다들 `GetCooldownGameplayEffect()`에서 얻은 클래스를 그대로 `ApplyEffectToSelf`에 넘기는 기존 관례 덕분에 클래스 비교만으로 자동 식별된다. **한 곳(베이스 클래스)에서 처리하므로 Q/E/RMB/R 전부 자동으로 적용되고, 개별 어빌리티는 아무것도 몰라도 된다** — `InputTag`가 없는 어빌리티(아이템 반응형 어빌리티 등)는 `ReduceCooldownByInputTag`가 스펙을 못 찾아 자연히 스킵된다. `UltimateHaste`는 `InputTag == TAG_InputTag_Ability_R`일 때만 추가로 합산되는 별도 어트리뷰트("전체 어빌리티 가속"과 "궁극기 전용 추가 가속"을 분리해달라는 요구사항 — 괴사의 "네크로의 칼날"이 첫 사용처). **`UltimateDamagePercent`**는 별개 축으로, R 어빌리티(`UP1GameplayAbility_StoneForgedSoul`/`UP1GameplayAbility_IonStrike`)가 데미지 판정 시점에 직접 `AttrSet->GetUltimateDamagePercent()`를 읽어 `ApplyDamageToTarget()`의 `DamageMultiplier` 인자로 `1.0f + 값`을 넘긴다(기존엔 둘 다 하드코딩 `1.0f`) — 새 배율 채널이 아니라 기존 `Data.DamageMultiplier`(`P1ExecCalc_Damage`가 이미 소비)를 그대로 재사용. 둘 다 GAS Attribute Modifier로 얹는 방식이라 특정 아이템 클래스를 몰라도 되고(스탯이니까), 반응형 온-히트 아이템(크로노 스트라이크/매너스)과 달리 별도 `UGameplayAbility` 서브클래스 없이 순수 GE만으로 표현 가능하다.

**`CriticalChance`/`CriticalDamage`는 `P1ExecCalc_Damage`에 연결 완료(2026-08-07)** — Source(공격자)에서 두 속성을 캡처해(다른 공격자 스탯과 동일하게 스냅샷) `Raw` 계산 직후 `bIsCriticalHit = FRand() < CriticalChance`로 한 번 판정하고, 적중 시 `CriticalDamage` 배율을 `PreMitigation`(방어 감산 **이전** 단계)에 곱한다 — 즉 크리티컬도 방어력의 영향을 그대로 받는다(크리 데미지가 방어 무시가 아님). ExecCalc는 서버(권한 보유 측)에서만 실행되므로 판정을 한 번만 굴려도 클라/서버 간 어긋날 여지가 없다. 로그(`[ExecCalc_Damage]`)에 `Crit=0/1(확률%, 배율x)`로 매 타격마다 찍힘. **아직 안 된 것**: 데미지 숫자 UI(`UP1DamageNumberWidget`)가 크리티컬 여부를 구분해서 다르게 보여주는 것(색상/크기 등)은 없음 — 필요해지면 `NotifyDamageDealt`가 `bIsCriticalHit`를 함께 클라이언트로 실어 보내야 함.

### AI (`Source/P1/AI/`)

정글 몬스터 전용. 히어로 쪽엔 AI가 없어(전부 사람이 조종) 이 폴더는 정글 몬스터에서만 쓰인다. **완료 — BT/Blackboard 에셋 구성 및 레벨 배치까지 전부 끝남(2026-08-17 확인).**

**핵심 설계 — 리시(원점 복귀) 판정 기준**: "플레이어가 원점에서 얼마나 떨어졌는지"가 아니라 **"몬스터 자신이 홈에서 얼마나 떨어졌는지"**로만 판단한다. 사용자가 직접 지적한 카이팅 문제(최대 사거리에서 때리고 살짝 물러나 다시 때리는 식으로 몬스터를 홈 근처에 붙잡아두면서 무한정 딜링) — 원점 기준 플레이어 위치로 판정했다면 플레이어가 반경을 들락날락할 때마다 판정이 초기화돼 무한정 끌고 다닐 수 있었을 것. 몬스터 자신의 위치로 판정하면 몬스터가 실제로 LeashRadius 밖까지 끌려나갔을 때만 발동하므로 "얼마나 멀리 유인 가능한가"에 대한 자연스러운 상한이 되고, 카이팅에 흔들리지 않는다.

| 클래스 | 상태 | 비고 |
|---|---|---|
| `AP1JungleMonsterAIController` | **완료** | Behavior Tree 기반 3상태(Idle/Combat/Reset) FSM을 돌리는 오케스트레이터. 블랙보드는 최소 2키만 씀: `TargetActor`(Object)/`IsLeashing`(Bool) — `HomeLocation`/`LeashRadius` 같은 "설정값"은 블랙보드에 중복 저장하지 않고 BT 노드들이 그때그때 `AP1JungleMonsterCharacter`에서 직접 읽는다(단일 소스 오브 트루스). `OnPossess()`가 `BehaviorTreeAsset`(EditDefaultsOnly)으로 `UseBlackboard`+`RunBehaviorTree` 수행 — `UseBlackboard`가 raw 포인터 참조(`UBlackboardComponent*&`)를 요구해 `TObjectPtr`인 `Blackboard` 멤버를 직접 못 넘기므로 로컬 raw 포인터로 받은 뒤 대입(빌드 중 처음 걸린 컴파일 에러, 아래 알려진 버그 참고할 정도는 아닌 사소한 타입 이슈). `NotifyAggro(InstigatorActor)` — `AP1JungleMonsterCharacter::OnHitReactEventReceived`가 호출, `TargetActor`를 세팅해 Combat 브랜치를 깨움(이미 `IsLeashing=true`면 무시) |
| `UBTService_P1JungleLeashCheck` | **완료** | Combat 브랜치에 붙는 서비스(0.2초 간격). 몬스터-HomeLocation 거리가 `LeashRadius`를 넘으면 `IsLeashing=true`로 **래치**(한 번 넘으면 Reset 브랜치 진입 후 홈 도착 시 `BTTask_P1ReturnToCamp`가 풀 때까지 유지 — 국지적으로 왔다갔다하며 깜빡이지 않음). 타겟이 무효화됐거나 `State.Dead` 태그가 붙어도(히어로는 죽어도 Pawn이 안 파괴되고 그 자리에 남으므로 `IsValid`만으론 부족 — 태그를 직접 확인) 같은 이유로 리시 트리거 |
| `UBTTask_P1ReturnToCamp` | **완료** | Reset 브랜치의 유일한 태스크. `ExecuteTask`에서 `EnterLeashRecovery()`(무적+회복 시작)+`MoveToLocation(HomeLocation)` 후 `InProgress` 반환, `TickTask`가 매 틱 도착 여부(`AcceptanceRadius` 이내) 검사해 도착하면 `ExitLeashRecovery()`(무적 해제+풀피 확정)+`IsLeashing=false`+`Succeeded` |
| `UBTTask_P1MeleeAttack` | **완료** | Combat 브랜치에서 `MoveTo(TargetActor, 사거리)` 뒤에 배치하는 순간 완료 태스크 — 매 틱 `AP1JungleMonsterCharacter::RequestMeleeAttack()` 호출만 하고 끝(쿨다운 게이팅은 어빌리티 쪽 `CooldownGameplayEffectClass`가 전담하므로 이 태스크는 게이팅 로직이 전혀 없음) |

**BT 레이아웃(에디터 구성 완료):**
```
Root Selector
  1. Reset   — Decorator: IsLeashing==true       → BTTask_P1ReturnToCamp
  2. Combat  — Decorator: TargetActor is set      → Service: BTService_P1JungleLeashCheck
                 Selector(Loop)
                   Sequence: MoveTo(TargetActor, 사거리) → BTTask_P1MeleeAttack
  3. Idle    — (fallback, 제자리 대기)
```

| 클래스 | 상태 | 비고 |
|---|---|---|
| `AP1JungleCampAnchor` | **완료** | 레벨에 직접 배치하는 캠프 지점 — 시각 마커(`MarkerMeshComponent`, Static Mesh는 에디터에서 지정, 콜리전 없음)와 스폰 데이터(`MonsterClass`/`MonsterCount`/`RespawnDelay`/`LeashRadiusOverride`)를 한 액터로 묶은 단일 소스 오브 트루스. 별도 스폰 매니저 싱글턴 없이 캠프마다 자기 몬스터 무리의 생사를 직접 관리(캠프들이 서로 독립적이라 공용 매니저가 불필요). `BeginPlay()`(서버 전용)에 `SpawnMonster()` → 무리 전체가 죽으면(아래 참고) `RespawnDelay`초 뒤 같은 자리에 무리 전체 재스폰. 레벨 배치 액터라 자체 리플리케이션은 불필요(모든 클라가 레벨 데이터로 동일하게 보유) — 실제 복제는 이 액터가 스폰하는 `AP1JungleMonsterCharacter` 쪽이 담당. **매치 시간 기반 레벨링**: `MonsterLevelByMatchTimeTable`(CurveTable, Row="MonsterLevelByMatchTime", `Data/CT_MonsterLevelByMatchTime.json` 임포트 — 0분=1레벨~13분=10레벨에서 캡, 2026-07-29에 10~15분 매치 길이에 맞춰 18분→13분으로 압축)을 `ComputeMonsterLevel()`이 `GetWorld()->GetTimeSeconds()`(분 단위 환산)로 평가해 스폰마다 레벨을 정한다. `AP1GameState`에 아직 정식 매치 타이머가 없어서(로드맵 3번 항목, 미착수) 월드 시간을 근사치로 대신 씀 — 나중에 GameState가 생기면 그쪽 권위 시계로 교체 예정. **`SpawnMonster()`는 반드시 `SpawnActorDeferred`+`FinishSpawning()`을 쓴다** — 즉시 스폰(`SpawnActor`)은 호출 안에서 `BeginPlay()`를 동기 실행해버려 스폰 "이후"의 `SetMonsterLevel()` 호출이 이미 늦어버린다(투사체 바운스 설정 버그와 동일한 타이밍 함정 — `AP1JungleMonsterCharacter::SetMonsterLevel()`은 반드시 `FinishSpawning()` 전에 호출해야 `ApplyDefaultAttributes()`가 정확한 레벨로 GE를 적용함). **다중 몬스터 스폰(배열 기반, 2026-07-29)**: `CurrentMonster` 단일 포인터를 `CurrentMonsters` 배열로 교체 — `MonsterCount`(기본 1, 기존 캠프와 동일 동작)만큼 `SpawnMonster()`가 루프를 돌며 각 인스턴스를 스폰한다. `MonsterCount>=2`면 `ComputeSpawnOffset(Index)`가 `PackSpawnRadius` 반경의 원형 배치 오프셋을 계산해 서로 겹치지 않게 흩뿌리고, **각 몬스터의 `HomeLocation`은 앵커 중심이 아니라 자신이 실제로 스폰된 좌표**로 설정한다(무리끼리 서로 다른 리시 기준점을 가져야 한 마리를 유인해도 나머지가 자기 자리에 남아있는 자연스러운 동작이 됨). `OnMonsterDied()`는 죽은 개체만 배열에서 제거하고, **`CurrentMonsters.Num()==0`(무리 전체 전멸)일 때만** 리스폰 타이머를 시작 — 무리 중 일부만 죽었으면 타이머가 안 돌고 나머지가 계속 싸운다 |

**정글 몬스터 사망/리스폰(신규, `AP1JungleMonsterCharacter`)**: 히어로의 `State.Dead`(그 자리에서 GE 만료까지 대기 후 리스폰) 패턴과 다르게 설계됨 — 몬스터는 `Event.Character.Died`를 구독해(`OnDiedEventReceived`) 받는 즉시 `OnMonsterDied` 델리게이트를 브로드캐스트하고 스스로 `Destroy()`한다. 캠프 스포너가 완전히 새 인스턴스를 다시 만드는 방식이 "죽었다 사라지고 나중에 다시 나타나는" 몬스터 리스폰의 표준적인 형태이기 때문(히어로는 "죽어도 그 자리, 잠시 후 부활"이 더 자연스러워 반대로 설계됨). 이와 짝을 맞춰 생성자에 `AutoPossessAI = PlacedInWorldOrSpawned`도 추가됨 — 기본값(Disabled)이면 `AIControllerClass`가 지정돼 있어도 `SpawnActor`로 런타임 스폰된 몬스터를 아무도 Possess하지 않아 BT가 영원히 안 돎(레벨에 직접 배치해 테스트하는 경우도 함께 커버).

**스턴 중에도 계속 움직이던 버그 수정(신규, 2026-08-17)**: 히어로는 `AP1PlayerController::HandleMove()`가 입력 단계에서 `State.Stunned`를 직접 체크해 이동을 막지만, 이 캐릭터는 플레이어 입력이 아니라 BT/AIController가 직접 이동을 처리해서 그 체크 경로를 아예 안 거친다 — 그래서 스턴을 맞아도 계속 걸어오는 버그가 있었다(공격 자체는 베이스 `UP1GameplayAbility`의 `ActivationBlockedTags`에 `State.Stunned`가 이미 포함돼 있어 원래도 막혀 있었음 — 이동만 새는 구멍이었음). `OnStunTagChanged()`(신규, `BeginPlay()`에서 `RegisterGameplayTagEvent(TAG_State_Stunned, NewOrRemoved)`로 구독)가 0→양수 전이 시 `AIController::StopMovement()`(진행 중이던 이동 즉시 정지)+`BrainComponent::PauseLogic()`(BT 틱 자체 정지)를, 양수→0 전이 시 `ResumeLogic()`을 호출한다(`TargetActor` 블랙보드 키가 남아있으면 Combat 브랜치가 자연스럽게 이어짐 — 별도 복구 로직 불필요). 사망 처리(`OnDiedEventReceived`)가 쓰는 `StopMovement`+`StopLogic` 패턴과 정확히 대응(다만 스턴은 재개 가능하다는 점만 다름).

### UI (`Source/P1/UI/`)

디렉토리 구조: `UI/HUD/`, `UI/Widget/`, `UI/WidgetController/`

**Widget:**

| 클래스 | 상태 | 비고 |
|---|---|---|
| `UP1UserWidget` | 완료 | 모든 위젯 베이스. `SetWidgetController()` → `OnWidgetControllerSet()` 패턴 |
| `UP1SegmentedBarWidget` | 완료 | `UProgressBar`(FillBar) + NativePaint 구분선. `SetValues(current, max, regen)`. `SegmentSize`/`DividerColor` EditAnywhere. **`FillColor`/`BackgroundColor`도 EditAnywhere+`SetFillColor()`/`SetBackgroundColor()`(BlueprintCallable)로 노출** — 이 위젯을 배치하는 쪽(WBP_P1Overlay에서 HealthBar/ManaBar 인스턴스마다, 또는 위젯 컨트롤러 런타임 코드)에서 색을 지정. `NativePreConstruct`에서 적용해 디자이너 미리보기에도 즉시 반영됨. Fill은 `UProgressBar::SetFillColorAndOpacity()`로 바로 되지만, Background는 단순 색상 프로퍼티가 없어서(Style의 `BackgroundImage` 브러시 틴트로만 제어 가능) `GetWidgetStyle()`로 스타일을 복사해 `BackgroundImage.TintColor`만 바꾸고 `SetWidgetStyle()`로 재적용하는 방식 사용 |
| `UP1SkillIconWidget` | 완료 | 스킬 아이콘. `CooldownOverlay`(UImage, MID로 CooldownPercent 파라미터), `CooldownText`. `StartCooldown()`/`ClearCooldown()`. NativeTick으로 0.1s 단위 업데이트. **`SkillIconImage`도 마름모(다이아몬드) 모양을 내기 위해 CooldownOverlay와 같은 MID 패턴 사용** — 위젯을 45도 회전시키면 아이콘 그림 자체가 기울어지므로, 대신 UV 기반 마름모 알파 마스크 머티리얼(`IconTexture` Texture Parameter 필요)을 Brush로 설정해두면 `NativeConstruct`가 MID를 만들고 `SetSkillIcon()`이 텍스처 파라미터만 갈아끼운다. WBP의 Brush가 마스크 머티리얼이 아니면(일반 텍스처/빈 Brush) `IconMID`가 null로 남아 예전처럼 텍스처를 직접 Brush에 설정하는 방식으로 자동 폴백(하위 호환). **스킬 포인트 투자 UI(신규)**: `InvestButton`(`UWidget*`, BindWidgetOptional — 클릭을 받지 않는 순수 시각적 표시기. 투자는 클릭이 아니라 `AP1PlayerController`의 Ctrl+스킬키로 처리하므로 실제 타입은 UButton이어도 무방, `SetVisibility`만 호출됨). `SetInvestButtonVisible(bool)`은 `Collapsed`가 아니라 `SelfHitTestInvisible`/`Hidden`을 씀 — `Collapsed`를 쓰면 아이콘 위 칸을 차지하는 VerticalBox 구조상 꺼질 때마다 스킬 아이콘이 위로 밀려 올라가는 문제가 있어, 레이아웃 공간은 유지하고 보이기만 껐다 켠다. `SetLocked(bool)` — 어빌리티 Level이 0(미투자)이면 `SkillIconImage`에 `CooldownTint`를 적용해 쿨다운처럼 어둡게 표시(`bIsLocked` 플래그로 `ClearCooldown()`과 우선순위 조율, 실전에서는 잠긴 어빌리티가 쿨다운을 탈 수 없어 겹칠 일 없음) |
| `UP1FloatingStatusWidget` | 완료 | 캐릭터 머리 위 체력/마나 위젯. `OnWidgetControllerSet()`에서 FloatingStatusWidgetController 델리게이트 바인딩. **스턴 표시**: `StunProgressBar`(`UProgressBar*`)+`StunLabelText`(`UTextBlock*`), 둘 다 BindWidgetOptional. 컨트롤러의 `OnStunStateChanged(bIsStunned, Duration)`을 받아 표시(라벨="Stun", 바=Duration을 100%로 시작)하고, `NativeTick`이 `RemainingStunDuration`을 매 프레임 감소시켜 바를 줄인다. **숨김은 NativeTick의 카운트다운이 아니라 오직 컨트롤러가 `bIsStunned=false`를 브로드캐스트할 때(=태그가 빠질 때)만** 한다(아래 알려진 버그의 프록시 카운트다운 사유). **표시/숨김을 Visibility/RenderOpacity가 아니라 콘텐츠(SetText ""↔"Stun", SetPercent 0↔n)로만 처리하는 게 핵심** — WidgetComponent 렌더타겟은 자식의 콘텐츠 변경엔 다시 그리지만 Visibility/Opacity 변경엔 안 그려서, 값은 바뀌는데 화면엔 안 반영되던 함정이 있었다(아래 알려진 버그 참고) |
| `UP1DamageNumberWidget` (`Widget/`) | 완료 | 데미지 팝업 텍스트 — 위젯 컨트롤러/ASC 연결이 필요 없는 1회성 값 표시라 `UP1UserWidget`이 아니라 `UUserWidget` 직접 상속(`UP1PreGameHUDWidget`과 같은 이유). `DamageText`(BindWidget)에 `SetDamageAmount(float)`가 반올림한 정수를 세팅 — 색상/폰트/등장 애니메이션은 전부 WBP 담당 |
| `AP1DamageNumberActor` (`UI/`) | 완료 | 데미지 팝업 1회성 액터. `AP1PlayerController::ClientShowDamageNumber()`가 **로컬(클라이언트 전용) SpawnActor**로 생성 — 서버가 존재를 전혀 모르고 리플리케이트되지도 않아 "내가 입힌 데미지만 나에게 보인다"(LoL/Dota 컨벤션)가 스폰 메커니즘 자체로 자연히 충족된다. `FloatingStatusComponent`와 동일한 `UP1FloatingWidgetComponent`(Screen space+카메라거리 스케일) 재사용. `Tick()`이 `RiseSpeed`로 월드 위치를 위로 이동시키는 것만 C++이 소유(게임 오브젝트 위치 이동이지 "위젯 레이아웃/스타일 애니메이션"이 아니라서 — `MulticastPlayMovingParticleEffect`와 같은 구분 기준), 페이드아웃 등 나머지는 WBP UMG Animation 몫. `LifeSpanSeconds` 경과 시 `SetLifeSpan()`으로 자동 파괴 |
| `UP1PreGameHUDWidget` | 완료 (C++, WBP 배치 대기) | PreGame 최상위 HUD — **Login/Signup/MatchQueue 화면을 별도 위젯 클래스(아래 3개)로 분리**하고, 이 HUD는 셋을 멤버(`LoginWidget`/`SignupWidget`/`MatchQueueWidget`, 전부 BindWidget 필수)로 들고 "어느 화면을 보여줄지"만 오케스트레이션한다 — 처음엔 위젯 하나에 패널 3개(Visibility 토글)로 구현했다가, 화면당 클래스를 분리하는 게 SRP상 더 낫다는 판단으로 재구성함(자식 위젯이 늘어나는 대신 각 클래스가 자기 화면 입력만 알면 됨, WBP 디자이너도 화면별로 따로 작업 가능). **`UP1UserWidget`(위젯 컨트롤러+ASC 패턴)을 상속하지 않고 순수 `UUserWidget`을 직접 상속** — 데이터 소스가 GAS/ASC가 아니라 `UP1BackendSubsystem`(HTTP)이라 위젯 컨트롤러 패턴이 안 맞기 때문. **책임 분리**: 자식 위젯(`UP1LoginWidget`/`UP1SignupWidget`/`UP1MatchQueueWidget`)은 자기 입력 필드를 읽어 `UP1BackendSubsystem`에 요청(Login/Signup/JoinQueue)을 보내는 것과, 화면 전환 "의사"를 델리게이트로 알리는 것(`OnRequestSignupScreen`/`OnRequestLoginScreen`)까지만 담당 — 결과 처리(상태 메시지, 실제 화면 전환, 매칭 성사 시 `ClientTravel`)는 전부 이 HUD가 `UP1BackendSubsystem`의 4개 델리게이트(`OnSignupComplete`/`OnLoginComplete`/`OnQueueJoined`/`OnMatchFound`)를 직접 구독해서 처리한다 — 자식은 백엔드 결과를 몰라도 됨. `EP1LobbyScreen` enum(Login/Signup/MatchQueue)을 받는 private `ShowScreen()`이 나머지 두 자식을 Collapsed로 접고 지정한 것만 Visible로 전환. 공용 `StatusText`(BindWidgetOptional)는 세 자식 위젯 바깥(항상 보이는 자리)에 배치. **시작 화면 판단(2026-08-12)**: `NativeConstruct()`가 무조건 Login으로 시작하지 않고 `Backend->IsLoggedIn()`(토큰 유무)을 확인해 로그인 상태면 바로 MatchQueue로 시작한다 — 매치 종료 후 `AP1PlayerController::ReturnToLobby()`로 PreGame 레벨에 다시 들어올 때, `ClientTravel`이 이 위젯/PlayerController/PlayerState를 전부 새로 만들어버려 "마지막에 보던 화면" 자체가 남아있지 않기 때문에, 그 대신 레벨 전환에도 살아남는 유일한 근거(`UP1BackendSubsystem`의 로그인 토큰, GameInstanceSubsystem이라 `ClientTravel`을 넘어 유지됨)로 시작 화면을 매번 새로 도출한다 — "이전 상태 복원"이 아니라 "유일하게 남은 근거로 재판단" |
| `UP1LoginWidget` | 완료 (C++, WBP 배치 대기) | Login 화면 전용. `UsernameBox`/`PasswordBox`/`LoginButton`(BindWidget)+`GoToSignupButton`(BindWidgetOptional). 로그인 버튼 클릭 시 `UP1BackendSubsystem::Login()` 호출, "회원가입 화면으로" 버튼 클릭 시 `OnRequestSignupScreen`(dynamic multicast) 브로드캐스트만 하고 화면 전환 자체는 하지 않음(HUD가 구독해서 처리) |
| `UP1SignupWidget` | 완료 (C++, WBP 배치 대기) | Signup 화면 전용 — `UP1LoginWidget`과 대칭 구조. `UsernameBox`/`PasswordBox`/`SignupButton`(BindWidget)+`GoToLoginButton`(BindWidgetOptional), `OnRequestLoginScreen` 브로드캐스트 |
| `UP1MatchQueueWidget` | 완료 (C++, WBP 배치 대기) | 매칭 대기 화면 전용(이름을 `Lobby`가 아니라 `MatchQueue`로 지은 이유: "Lobby"는 이미 PreGame 레벨/시스템 전체를 가리키는 이름이라 화면 하나에 재사용하면 헷갈림). **`QueueButton` 하나가 "매칭 시작"/"취소" 두 상태를 겸함** — 로컬 `bIsQueued` 플래그로 판단해 클릭 시 `JoinQueue()` 또는 `LeaveQueue()` 분기 호출, `QueueButtonLabel`(BindWidgetOptional, 버튼 안에 넣은 TextBlock — `UButton` 자체엔 `SetText`가 없어 별도 바인딩 필요)로 라벨 전환. 상태 갱신은 `UP1BackendSubsystem`의 `OnQueueJoined`/`OnQueueLeft`를 직접 구독해서 처리(HUD가 처리하는 공용 상태 텍스트/화면 전환/`ClientTravel`과는 별개 관심사 — 둘 다 같은 델리게이트를 동시에 구독해도 무방). **히어로 픽커 내장(신규, 2026-08-18)**: `HeroPickerWidget`(`UP1HeroPickerWidget`, BindWidgetOptional)을 이 화면 안에 얹는다 — 별도 "캐릭터 선택" 화면/`EP1LobbyScreen` 상태를 만들지 않고 매칭 화면에 위젯 하나로 통합한 것(처음엔 별도 화면 안이었으나 더 단순한 방향으로 재설계됨). 기존에 유일하게 있던 큐 상태 전이 지점인 `SetQueuedState(bool)`(위 라벨 전환 로직) 안에 `HeroPickerWidget->SetLocked(bIsQueued)` 한 줄만 추가해 "대기 중엔 히어로 변경 불가"를 새 상태 변수 없이 구현 |
| `UP1HeroPickerWidget`/`UP1HeroSelectSlotWidget`/`FP1HeroDefinition`(`Characters/P1HeroTypes.h`) | 완료 (C++, WBP/DataTable 배치 대기) | **캐릭터 선택(신규, 2026-08-18)** — `FP1HeroDefinition`(`HeroId` FName + `HeroClass` `TSubclassOf<AP1HeroCharacter>`)을 담는 `UDataTable`(`DT_Heroes`) 하나가 히어로 카탈로그 전체다. 표시용 이름/포트레이트는 중복 저장하지 않고 `HeroClass.GetDefaultObject<AP1HeroCharacter>()`의 `GetHeroDisplayName()`/`GetHeroPortrait()`를 그대로 읽는다. 이 테이블을 **클라(이 위젯, 표시용)와 서버(`AP1ArenaGameMode::HeroTable`, 화이트리스트 겸 검증용)가 각자 독립된 `EditDefaultsOnly UDataTable*`로 참조**한다(둘 다 에디터에서 같은 `DT_Heroes` 에셋을 지정해야 함 — `DT_ShopItems`를 `AP1PlayerState`와 상점 위젯이 각자 따로 참조하는 기존 패턴과 동일). **`UP1HeroSelectSlotWidget`**(리프)은 `UP1ShopItemSlotWidget`의 "클릭 시 자기 데이터만 브로드캐스트, 하이라이트는 부모가 관리" 패턴을 그대로 따름 — `SetSlotData(FP1HeroDefinition)`, `FOnHeroSlotSelected OnSelected(FName HeroId)`, `SetSelectedVisual(bool)`(`SelectedHighlight` BindWidgetOptional 토글). **`UP1HeroPickerWidget`**(부모, `UP1MatchQueueWidget`에 내장)은 `NativeConstruct()`에서 `HeroTable->GetAllRows`로 슬롯을 생성해 `SlotsByHeroId`(TMap)에 캐싱하고, 슬롯 선택 시 `Backend->SetSelectedHeroId(HeroId)` + 전체 슬롯 하이라이트 갱신 — **별도 "확인" 버튼 없이 클릭 즉시 확정**된다(`SelectedHeroId` 기본값이 Greystone이라 "미선택" 상태 자체가 없어서 즉시 커밋해도 안전). `SetLocked(bool)`은 `HeroSlotContainer->SetIsEnabled(!bLocked)` 한 줄 — UMG의 `SetIsEnabled`가 자식 버튼 전체에 전파되므로 개별 슬롯을 순회할 필요가 없다 |
| `UP1ShopWidget`+`UP1ShopStatsWidget`+`UP1ShopItemListWidget`+`UP1ShopItemDetailWidget`+`UP1ShopInventoryWidget`+`UP1ShopItemSlotWidget`+`UP1ShopInventorySlotWidget`+`UP1ShopStatChipWidget`+`UP1ShopUniqueAbilityWidget` | 완료 (C++, WBP/DataTable 배치 대기) | 상점 화면. Scoreboard와 달리 Gold/Inventory/스탯 변경을 실시간 반영해야 해서 `UP1WidgetController` 패턴을 쓴다(`UP1ShopWidgetController` 별도 항목 참고). **위젯 생성 시점도 Scoreboard(PlayerController가 첫 입력 시 지연 생성)와 다르다** — `AP1HUD::InitShop()`이 `InitOverlay()`와 같은 시점(`AP1HeroCharacter::HandleAbilitySystemReady()`)에 한 번만 만들어 Collapsed로 뷰포트에 올려두고, `AP1PlayerController::HandleToggleShop()`은 Visibility만 토글(+입력모드 전환, 아래 참고).<br><br>**레이아웃(2026-08-06, LoL 아레나 상점 참고 3열 구조로 확정)**: 좌측 `UP1ShopStatsWidget`(현재 전투 스탯) / 중앙 `UP1ShopItemListWidget`(역할군 필터링 카탈로그)+그 아래 `UP1ShopInventoryWidget`(보유 아이템 6칸) / 우측 `UP1ShopItemDetailWidget`(선택 아이템 상세). `UP1ShopWidget`은 역할군 탭 버튼 6개(`CarryTabButton`~`SupportTabButton`, 개수 고정이라 동적 생성 없이 이름으로 직접 바인딩)만 관리하고, 나머지 4개 자식 전부에게 `SetWidgetController()`로 같은 컨트롤러를 그대로 전파한다(D1류 GAS 코스에서 익숙한 패턴 — 부모가 매번 계산해서 떠먹여주는 대신 각 자식이 스스로 필요한 델리게이트를 구독). `UP1ShopWidgetController`엔 `GetP1PlayerState()`+`GetAbilitySystemComponent()` 두 타입 캐스팅 접근자가 있어 자식들이 여기서 필요한 걸 직접 꺼내 쓴다.<br><br>**인터랙션(2026-08-06, 두 번 손봄)**: 카탈로그에서 좌클릭=선택(`UP1ShopItemSlotWidget`이 `OnSelected` 브로드캐스트 → `UP1ShopItemListWidget`이 중계 → `UP1ShopWidget::HandleItemSelected()`가 `ItemDetailWidget->ShowItem()` 호출 — 이 선택 라우팅만은 순수 로컬 UI 상호작용이라 컨트롤러 전파와 무관하게 부모가 계속 중개함). 구매는 **카탈로그 칸(`UP1ShopItemSlotWidget`)을 더블클릭**하면 된다 — **`NativeOnMouseButtonDoubleClick()` 방식은 실제로 동작하지 않아 폐기했다(2026-08-11)**: `SelectButton`이 칸 전체를 덮는 투명 버튼이라 마우스 입력을 항상 먼저 가로채고, `NativeOnMouseButtonDoubleClick`은 위젯 자기 자신이 히트테스트 대상일 때만 호출되므로 버튼 뒤의 부모 위젯까지 이벤트가 도달하지 않는다(루트 Visibility를 Visible로 해도 무관 — 히트테스트에서 버튼이 항상 이김). `UButton`엔 `OnDoubleClicked` 이벤트 자체가 없어서, 대신 **`HandleSelectClicked()`(SelectButton의 `OnClicked` 핸들러) 안에서 `LastClickTimeSeconds`로 두 번째 클릭이 0.35초 안에 들어오는지 직접 판정**해 더블클릭이면 `ServerBuyItem` 호출 — 이미 보유 중이면 클라이언트에서 조용히 막고 RPC조차 안 보냄, 서버도 `Inventory.Contains()`로 다시 거부하니 이중 방어. **처음엔 우측 상세 패널을 더블클릭하는 방식이었다가, 상세 패널은 순수 정보 열람용으로 남기고 목록 쪽으로 옮김**. 판매는 인벤토리 칸을 **우클릭**하면 된다(`UP1ShopInventorySlotWidget::NativeOnMouseButtonUp()` 오버라이드가 `RightMouseButton`일 때 직접 `ServerSellItem` 호출 — 이쪽은 `SelectButton` 같은 전체를 덮는 자식 버튼이 없어서 `NativeOnMouseButtonUp`이 정상적으로 위젯 자신에 도달한다). `UP1ShopItemDetailWidget`엔 클릭 인터랙션이 전혀 없다 — 순수 정보 표시만. 인벤토리 칸(우클릭)은 WBP 루트의 Visibility가 Visible이어야 이벤트를 받는다(SelfHitTestInvisible이면 안 들어옴). **같은 아이템 중복 구매는 금지**(2026-08-06에 잠깐 허용으로 바꿨다가 되돌림 — "깡스탯은 중복 적용, 특수효과는 한 번만" 같은 걸 하려면 GE를 둘로 쪼개고 스택/제거 로직을 따로 관리해야 해서 복잡도가 커진다는 이유. 지금은 고유 능력별로 별도 GE를 두는 쪽으로 그 문제 자체를 우회함, 아래 참고).<br><br>**`UP1ShopStatsWidget`**은 `UP1AttributeSet`의 전투 스탯 14종(PhysicalPower/AttackSpeed/MagicalPower/PhysicalPenetration/MagicalPenetration/PhysicalArmor/MagicalArmor/MaxHealth/HealthRegen/LifeSteal/MaxMana/ManaRegen/AbilityHaste/Tenacity)을 각각 `AbilitySystemComponent->GetGameplayAttributeValueChangeDelegate()`에 `AddUObject`로 직접 구독(트레일링 payload로 대상 TextBlock+퍼센트표시여부를 넘겨 16개를 반복 없이 처리) — **`CriticalChance`/`CriticalDamage` 추가(2026-08-07)**: 처음엔 `UP1AttributeSet`에 대응 속성이 없어서 스탯 패널에서 제외했었는데, 이후 두 속성을 `Attributes|Combat` 카테고리에 추가해서(`CriticalChance`는 [0,1] 클램프, `CriticalDamage`는 1.0 미만으로 못 내려감, 기본값 0%/150%) 이제 나머지 스탯과 동일하게 `CriticalChanceText`/`CriticalDamageText`로 표시된다(둘 다 `bAsPercent=true`). **`AttackSpeed`만 `bAlreadyPercent=true`로 다르게 처리한다**(2026-08-07 버그 수정 — `AttackSpeed`는 어빌리티 PlayRate 계산이 100 기준 퍼센트 스케일로 직접 나눠 쓰기 때문에 다른 %스탯처럼 ×100을 하면 안 됨. 안 그러면 120 같은 정상값이 12000%로 보임 — 위 AttributeSet 목록의 경고 박스 참고). 전부 `BindWidgetOptional`이라 WBP에 원하는 것만 배치해도 된다.<br><br>**`UP1ShopInventoryWidget`**은 `PlayerState->GetMaxInventorySlots()`(신규 getter)개만큼 항상 `UP1ShopInventorySlotWidget`을 채운다(빈 칸도 포함 — `Inventory.IsValidIndex(Index)`가 false면 `ItemData=nullptr`로 넘겨 빈 아이콘 표시), `Controller->OnInventoryChanged` 구독으로 구매/판매마다 통째로 다시 그린다. **`UP1ShopInventorySlotWidget`**은 아이콘만 표시하는 순수 위젯(이름/가격 없음 — 상세 조회 대상이 아니라 우클릭으로 바로 파는 대상이라 컨트롤러도 필요 없어 `UUserWidget` 직접 상속). **`FP1ShopItemData::DisplayStats`**(`TArray<FP1ItemDisplayStat>`, `Label`(FText)+`Value`(float)+`bAsPercent`)는 상세 패널에 "+45 물리 공격력" 식 칩으로 나열할 **순수 표시용** 데이터 — 실제 스탯 적용은 여전히 `StatEffectClass` GE가 담당하므로 두 수치가 어긋나지 않게 사람이 직접 맞춰야 한다(자동 동기화 없음). `FGameplayAttribute`를 직접 안 쓰고 텍스트 라벨로 받은 이유는 `FGameplayAttribute`가 리플렉션 기반이라 DataTable JSON 임포트로 못 채우기 때문.<br><br>**고유 능력(2026-08-06 추가)**: LoL 아레나의 "절단"/"디보어" 같은 이름 붙은 특수효과를 위해 `FP1ItemUniqueAbility`(`AbilityName`+`AbilityDescription`+`EffectClass`) 구조체와 `FP1ShopItemData::UniqueAbilities`(배열, 아이템당 0개 이상) 추가 — 깡스탯(`StatEffectClass`, GE 하나)과 달리 고유 능력은 **각자 자기 GE를 따로** 갖는다(나중에 "기본 공격 시에만 발동" 같은 조건이 생기면 그 GE 하나만 손보면 되게). `ServerBuyItem`이 `StatEffectClass`+`UniqueAbilities[].EffectClass`를 전부 적용해 핸들들을 모아 `AP1PlayerState::ActiveItemEffects`(`TMap<FName, TArray<FActiveGameplayEffectHandle>>`, 예전엔 `ActiveItemStatEffects`라는 이름의 핸들 하나짜리 맵이었음)에 저장하고, `ServerSellItem`이 그 배열 전체를 제거한다. 이 변경과 함께 아이템 전체를 아우르는 범용 `Description` 필드는 **삭제**했다 — 고유 능력 설명이 그 역할을 대신하므로 필요 없어짐.<br><br>**상세 패널 구조를 텍스트 뭉치에서 반복 위젯 리스트로 전환(2026-08-06 세 번째 변경)**: 처음엔 `StatsText`/`UniqueAbilitiesText` TextBlock 하나에 줄바꿈으로 다 이어붙이는 방식이었는데, 스탯을 한 줄에 2개씩 그리드로 배치하고 싶다는 요구로 반복 생성 방식으로 바꿨다. `UP1ShopItemDetailWidget`이 `StatsContainer`(WrapBox 등 Panel, `StatChipWidgetClass`로 `DisplayStats` 개수만큼 `UP1ShopStatChipWidget` 반복 생성)와 `UniqueAbilitiesContainer`(VerticalBox 등 Panel, `UniqueAbilityWidgetClass`로 `UniqueAbilities` 개수만큼 `UP1ShopUniqueAbilityWidget` 반복 생성)를 매 `ShowItem()`마다 `ClearChildren()`+다시 채움 — 카탈로그 목록과 같은 패턴. **`UP1ShopStatChipWidget`**은 `ValueText`(TextBlock) 하나로 "+45 물리 공격력" 포맷팅, **`UP1ShopUniqueAbilityWidget`**은 `AbilityNameText`+`AbilityDescriptionText` 두 TextBlock — 둘 다 컨트롤러/PlayerState 필요 없는 순수 표시 위젯. **WrapBox에서 "한 줄에 정확히 2개씩"을 보장하려면 칩 내용 길이에 따라 자동 줄바꿈에 맡기지 말고, `WBP_ShopStatChip` 루트를 `SizeBox`로 감싸 고정 너비(예: `StatsContainer` 전체 너비의 절반)를 줘야 한다** — 안 그러면 짧은 텍스트 스탯끼리는 3개가 붙고 긴 텍스트는 혼자 줄바꿈되는 식으로 들쭉날쭉해진다. 클라이언트 쪽 선제 검증(골드 부족 시 등)은 없음 — 서버가 항상 재검증. **입력**: `ShopAction`(토글, `Started`만 바인딩 — Scoreboard의 Tab 홀드와 달리 마우스 클릭이 필요해서 홀드가 아니라 토글), `AP1PlayerController::HandleToggleShop()`이 열 때 `FInputModeGameAndUI`+`bShowMouseCursor=true`(WASD는 유지하고 마우스만 커서로 풀어 클릭 가능), 닫을 때 `FInputModeGameOnly`+커서 숨김으로 복귀. **닫는 경로가 두 개라 공용 함수로 뺐다(2026-08-08)**: `ShopAction` 토글과 `WBP_Shop`의 `CloseButton`(`UP1ShopWidget::HandleCloseClicked()`) 둘 다 `AP1PlayerController::CloseShop()`을 호출 — 원래 `HandleCloseClicked()`가 위젯 자기 Visibility만 Collapsed로 바꾸고 끝나서, X버튼으로 닫으면 입력모드가 `GameAndUI`에 그대로 남고 마우스 커서도 안 사라지는 버그가 있었음. |
| `UP1ScoreboardWidget`/`UP1ScoreboardTeamWidget`/`UP1ScoreboardPlayerRowWidget`/`UP1ScoreboardWidgetController` | 완료 (C++, WBP 배치 대기) | Tab 홀드 점수판 — 3중 구조(전체→팀별→개인)로 의도적으로 분리: 종료조건이 아직 설계 안 돼 팀 단위 점수를 어디에 얹을지 미정이라, 나중에 팀 점수가 생기면 `UP1ScoreboardTeamWidget`에 프로퍼티만 추가하면 되게 미리 확장 지점을 열어둔 것(지금은 추측성 프로퍼티를 넣지 않음) — **그 팀 점수가 실제로 생겼다(2026-08-12, 아래 매치 결과 항목 참고)**: `UP1ScoreboardTeamWidget`에 `TeamScoreText`(TextBlock, 선택) 추가, `RefreshTeam()`이 `GameState->GetTeamKillScore(DesignatedTeamId)`를 읽어 표시 — 매치 진행 중엔 Tab 스코어보드에서 실시간 순위 확인용, 매치 종료 후엔 결과창(`UP1MatchResultWidget`)에서 최종 스코어 확인용으로 같은 위젯이 그대로 재사용된다. **컨트롤러 전파(2026-07-31 변경)**: 상점의 `SetWidgetController()` 전파 패턴과 통일하기 위해 `UP1ScoreboardWidget`/`UP1ScoreboardTeamWidget`도 `UUserWidget`이 아니라 `UP1UserWidget`을 상속하게 됐다(`UP1ScoreboardPlayerRowWidget`만 예전처럼 `UUserWidget` 직접 상속 유지 — 반복 생성되는 개인 행이라 상점의 `UP1ShopItemSlotWidget`과 같은 이유로 컨트롤러가 필요 없음). 다만 Scoreboard는 애초에 ASC 반응형 데이터가 없는 스냅샷 UI라, 새로 만든 `UP1ScoreboardWidgetController`엔 **델리게이트가 하나도 없다** — `GetP1GameState()` 접근자 하나만 제공해 위젯 트리에 GameState 참조를 전파하는 용도로만 쓴다(상점처럼 `AP1HUD` 레지스트리를 타지 않고, `AP1PlayerController::HandleScoreboardShow()`가 스코어보드 위젯을 최초 생성하는 시점에 `NewObject<UP1ScoreboardWidgetController>()`로 직접 만들어 `SetWidgetControllerParams()`+`SetWidgetController()`를 그 자리에서 바로 호출 — `BP_P1HUD`에 노출되는 EditDefaultsOnly 클래스 프로퍼티가 없어 이 컨트롤러는 에디터 작업이 따로 필요 없다). **팀 위젯은 런타임 생성이 아니라 WBP 디자이너에 미리 배치**(2026-07-30 변경) — `UP1ScoreboardWidget::OnWidgetControllerSet()`이 `WidgetTree->ForEachWidget()`으로 미리 배치된 `UP1ScoreboardTeamWidget` 인스턴스들을 찾아 같은 컨트롤러를 그대로 `SetWidgetController()`로 넘겨주고, `RefreshScoreboard()`는 그 인스턴스들을 다시 훑어 각자의 `RefreshTeam()`을 부르기만 한다(`TeamWidgetClass`/`TeamListContainer`/`TeamWidgetsByTeamId` 같은 동적 생성용 프로퍼티는 없음). **`UP1ScoreboardTeamWidget::RefreshTeam()`**(예전 `SetTeamData(TArray<AP1PlayerState*>)`을 대체 — 부모가 미리 필터링해서 넘겨주는 대신, 이제 `Cast<UP1ScoreboardWidgetController>(WidgetController)->GetP1GameState()->PlayerArray`를 자기 `GetDesignatedTeamId()`(EditAnywhere, 인스턴스별로 0/1/2 지정)로 직접 걸러 스스로 채움)가 매번 `PlayerListContainer->ClearChildren()` 후 `UP1ScoreboardPlayerRowWidget`을 팀원 수만큼 재생성(홀드 중에만 잠깐 뜨는 UI라 풀링 없이도 비용 무시할 수준). **`UP1ScoreboardPlayerRowWidget::SetPlayerData()`**는 닉네임=`PlayerState->GetPlayerName()`(엔진 기본 제공, `TruncateByDisplayWidth()`로 한글/영문대문자=2, 나머지=1 폭 계산해 16폭 제한), 캐릭터=`PlayerState->GetHeroDisplayName()`(`AP1PlayerState`에 추가한 **복제되는** `FText` 필드 — `PlayerState->GetPawn()`은 원격 클라이언트에서 신뢰할 수 없어서 대신 씀, `AP1HeroCharacter::HandleAbilitySystemReady()`가 서버에서 매 스폰마다 채워줌), KDA=`GetKills()/GetDeaths()/GetAssists()`. 실시간 델리게이트 구독이 아니라 **스냅샷**(홀드해서 열 때마다 `RefreshScoreboard()` 재호출) 방식 — 필요해지면 나중에 `OnKDAChangedNative` 구독으로 실시간화 가능 |
| `UP1MatchResultWidget` | 완료 (C++, WBP 배치 대기) | **매치 종료 결과창(신규, 2026-08-12)** — `AP1PlayerController::ClientShowMatchResult()`가 매치 종료 시점에 한 번 생성해 띄운다. `UP1ScoreboardWidget`을 거의 그대로 본떠 만들었다: `UP1UserWidget` 상속, `WinningTeamText`(선택, `SetWinningTeamId()`가 "Team N 승리!" 포맷팅), `OnWidgetControllerSet()`이 `WidgetTree->ForEachWidget()`으로 미리 배치된 `UP1ScoreboardTeamWidget` 인스턴스에 컨트롤러 전파, `RefreshResult()`가 각 팀 위젯의 `RefreshTeam()` 호출 — **팀별 최종 KDA 목록은 새 위젯 클래스 없이 `UP1ScoreboardTeamWidget`/`UP1ScoreboardPlayerRowWidget`을 그대로 재사용**한다(둘 다 "GameState->PlayerArray를 팀별로 훑어 보여준다"는 동일한 요구). 컨트롤러도 새로 안 만들고 `UP1ScoreboardWidgetController`를 그대로 재사용(델리게이트 없이 GameState 접근만 제공하는 얇은 클래스라 결과창에 정확히 필요한 것과 같음) |

**HUD:**

| 클래스 | 상태 | 비고 |
|---|---|---|
| `UP1HUDWidget`+`UP1HUDInventoryWidget`+`UP1HUDInventorySlotWidget` | 완료 (C++, WBP 배치 대기) | 메인 HUD. HealthBar/ManaBar(UP1SegmentedBarWidget), SkillIcon_Q/E/R/RMB/Passive/LMB/Flash(UP1SkillIconWidget). **좌하단 플레이어 정보**: `LevelText`/`KDAText`/`GoldText`(UTextBlock, BindWidgetOptional)+`ExperienceBar`(UP1SegmentedBarWidget, HealthBar/ManaBar와 같은 타입 재사용) — `OnLevelChanged`("Lv. N")/`OnKDAChanged`("K / D / A")/`OnGoldChanged`("N G")/`OnExperienceChanged`(`ExperienceBar->SetValues(Current, Max(XPRequired,1))`, 0으로 나누기 방지로 1 클램프) 핸들러가 텍스트/바를 갱신. **우측하단 보유 아이템(읽기전용, 2026-08-08 추가)**: `InventoryWidget`(`UP1HUDInventoryWidget`, BindWidgetOptional)이 상점의 `UP1ShopInventoryWidget`과 같은 데이터(`AP1PlayerState::Inventory`)를 보여주지만 **우클릭 판매 없음** — 상점을 안 열어도 항상 보이는 자리라 실수로 팔리는 사고를 막으려고 의도적으로 뺐다(판매는 상점 열었을 때만 `UP1ShopInventorySlotWidget`에서). `UP1ShopWidgetController`를 새로 안 만들고 **HUD가 이미 쓰는 `UP1OverlayWidgetController`를 그대로 전파**받아 `GetP1PlayerState()`(베이스 `UP1WidgetController`로 승격, 아래 참고)만 꺼내 쓰고, 인벤토리 변경은 컨트롤러를 안 거치고 `AP1PlayerState::OnInventoryChangedNative`(네이티브 델리게이트)에 직접 구독 — Overlay 컨트롤러에 상점 전용 델리게이트를 옮겨 심지 않기 위한 선택. `UP1HUDInventorySlotWidget`은 아이콘만 표시하는 완전 읽기전용 위젯(클릭 핸들러 자체가 없음). **`UP1WidgetController::GetP1PlayerState()`를 베이스 클래스로 승격**(원래 `UP1ShopWidgetController`에만 있었음) — Overlay 컨트롤러도 같은 접근자가 필요해져서 중복 제거 겸 공용화. |
| `UP1MinimapWidget`+`UP1MinimapIconWidget` | 완료 (C++, WBP/배경 이미지/월드 범위 실측 배치 대기) | **미니맵(2026-08-12 신규)** — `HUD` 안에 `MinimapWidget`(BindWidgetOptional)으로 배치. **컨트롤러/ASC를 전혀 안 씀** — `UP1HUDWidget::RefreshMatchTime()`(`MatchTimeText`를 매 프레임 `GetWorld()->GetGameState<AP1GameState>()`로 직접 폴링)과 동일한 패턴을 그대로 따라, `NativeConstruct()`에서 `FTimerHandle`로 `RefreshIntervalSeconds`(기본 0.2초)마다 `RefreshMinimap()`을 예약한다. **시야는 서버 권위 안티치트 시스템이 아니라 전부 클라이언트 로컬 계산이다** — 캐릭터 위치는 이미 표준 `ACharacter` 무브먼트 리플리케이션으로 모든 클라이언트에 도달해 있으므로(3D 렌더링을 위해 애초에 숨길 수 없음), "적이 아군 시야 안에 있는지"는 미니맵 표시 여부만 클라이언트가 로컬로 걸러내는 순수 연출이다(진짜 정보 은닉이 필요해지면 나중에 서버 권위 vision/relevancy 시스템으로 대체 가능). `RefreshMinimap()`: (1) `GameState->PlayerArray`에서 로컬 팀 소속의 **살아있는**(`AbilitySystemComponent->HasMatchingGameplayTag(TAG_State_Dead)` 아닌) 아군 Pawn 위치를 수집, (2) 아군은 항상 표시하고 적은 그 아군 위치들과의 최단거리가 `VisionRadius`(기본 1500) 이내일 때만 표시 — `TMap<AP1PlayerState*, UP1MinimapIconWidget*>` 풀로 아이콘을 생성/갱신하고 더 이상 안 보이면 `RemoveFromParent()`+풀에서 제거, (3) **정글 캠프 아이콘** — `AP1JungleCampAnchor`는 의도적으로 논리플리케이트 액터라(`CurrentMonsters`는 서버 전용 배열) 직접 못 읽으므로, `NativeConstruct()`에서 `TActorIterator<AP1JungleCampAnchor>`로 캠프 위치만 한 번 캐싱해두고, 매 리프레시마다 그 위치 `CampDetectionRadius`(기본 500, `AP1JungleCampAnchor::PackSpawnRadius`가 private이라 못 읽어서 미니맵 전용 근사치로 따로 둠) 안에 살아있는 `AP1JungleMonsterCharacter`가 하나라도 있는지(`TActorIterator`로 매번 스캔, 몬스터~35마리×캠프7개=245회 거리비교라 0.2초 주기에 비용 무시 가능) 판단해 그 캠프 아이콘 Visibility를 토글한다(캠프 전멸→사라짐, 리스폰→다시 나타남). **좌표 변환**: `WorldToMinimapLocal()`이 `WorldBoundsMin`/`WorldBoundsMax`(EditDefaultsOnly, 아레나 월드 X/Y 범위 — 에디터에서 실측 필요) 기준 선형 보간 후 `MinimapIconContainer`(CanvasPanel)의 실제 크기를 곱해 로컬 좌표를 계산 — 배경 캡처 카메라가 정확히 수직/무회전이어야 이 매핑이 어긋나지 않는다. **팀 색상**: `TeamColors`(EditDefaultsOnly `TArray<FLinearColor>`, 인덱스=TeamId)를 미니맵 전용으로 새로 둠 — 이 프로젝트 어디에도 팀→색상 매핑이 없었음(Scoreboard도 그냥 "Team N" 텍스트), 실제로 다른 곳에서도 필요해지기 전까진 공용 유틸로 안 뽑는다. `UP1MinimapIconWidget`은 `IconImage`(Image) 하나+`SetIconColor()`뿐인 순수 리프 위젯(컨트롤러 불필요, `UUserWidget` 직접 상속) — 플레이어/캠프 아이콘 둘 다 재사용. |
| `AP1HUD` | 완료 | 위젯 컨트롤러 레지스트리. `InitOverlay()` → WBP 생성 + SetWidgetController + BroadcastInitialValues |

**WidgetController:**

| 클래스 | 상태 | 비고 |
|---|---|---|
| `UP1WidgetController` | 완료 | 베이스. `FWidgetControllerParams`(PC/PS/ASC/AS). `FOnAttributeChangedSignature` 델리게이트 정의 |
| `UP1OverlayWidgetController` | 완료 | 6개 델리게이트(Health/MaxHealth/HealthRegen/Mana/MaxMana/ManaRegen). ASC 어트리뷰트 변경 구독. **Progression 델리게이트(신규)**: `OnGoldChanged`/`OnExperienceChanged`(Gold/Experience 둘 다 GAS Attribute라 기존과 동일하게 `GetGameplayAttributeValueChangeDelegate()` 구독)는 Health/Mana와 같은 경로. `OnLevelChanged`/`OnKDAChanged`는 GAS Attribute가 아닌 `AP1PlayerState`의 plain int라 이 어트리뷰트 델리게이트 경로를 못 쓰므로, `BindCallbacksToDependencies()`에서 `AP1PlayerState`로 캐스트해 `OnCharacterLevelChangedNative`/`OnKDAChangedNative`(네이티브 델리게이트, PlayerState 섹션 참고)를 직접 구독한다. `BroadcastExperience()` 헬퍼가 Experience 변경 시**와** CharacterLevel 변경 시(XPRequired가 레벨마다 달라지므로) 양쪽에서 호출돼 `OnExperienceChanged(CurrentXP, XPRequired)`를 다시 쏜다. **스킬 포인트 투자 UI 델리게이트(신규)**: `OnAbilityInvestStateChanged(InputTag, bCanInvest)`/`OnAbilityLockedStateChanged(InputTag, bLocked)` — 둘 다 `BroadcastAbilityInvestState()`가 `ASC->GetActivatableAbilities()`를 순회하며 계산(`MaxAbilityLevel<=1`인 좌클릭/패시브는 스킵). `bCanInvest = SkillPoints>0 && Spec.Level<MaxAbilityLevel && CharacterLevel>=RequiredCharacterLevelForNextRank`, `bLocked = Spec.Level<=0`(포인트 유무와 무관한 별개 축 — 포인트가 없어도 미투자면 계속 잠김 표시). `PS->OnSkillPointsChangedNative`(레벨업으로 증가하든 투자로 감소하든 항상 이 델리게이트가 뜸 — 어빌리티 레벨 변경은 반드시 스킬 포인트 변경과 같은 서버 틱에서 함께 일어나므로 별도 "어빌리티 스펙 변경" 구독 없이 이 하나로 충분, `UAbilitySystemComponent::AbilitySpecDirtiedCallbacks`는 서버에서만 브로드캐스트되고 클라 replication 시엔 안 불려서 애초에 못 씀) + `BroadcastAbilityInfo()` 끝에서 호출로 초기 상태도 커버 |
| `UP1FloatingStatusWidgetController` | 완료 | 4개 델리게이트(Health/MaxHealth/Mana/MaxMana). 캐릭터별 per-instance 생성. **스턴(RefreshStun 통합)**: `OnStunStateChanged(bIsStunned, Duration)`을 쏘는 유일한 진입점이 `RefreshStun()`이고, 이걸 **두 트리거**가 각자 도착 시점에 호출한다 — (1) `ASC->RegisterGameplayTagEvent(State.Stunned)`(전원 복제, 표시/숨김 결정), (2) `AP1PlayerState::OnStunTimeChangedNative`(`StunEndServerTime` 복제 도착 시, 정확한 남은시간). **표시/숨김은 태그 카운트로, 남은시간은 `StunEndServerTime − GameState->GetServerWorldTimeSeconds()`로** 계산 — `GetActiveEffectsTimeRemaining()`(GE 조회)을 **안 쓴다**. 예전엔 GE 조회로 남은시간을 얻었는데, Mixed 모드에서 GE는 소유자에게만 복제돼 적을 보는 프록시 월드에선 조회가 빈 배열→스턴바 안 뜨는 버그가 있었음(아래 알려진 버그 참고). 태그와 시간값 도착 순서가 무보장이라 양쪽이 같은 `RefreshStun`을 호출해 마지막엔 정확한 값으로 수렴시킨다(먼저 온 쪽 폴백 1초/스킵, 나중 온 쪽 보정). `BroadcastInitialValues()`도 `RefreshStun()` 한 번 호출로 초기 상태 커버 |

**에디터 작업 현황 (WBP):**
- WBP_SegmentedBar: 루트 Overlay + FillBar(ProgressBar) + ValueText + RegenText 배치 필요
- WBP_P1Overlay: HealthBar + ManaBar User Widget 배치 + FillColor/SegmentSize 설정 필요
- WBP_SkillIcon: SizeBox(고정 크기) → Overlay(SkillIconImage → CooldownOverlay → CooldownText 순서, z-order=배치 순서) 구조로 배치. `SkillIconImage`의 Brush에 마름모 마스크 머티리얼(`IconTexture` Texture Parameter, UV 맨해튼거리 `abs(u-0.5)+abs(v-0.5)` 기반 마스크를 Opacity로 출력) 설정 필요 — 없으면 사각형 아이콘으로 폴백. `CooldownOverlay`(M_CooldownMaterial, `CooldownPercent` Scalar Parameter) + `CooldownText`(Overlay 슬롯 Center/Center 정렬)도 배치 필요
- BP_P1HUD: OverlayWidgetClass=WBP_P1Overlay, OverlayWidgetControllerClass=P1OverlayWidgetController 설정 필요
- ArenaGameMode HUDClass=BP_P1HUD 설정 필요
- **WBP_P1Overlay 좌하단 플레이어 정보(신규)**: `LevelText`/`KDAText`/`GoldText`(TextBlock)+`ExperienceBar`(UP1SegmentedBarWidget 인스턴스) 4개 위젯을 정확히 이 이름으로 배치 필요 — 전부 BindWidgetOptional이라 없어도 크래시는 안 나고 해당 UI만 안 뜸. 시각적으로 묶고 싶으면 그냥 Overlay/VerticalBox로 감싸면 됨(C++은 이름만 찾으므로 컨테이너 중첩 깊이 무관). `ExperienceBar`는 `bShowLabels`로 숫자 표시 여부 선택 가능

### Camera (`Source/P1/Camera/`)

| 클래스 | 상태 | 비고 |
|---|---|---|
| `AP1PlayerCameraManager` | 완료 | FOV(80°) / Pitch 범위(-40°~+15°) 상수 정의 |
| `UP1CameraComponent` | 완료 | `UCameraComponent` 확장, `CameraModeStack` 관리, `DetermineCameraModeDelegate` |
| `UP1CameraMode` | 완료 | 카메라 모드 추상 베이스 (ViewOffset, FOV, 블렌딩 파라미터). **카메라 관통 방지(Lyra 스타일)**: `PreventCameraPenetration(Pivot, DesiredLocation, DeltaTime)`이 Pivot~DesiredLocation 사이를 구체 스윕해 지형/벽에 카메라가 박히는 걸 감지, `CurrentPenetrationPullback`을 목표값으로 비대칭 속도(`FMath::FInterpConstantTo`, 파고들 땐 빠르게 당기고 빠져나올 땐 느리게 복귀)로 보간해 `UpdateView()`가 최종 카메라 위치에 반영 |
| `UP1CameraModeStack` | 완료 | 모드 스택 관리, 블렌딩 평가 (Linear / EaseIn / EaseOut / EaseInOut) |
| `UP1HeroComponent` | 완료 | PawnComponent — `DefaultCameraMode` → `DetermineCameraModeDelegate` 연결 |

### 기타

| 항목 | 상태 | 비고 |
|---|---|---|
| `LogP1` 커스텀 로그 카테고리 | 완료 | `P1.h` / `P1.cpp` |
| Enhanced Input IMC / IA 에셋 | 완료 | BP_P1PlayerController에서 AbilityInputActions(TMap) + DefaultMappingContext 설정 |

---

## 알려진 버그 / 설계 결정 기록

| 항목 | 내용 |
|---|---|
| 어빌리티 중복 부여 방지 | `InitAbilityActorInfo`와 `AddDefaultAbilities` 분리. `PossessedBy`에서만 `AddDefaultAbilities` 호출 |
| LocalPredicted 이중 데미지 | `OnHitEventReceived`에 `IsNetAuthority()` 체크 추가 — 서버에서만 오버랩/데미지 실행 |
| **서버 +1 콤보 버그 (근본 원인)** | 커스텀 입력 라우팅(`AbilityInputTagReleased`)이 release를 서버에 전파하지 않아, 서버 인스턴스의 `InputReleased()`가 절대 호출되지 않음 → 서버가 버튼을 계속 눌린 걸로 오인 → 콤보 +1. **해결:** base ability `bReplicateInputDirectly=true` + ASC에서 non-authority일 때 `ServerSetInputPressed/Released()` 호출 (표준 `AbilityLocalInputPressed` 패턴) |
| 콤보 입력 방식 | hold-to-combo 지원. `bInputHeld` 플래그(ActivateAbility에서 true 시작, InputPressed/InputReleased로 갱신)로 추적. `OnHitEventReceived`에서 `bInputHeld`면 콤보 예약. 윈도우 중 재입력(tap)도 지원 |
| RMB 조준 장판이 Substrate Deferred Decal에서 안 보임 (**과거 기록, 현재는 반대로 해결됨**) | 당시엔 Coverage 연결/Fade/Show Flag 등 여러 원인 시도 후에도 미해결 → `UDecalComponent` 자체를 포기하고 `UStaticMeshComponent`(평면) + 일반 Translucent Surface 머티리얼로 교체해 임시 해결했었다. **이후 재시도에서 Domain=Deferred Decal + Blend Mode=TranslucentGreyTransmittance 조합으로 정상 렌더링을 확인**(원인은 최종 미상 — 당시 Blend Mode 설정이 잘못됐었을 가능성) → 전 지면 조준 어빌리티를 Deferred Decal로 통일하고 StaticMesh 버전 클래스는 삭제 |
| RMB 도약이 포물선이 아니라 제자리/직진 | 1차 원인: 애니메이션에 수직 루트모션이 없어 MotionWarping이 아크를 만들 게 없었음 → `ApplyRootMotionJumpForce`(코드로 아크 생성)로 교체. 2차 원인: 몽타주의 루트모션이 켜져 있어 캐릭터를 제자리에 고정, JumpForce를 무효화 → 소스 AnimSequence들의 Root Motion을 꺼서 해결 |
| RMB 착지 판정이 공중에서 발동(캐릭터가 목표에 도달하기 전) | `ApplyRootMotionJumpForce`의 Duration을 몽타주 전체 길이로 줬더니, Land 노티파이가 타임라인 중간(약 60%)에 있어 노티파이 발동 시점에 캐릭터가 아직 도약 초반이었음. Duration을 "Land 노티파이의 실제 트리거 시점"(`FAnimNotifyEvent::GetTriggerTime()`)으로 바꿔 해결 — 오차가 649→94(캡슐 반높이 수준)로 감소 |
| 도약 중 LMB 입력 시 캐릭터가 목표를 지나쳐 멀리 날아감 | LMB 몽타주가 같은/겹치는 슬롯에서 RMB 몽타주를 인터럽트 → `OnLeapMontageInterrupted`가 `EndAbility` 호출 → `ApplyRootMotionJumpForce`가 아크 도중 강제 종료 → `MaintainLastRootMotionVelocity`로 잔여 속도가 일반 물리로 넘어가 무통제 비행. `UP1GameplayAbility_AssaultTheGates`가 `ActivationOwnedTags`로 `State.Attacking`을 소유해 도약 내내 LMB 발동 자체를 차단해 해결 |
| RMB 몽타주(Jump_Melee 스윙)가 재생되는데 화면에 안 보임 | GAS/AnimInstance 레벨에서는 정상 재생(로그로 세그먼트 진입 확인됨)됐지만, 몽타주가 AnimBP에서 실제로 참조하지 않는 `DefaultSlot`을 쓰고 있어 AnimGraph에 반영 안 됨. LMB 기본공격이 쓰는 `UpperBody` 슬롯은 하체를 Locomotion에 맡기므로 전신 도약 스킬에는 부적합 — 대신 Locomotion+UpperBody 블렌드 이후의 **FullBody 전용 Slot**을 AnimBP에 추가해 그 슬롯으로 교체해 해결 |
| `MeleeAttack_SacredOath`(어빌리티 변종)가 발동 자체를 안 함 | 로그의 `DynamicTags=(GameplayTags=)`(빈 값)와 `Default__P1GameplayAbility_MeleeAttack_SacredOath`(BP `_C` 접미사 없음)로 원인 특정 — **C++ 클래스를 Blueprint화하지 않고 `DefaultAbilities`에 직접 넣으면**, `InputTag`가 비어있어 `AddDefaultAbilities()`가 `DynamicSpecSourceTags`를 못 채우고, 입력 라우팅이 이 스펙을 영원히 못 찾는다. **어빌리티 변종은 반드시 Blueprint(부모=C++ 서브클래스)로 만들고 `InputTag`를 평소 버전과 동일하게 명시적으로 설정**해야 한다 (BP 상속이 아니라 별도 BP라 InputTag가 자동으로 안 넘어옴) |
| 코스메틱 효과 되돌리기를 "버프 태그 존재 여부"로 판단하면 경쟁 상태 발생 | Sacred Oath 검 발광을 "태그가 아직 있으면 자연만료로 원복" 방식으로 짰더니, GE 자체의 Duration 만료가 원복 타이머와 거의 동시에 태그를 스스로 지워버려 "조기소모로 이미 원복됨"과 "자연만료됐지만 아무도 원복 안 함"을 구분 못 함 → 무한 지속 버그. **1차 해결(임시)**: 태그 체크 없이 무조건 원복. **최종 해결**: 타이머 자체를 없애고 `RegisterGameplayTagEvent`로 태그 카운트 변경 이벤트를 구독 — 아래 항목 참고 |
| "강화된 기본공격" 어빌리티 변종이 같은 InputTag에서 평소 버전과 경쟁하는 구조 자체가 근본 원인이었던 다중 버그 | `MeleeAttack_SacredOath`를 평소 `MeleeAttack`과 같은 `InputTag.Ability.BasicAttack`에 `ActivationRequiredTags`/`ActivationBlockedTags`로 상호 배타 게이팅해 등록했더니: (1) Blueprint화 누락으로 `DynamicTags` 비어 발동 자체 불가(→ BP 생성으로 해결), (2) 로그 정밀 분석 결과 BP 수정 후에도 여전히 `TryActivateAbility result: 0`이 계속 나옴 → 원인은 **클라이언트 예측 GE가 서버 authoritative 버전과 조율되는 타이밍 문제**: 클라이언트 자체 시계로 버프 적용 후 겨우 ~1.2초 만에(5초 Duration인데도) `Buff.SacredOath.Active`가 로컬에서 사라진 것처럼 보임 — `SacredOath`(E)의 `CastMontage`가 짧게 끝나 클라 인스턴스가 `EndAbility`를 호출하는 시점이 서버의 authoritative 확인보다 빠르면 예측된 GE가 조기에 씻겨나갈 수 있음. 부가적으로 (3) 같은 `AbilityInputTagPressed` 루프 안에서 먼저 시도돼 성공한 어빌리티가 `State.Attacking`(전 어빌리티 공통 소유 태그)을 즉시 걸어버려 같은 루프의 나머지 후보를 오염시킬 수 있는 구조적 위험도 발견. **최종 해결**: 어빌리티 스왑(경쟁) 구조 자체를 폐기 — Sacred Oath 시너지가 있는 영웅은 아예 `MeleeAttack_SacredOath`를 자신의 유일한 BasicAttack으로 등록하고, 버프 판정은 액티베이션 시점이 아니라 **히트 적용 시점**에 어빌리티 내부에서 직접 체크. 이러면 위 세 문제가 전부 구조적으로 발생 불가능해진다(경쟁할 두 번째 어빌리티가 없으므로) |
| Stoicism 디플렉트가 매번 발동(쿨다운이 전혀 안 걸림) | 로그에서 `Default__P1GameplayAbility_StoicismDeflect`(BP `_C` 접미사 없음)로 원인 특정 — `MeleeAttack_SacredOath` 때와 완전히 같은 실수(위 항목 참고): raw C++ 클래스가 `DefaultAbilities`에 직접 들어가 에디터에서 설정한 `CooldownGameplayEffectClass`가 반영 안 됨. `CommitAbilityCooldown()`은 쿨다운 GE가 없으면 "적용할 게 없다"는 이유로 그냥 `true`(성공)를 반환하기 때문에, 로그만 봐서는 "커밋에 성공했는데 왜 쿨다운이 안 걸리지"처럼 보여서 원인 파악이 헷갈리기 쉬움 — **`OnCooldownTagChanged` 같은 태그 이벤트 리스너 로그가 한 번도 안 찍힌다면 태그 자체가 안 붙고 있다는 뜻**이므로 이럴 때 BP 등록 여부부터 의심할 것 |
| 상시 패시브(`GiveAbility`만으로 부여)가 영원히 실행되지 않음 | `UP1GameplayAbility_StoicismVitality`가 반복 타이머를 `ActivateAbility` 안에서 시작하는데, `AddDefaultAbilities()`가 `ASC->GiveAbility()`로만 부여해서 `ActivateAbility` 자체가 한 번도 안 불림 — 입력 바인딩도 없고(`InputTag` 없음) `AbilityTriggers`도 없는 어빌리티는 `GiveAbility`만으로는 절대 활성화되지 않는다(부여=활성화가 아님). **해결**: 베이스 `UP1GameplayAbility`에 `bActivateOnGranted` 플래그를 추가하고, `AddDefaultAbilities()`가 이 플래그를 보고 `GiveAbility` 대신 `GiveAbilityAndActivateOnce`로 부여하도록 분기 — 이 어빌리티에서 처음 발견됐지만 앞으로 "입력/트리거 없이 상시 작동해야 하는" 모든 패시브에 재사용 가능한 범용 플래그 |
| `UAttributeSet::GetOwningActor()`가 Pawn이 아니라 PlayerState를 반환 | `HandleKillRewards()`에서 처음엔 `Cast<AP1HeroCharacter>(GetOwningActor())`로 작성했으나 이 ASC는 PlayerState에 있으므로(`GetOwningActor()`=ASC의 `OwnerActor`=`AP1PlayerState`) 항상 null이 됨 — `Cast<AP1PlayerState>(GetOwningActor())->GetPawn()`으로 먼저 PlayerState를 얻은 뒤 Pawn을 가져와야 한다. 빌드 전에 자체 발견/수정 |
| **CurveTable JSON 임포트 시 "Key 'Keys' on row 'X' is not a float and cannot be parsed" 에러** | `{"Name": "Row", "Keys": [{"Time":X,"Value":Y}, ...]}` 형태(=`UCurveFloat` 에셋을 익스포트했을 때 나오는 내부 직렬화 포맷)로 `Data/CT_*.json`을 전부 작성했었는데, 이건 **CurveTable 임포터가 기대하는 포맷이 아니다** — CurveTable은 CSV와 동일한 구조로 "Row 하나 = 커브 하나, 시간값이 컬럼 헤더"여야 한다: `[{ "Name": "RowName", "1": 42, "2": 98, ... }]` (컬럼 키가 시간, 값이 float). `Data/` 하위 모든 `CT_*.json`을 이 포맷으로 재작성해 해결 |
| GE Custom Calculation Class의 에셋 참조 프로퍼티가 항상 비어있음 | `UGameplayModMagnitudeCalculation` 서브클래스(`UP1MMC_GoldKillBounty`)의 `KillStreakBonusTable`(`UCurveTable*`) 같은 `EditDefaultsOnly` 프로퍼티는, GE가 그 네이티브 C++ 클래스를 Custom Calculation Class로 직접 참조하면 **CDO 기본값(null)**이 그대로 쓰인다 — 네이티브 클래스의 CDO는 콘텐츠 브라우저에서 값을 편집할 방법이 없기 때문. 다른 "BP화 필요" 사례(`MeleeAttack_SacredOath`, `StoicismDeflect`)와 같은 근본 원인의 변형: **이 MMC를 상속하는 블루프린트(`MMC_GoldKillBounty`)를 만들어 그 Class Defaults에서 값을 지정하고, GE의 Calculation Class로는 이 BP를 선택**해야 한다 |
| **패키징 배치파일이 한글/em-dash 포함 시 깨짐** | `.bat` 파일에 한글 주석이나 em-dash를 UTF-8로 저장하면 `cmd.exe`가 시스템 ANSI 코드페이지(CP949)로 파싱해 이후 줄(캐럿 라인 컨티뉴에이션 포함)이 통째로 깨진다(`'1.uproject' is not recognized...` 식의 파편적 오류). **해결**: 배치파일 전체를 순수 ASCII/영문으로만 작성 |
| **패키지드 빌드가 짧은 맵 이름을 못 찾음 / `-allmaps`가 실제로 맵을 안 쿡함** | 에디터에서는 `Arena`처럼 짧은 이름으로 맵 로드가 되지만, 패키지드(쿡된) 빌드는 전체 패키지 경로(`/Game/Maps/Arena`)가 필요하다. 또한 `RunUAT BuildCookRun -allmaps`가 이 프로젝트에서는 `/Game/Maps/*.umap`을 실제로 쿡하지 않는 현상이 있었음(쿡 로그에 맵 참조 0회, 쿡 시간도 비정상적으로 짧음) — **해결**: `-allmaps` 대신 `-Map=/Game/Maps/PreGame+/Game/Maps/Arena`(클라)/`-Map=/Game/Maps/Arena`(서버)로 명시 |
| **패키지드 서버가 Arena 대신 빈 `/Engine/Maps/Entry`+`GameModeBase`로 폴백** | `DefaultEngine.ini`의 `GlobalDefaultGameMode`가 실제로 존재하지 않는 경로(`/Game/Blueprints/GameModes/BP_P1ArenaGameMode`, 자산이 옮겨지기 전 경로)를 가리키고 있었음 — 쿡 로그에 `Unable to find package for cooking ... BP_P1ArenaGameMode` 경고(치명적 에러 아니라 조용히 스킵됨)가 뜨고, 런타임에 `Game class is 'GameModeBase'`로 폴백. 실제 자산 위치(`/Game/Blueprints/Game/BP_P1ArenaGameMode`)로 ini 경로를 고쳐 해결 |
| **데디케이티드 서버+원격 클라 접속은 성공했는데 HUD가 전혀 안 뜨고 입력도 안 됨 (2단계 버그)** | (1) HUD 미표시: `AP1PlayerController::CreateHUDForASC`(구, 현재는 제거됨)를 거치는 간접 경로가 `IsLocalController()`/`GetPlayerState<AP1PlayerState>()`(Pawn이 아니라 **Controller 자신의** PlayerState 포인터, 별개 리플리케이트 프로퍼티) 등 여러 조기 리턴 지점에서 로그 없이 조용히 실패할 수 있었음 — 실제 원인 확정 전에 방어적으로 `AP1HeroCharacter::OnRep_Controller()` 재시도 훅을 추가했으나, 이후 D1 프로젝트(동일 데디서버 환경에서 정상 동작)와의 비교로 근거가 약함이 드러나 제거함. **최종 해결**: 간접 경로(`CreateHUDForASC`) 자체를 없애고 `AP1HeroCharacter`가 이미 검증된 `PS`/`ASC`를 그대로 들고 `PC->GetHUD<AP1HUD>()->InitOverlay()`를 직접 호출하도록 인라인화 — 불필요한 재조회 지점을 원천 제거. (2) 입력 미작동: PreGame 로비(`AP1LobbyPlayerController`)가 `FInputModeUIOnly`로 바꿔둔 `GameViewportClient`의 마우스 캡처 모드가 `ClientTravel` 이후에도 안 풀린 채 남아있었음 — `AP1PlayerController::BeginPlay()`에서 로컬 컨트롤러면 `SetInputMode(FInputModeGameOnly())`+`bShowMouseCursor=false`로 명시적으로 되돌려 해결 |
| **머리 위 스턴바가 "화면에만" 안 뜸 (긴 삽질, 근본 원인은 멀티월드 리플리케이션)** | 데이터 파이프라인(태그 이벤트→컨트롤러→위젯)은 로그상 완벽한데 화면엔 안 뜨는 증상. 긴 오진 끝에 두 개의 독립적 원인이었음. **원인 A — WidgetComponent 렌더 특성**: 머리 위 위젯은 `UP1FloatingWidgetComponent`(WidgetComponent)가 렌더타겟 텍스처로 그리는데, 자식 위젯의 **콘텐츠 변경**(SetText/SetPercent, HP/MP 바가 쓰는 방식)엔 텍스처를 다시 그리지만 **Visibility/RenderOpacity 변경**엔 다시 안 그린다 — 그래서 `SetVisibility`/`SetRenderOpacity`로 스턴바를 껐다 켜면 값은 바뀌는데(로그·Reflector엔 반영) 화면 텍스처는 옛 프레임 유지. **해결 A**: 표시/숨김을 전부 콘텐츠 변경으로만 처리(라벨 텍스트 ""↔"Stun", 바 Percent 0↔n). **원인 B(진짜 근본, 결정타) — Mixed 리플리케이션 갭**: PIE는 서버+클라2 = 월드 3개라 같은 캐릭터의 머리 위 위젯 인스턴스도 월드마다 하나씩(총 3개) 존재. 컨트롤러가 남은시간을 `GetActiveEffectsTimeRemaining()`(스턴 GE 조회)으로 얻었는데, **Mixed 모드에선 GE가 소유자(+서버)에게만 복제되고 `State.Stunned` 태그만 전원 복제** → GE 없는 프록시 월드(=적을 보는 클라 화면, 자기 위젯은 `SetVisibility(!IsLocallyControlled())`로 숨겨서 뷰어는 항상 남의 위젯을 봄)에선 GE 조회가 빈 배열 → 옛 코드의 "빈 배열이면 skip" 가드가 브로드캐스트를 죽여서 **정작 봐야 할 그 화면에서만** 스턴이 안 떴다. 로그가 완벽해 보였던 건 그 로그가 GE 있는 다른 월드 인스턴스에서 찍힌 거였음(위젯 `this` 포인터 주소가 3개로 갈리는 걸 찍어서 확정). **해결 B**: (1) 표시/숨김은 **태그 카운트**로만(전원 복제되므로 프록시도 정확), (2) 카운트다운 정확도는 `AP1PlayerState::StunEndServerTime`(복제 float)+`GameState->GetServerWorldTimeSeconds()`(동기화 서버시계)로 계산 — GE 조회를 아예 버림, (3) 태그·시간값 도착 순서 무보장이라 둘 다 `RefreshStun()`을 호출해 수렴. **교훈: 태그 기반 상태 UI는 GE(ActiveGameplayEffects) 조회에 의존하면 Mixed 모드 프록시에서 깨진다 — 태그는 전원, GE는 소유자만. 크로스머신 시간 비교는 로컬 `GetTimeSeconds()`가 아니라 `GetServerWorldTimeSeconds()`를 쓸 것.** 또 "로그는 완벽한데 화면 안 뜸"이 반복되면 멀티월드에서 여러 인스턴스 중 엉뚱한 걸 업데이트하는지 `this` 포인터를 의심할 것 |
| **정글 몬스터 스폰 직후 클라이언트에서 `check(Result)` 크래시 (`UAttributeSet::GetOwningAbilitySystemComponentChecked()`)** | 콜스택: `UP1AttributeSet::OnRep_Health` → `GAMEPLAYATTRIBUTE_REPNOTIFY` 매크로가 내부적으로 `GetOwningAbilitySystemComponentChecked()`를 호출 → `GetOwningAbilitySystemComponent()`가 null 반환 → check 실패. 원인: `IAbilitySystemInterface::GetAbilitySystemComponent()`가 반환하는 `CachedAbilitySystemComponent`를 `AP1JungleMonsterCharacter::BeginPlay()`에서야 채우고 있었는데, **클라이언트에서는 액터의 초기 리플리케이트 프로퍼티 수신(RepNotify 포함)이 `BeginPlay()`보다 먼저 일어날 수 있다** — 그 틈에 Health 리플리케이션이 도착하면 `CachedAbilitySystemComponent`가 아직 null. 히어로는 ASC/AttributeSet이 둘 다 `AP1PlayerState` 생성자에서 함께 만들어지고 PlayerState 자신이 `IAbilitySystemInterface`를 구현해서 이 경쟁이 원천적으로 없었는데(오브젝트 존재=인터페이스 해석 가능), 정글몹은 Pawn이 ASC를 직접 들고 있어서 별도 캐싱 타이밍에 의존하다 문제가 됨. **해결**: `CachedAbilitySystemComponent = AbilitySystemComponent;` 대입을 `BeginPlay()`가 아니라 **생성자**로 이동 — 생성자는 리플리케이션이 시작되기 전에 서버/클라 양쪽에서 항상 먼저 실행되므로 경쟁 자체가 사라진다. **교훈: Pawn이 자기 ASC를 직접 호스팅하는 구조(PlayerState 경유가 아닌)에서는, `IAbilitySystemInterface::GetAbilitySystemComponent()`가 참조하는 캐시 포인터를 `BeginPlay()`가 아니라 생성자에서 채워야 클라이언트의 조기 리플리케이션(특히 RepNotify)이 안전하다.** |
| **진실의 일격 손 발광(Niagara)이 버프가 끝나도 영구히 안 사라짐** | `EmpowerEffect`를 1회성 `MulticastPlayNiagaraEffect()`(`bAutoDestroy=true`)로 재생했는데, 지속 버프용 손 발광 에셋은 보통 Looping으로 만들어져 있어 시스템이 "완료" 상태에 절대 도달하지 못함 — `bAutoDestroy`는 완료 시점에 파괴하는 트리거일 뿐이므로, 완료 자체가 안 오면 트리거도 안 걸려서 버프가 소모되든 4초 자연만료되든 이펙트만 계속 남음. **해결**: 지속형 `MulticastSetAttachedNiagaraEffect`/`MulticastStopAttachedNiagaraEffect`(신규, 위 표 참고)로 교체하고, 시작/중지를 `Buff.TrueStrike.Empowered` 태그의 `RegisterGameplayTagEvent(NewOrRemoved)`에 맡김 — Sacred Oath 검 발광 때와 같은 이유(바로 위 "코스메틱 효과 되돌리기를 태그 존재 여부로 판단" 항목)로, 원인(소모/자연만료)과 무관하게 태그 소멸 시점 한 곳에서만 반응하면 경쟁 없이 정확히 처리된다. **교훈: Looping으로 만들어질 가능성이 있는 지속 이펙트(버프 연출류)에는 절대 1회성 Play 함수를 쓰지 말 것 — 지속 여부가 불확실하면 지속형 Set/Stop 쌍 + 태그 이벤트 구독을 기본으로 삼을 것.** |

---

## 다음 작업 예정

**스킬 어빌리티 (순차 진행 — 데미지는 ExecCalc_Damage 공유, 도약류는 ApplyRootMotionJumpForce 패턴 재사용):**
- [x] 공유 기반: `Damage` 메타 어트리뷰트, `UP1ExecCalc_Damage`(계수 채널 방식), `Character.Type` 태그
- [x] **RMB (Assault The Gates)**: 완료, 풀 테스트 통과 (조준 장판/포물선 도약/착지 판정/AnimBP FullBody 슬롯까지 전부 해결)
- [x] **RMB 이펙트**: 착지 파티클 + 검 화염(스킬 사용 중 무기 슬롯 머티리얼 교체, `UP1AnimNotifyState_MaterialOverride`) 전부 완료
- [x] **E (Sacred Oath)**: **완료(2026-08-17 에디터 작업까지 확인)** — 버프(`GA_SacredOath`) + 강화된 MeleeAttack
- [x] **Q (Make Way)**: **완료(2026-08-17 에디터 작업까지 확인)** — 반복 타이머 기반 지속 회오리
- [x] **R (Stone Forged Soul)**: **완료(2026-08-17 에디터 작업까지 확인, 카메라 연출만 별도 작업으로 계속 보류)** — 2.5초 스테이시스(회복+무적+근접도 슬로우) → 착지 시 범위 물리피해
- [x] **패시브 (Stoicism)**: **완료(2026-08-17 에디터 작업까지 확인)** — 서로 성격이 달라 어빌리티 2개로 분리(디플렉트=반응형 쿨다운, 재생+방어력=상시 틱)
- [x] **전투 보상/레벨업 시스템**: C++ 완료, PIE 실전 검증(킬 시 Experience 획득 로그 확인). 아래 "전투 보상/레벨업 에디터 작업" 참고(일부 마무리 항목 남음)
- [x] 정글 몬스터 / AI 캐릭터: **완료(2026-08-17, BT/블랙보드/BP/캠프 배치까지 전부 확인)** — `AP1JungleMonsterCharacter`(Pawn 호스팅 ASC) + BT 기반 3상태(Idle/Combat/Reset) FSM(`AP1JungleMonsterAIController`+`UBTService_P1JungleLeashCheck`+`UBTTask_P1ReturnToCamp`/`UBTTask_P1MeleeAttack`) + `UP1GameplayAbility_JungleMonsterMeleeAttack`. 리시 판정은 몬스터 자신의 홈 거리 기준(카이팅에 안 흔들림 — 자세한 설계 배경은 위 AI 섹션 참고). **미니언 없는 게임으로 최종 설계 변경**됐던 배경은 여전히 유효(`AP1MinionCharacter`는 만들지 않음, 3라인+정글 에셋을 구하기 어려워 던전형 맵에 정글몬스터만 배치). 몬스터는 스폰 시점마다 매치 경과 시간에 따라 "레벨"이 갱신되고(`CT_MonsterLevelByMatchTime.json`, `AP1JungleCampAnchor::ComputeMonsterLevel()`), **처치 보상(골드/경험치)도 이 레벨로 스케일링된다(2026-08-16 구현 완료)** — `CT_MonsterRewardMultiplier.json`+`DT_MonsterGoldXP.json` 소비 로직은 `AP1JungleMonsterCharacter::GetKillReward()`/`UP1AttributeSet::HandleMonsterKillRewards()` 참고(위 Characters/AbilitySystem 섹션). 다만 몬스터 자신의 스탯(체력/공격력 등)은 레벨에 따라 스케일링되지 않고 여전히 고정값(`DefaultAttributesEffectClass`가 항상 같은 값 적용) — 몬스터 레벨이 오르면 "더 세지는" 건 아직 없고 "죽였을 때 보상만 늘어나는" 상태. `RiftHerald`/`ElementalDrake`/`BaronNashor`는 던전 맵 레이아웃에 실제로 들어갈지 미정이라 낮은 우선순위
- [x] **로비(PreGame) + 백엔드 매치메이킹**: C++/백엔드 코드 완료, 실제 데디케이티드 서버 패키징+원격 클라 접속까지 엔드투엔드 검증 완료(HUD/입력 버그 포함 전부 해결 — 위 "알려진 버그" 참고)
- [x] **스킬 포인트 투자 시스템**: C++ 완료·PIE 검증(1레벨 좌클릭/패시브만 활성, Ctrl+스킬키로 투자, R 6/11/15 레벨 게이트, 잠김/투자가능 아이콘 틴트까지 전부 로그로 확인). 아래 "스킬 포인트 투자 에디터 작업" 참고
- [x] **상점/인벤토리 시스템**: `AP1PlayerState`에 `ServerBuyItem`/`ServerSellItem`(원자적 골드 검증+차감/환불+인벤토리 반영 Server RPC)+`Inventory`(중복 보유 불가 플랫 배열 — `ServerBuyItem`이 `Inventory.Contains(ItemRowName)`이면 거부. 2026-08-06에 잠깐 중복 구매 허용으로 바꿨다가 "깡스탯은 중복 적용, 특수효과는 한 번만" 같은 걸 하려면 GE를 둘로 쪼개고 스택/제거 로직을 따로 관리해야 해서 복잡도가 커진다는 이유로 도로 금지로 되돌림 — 아이템 특수효과 시스템이 생기면 그때 재검토)+`ShopItemTable`(DataTable)+`MaxInventorySlots` C++ 완료. **패시브 골드 수입(2026-08-07)**: `PassiveGoldEffectClass`(Duration=Infinite+Period, Gold Modifier=Additive 고정값)를 `AP1PlayerState::StartPassiveGoldIncome()`이 매치당 **딱 한 번만** 적용(내부 `bPassiveGoldIncomeStarted` 플래그로 멱등 보장) — `AP1HeroCharacter::HandleAbilitySystemReady()`의 `HasAuthority()` 블록이 스폰마다(리스폰 포함) 호출은 하지만, `PassiveRegenEffectClass`와 달리 재적용은 안 됨. ASC가 PlayerState 소유라 폰이 죽어 사라져도 파괴되지 않으므로, 한 번 적용된 Periodic GE는 사망 중에도 계속 틱한다(리스폰마다 재적용했다면 사망~리스폰 구간마다 골드 수입이 끊겼을 것). **UI(LoL 아레나 참고 3열 레이아웃, 2026-08-06)**: `UP1ShopWidgetController`+`UP1ShopWidget`+`UP1ShopStatsWidget`+`UP1ShopItemListWidget`+`UP1ShopItemDetailWidget`+`UP1ShopInventoryWidget`+`UP1ShopItemSlotWidget`+`UP1ShopInventorySlotWidget` C++ 완료 — 아래 "상점 UI" 항목 및 "상점 에디터 작업 필요" 참고
- [x] **두 번째 영웅 Dekker**: RMB(Stasis Bomb)/Q(Photon Disruptor)/E(Containment Fence)/R(Ion Strike)/패시브(Rocket Boots) **전부 완료(2026-08-17 에디터 작업까지 확인)** — 자세한 내용은 위 `Abilities/Dekker/` 표 참고. 좌클릭 기본공격은 아직 그레이스톤 것을 임시로 공유 중, 전용 기본공격 미구현. 원형/사각형 지면 조준 데칼은 StaticMesh 버전을 전부 삭제하고 Deferred Decal로 통일(위 TargetActors 표 참고)
- [x] **전투 피드백 UI**: (1) 데미지 팝업 텍스트 — `AP1DamageNumberActor`+`UP1DamageNumberWidget`, 가해자에게만 로컬로 표시. **완료(2026-08-17)**. (2) `UP1FloatingStatusWidget` 스턴 지속시간 표시 — `State.Stunned` 남은 시간을 캐릭터 머리 위에 노출. C++ 완료, WBP 배치 여부는 미확인 — 아래 "전투 피드백 UI 에디터 작업" 참고
- [x] 정글 던전 맵 임포트 + 정글 몬스터 캠프 배치 + 플레이어팀 시작 위치 배치 — **완료(2026-08-17 확인)**, 카이팅에 안 흔들리는 리시 AI 포함 전체 사이클 PIE로 실전 검증됨(이번 세션 로그에서 캠프 무리 5마리 스폰/전투/리시/사망/리스폰까지 전부 확인)
- [x] **매치 흐름 (킬스코어 종료 → 결과창 → 로비 복귀)**: C++ 완료(2026-08-12) — `AP1GameState`에 `TeamKillScores`/`MatchState`/`WinningTeamId` 추가, `AP1ArenaGameMode`가 `KillScoreToWin` 임계치 판정+`EndMatch()` 오케스트레이션, `UP1AttributeSet::HandleKillRewards()`가 킬마다 팀 점수 훅, `UP1MatchResultWidget`(Scoreboard 팀 위젯 재사용)+`AP1PlayerController::ClientShowMatchResult()`가 결과창 표시 후 `ClientTravel`로 로비 복귀. 미니맵/캐릭터 선택 레벨은 이 다음 순서로 별도 작업 예정 — 아래 "매치 결과 에디터 작업" 참고
- [x] **아레나 맵 (콜로세움 에셋 임포트 및 레벨 구성)**: `Content/Maps/InGame.umap`(`/Game/Maps/InGame`)이 실제 콜로세움 아레나 맵 — Arena.umap이 아니라 이쪽이 정식 매치 레벨(2026-08-12 확인, 패키징 스크립트도 맞춰 수정함)
- [x] **미니맵**: C++ 완료(2026-08-12) — `UP1MinimapWidget`+`UP1MinimapIconWidget`, 정적 배경 이미지+아군 항상 표시+적은 아군 시야 반경 내에서만 표시(클라이언트 로컬 계산, 새 리플리케이션 없음)+정글 캠프 생사에 따라 아이콘 동적 표시. 아래 "미니맵 에디터 작업" 참고
- [x] **캐릭터 선택 → 매칭 → 선택한 히어로로 스폰**: C++/백엔드 코드 완료(2026-08-18) — 매칭 화면(`UP1MatchQueueWidget`)에 내장된 `UP1HeroPickerWidget`으로 히어로 선택(기본 Greystone, 대기 중엔 잠김), 선택값은 `UP1BackendSubsystem::SelectedHeroId`가 보관해 (1) `JoinQueue()` 매칭 신청 바디에 `heroId`로 실어 백엔드에 기록(순수 기록용, 매칭 로직은 안 씀), (2) `ClientTravel` URL `?HeroId=` 옵션으로 Arena 서버에 전달 — `AP1ArenaGameMode::InitNewPlayer`가 `HeroTable`(`DT_Heroes`, 화이트리스트 겸용)에서 조회해 `SelectedCharacterClass`를 채우고 기존 `GetDefaultPawnClassForController_Implementation`이 그대로 소비. 자세한 내용은 위 GameMode/Online/UI 섹션 참고. 아래 "캐릭터 선택 에디터 작업" 참고(전부 미완료 — DataTable/WBP 배치가 아직 하나도 안 됨)

**캐릭터 선택 에디터 작업 (전부 미완료):**
- [ ] `DT_Heroes` DataTable 생성(RowStruct=`FP1HeroDefinition`), 행 2개: `Greystone`→`BP_Greystone`, `Dekker`→`BP_Dekker`(`Content/Blueprints/Character/Hero/...`에 이미 존재)
- [ ] `BP_P1ArenaGameMode`의 `HeroTable`에 `DT_Heroes` 지정
- [ ] `WBP_HeroSelectSlot`(parent=`UP1HeroSelectSlotWidget`): `PortraitImage`/`NameText`/`SelectedHighlight`(선택 테두리 등)/`SelectButton`(칸 전체를 덮는 투명 버튼) 배치
- [ ] `WBP_HeroPicker`(parent=`UP1HeroPickerWidget`): `HeroSlotContainer`(예: HorizontalBox) 배치, Class Defaults에서 `HeroTable=DT_Heroes`, `HeroSlotWidgetClass=WBP_HeroSelectSlot` 지정
- [ ] `WBP_MatchQueue`(parent=`UP1MatchQueueWidget`)에 `WBP_HeroPicker` 인스턴스를 추가하고 이름을 `HeroPickerWidget`으로
- [ ] `Backend/` 서버 기동 후(사용자가 직접, Docker/MySQL 포함) 엔드투엔드 확인 — PIE 2클라이언트로 서로 다른 히어로 선택 후 매칭 → 각자 고른 히어로로 Arena 스폰되는지, 대기 중엔 픽커가 잠기는지

**정글 몬스터 에디터 작업 — 완료(2026-08-17):** BB/BT/`BP_JungleMonster`/`BP_JungleMonsterAIController`/`GA_JungleMonsterMeleeAttack`/`NavMeshBoundsVolume`/`CT_MonsterLevelByMatchTime`/`BP_JungleCampAnchor`/`DT_MonsterGoldXP`+`CT_MonsterRewardMultiplier` 전부 구성 완료. 레벨에 캠프 배치 완료, PIE로 어그로→추적→공격→리시→무적/회복→리스폰까지 전체 사이클 및 처치 시 골드/경험치 지급까지 이번 세션에서 실전 로그로 확인됨.

**스킬 포인트 투자 에디터 작업 — 완료** (1레벨 좌클릭/패시브만 활성, Ctrl+스킬키 투자, R 6/11/15 레벨 게이트, 잠김/투자가능 아이콘 틴트까지 PIE로 전부 확인됨)

**전투 보상/레벨업 에디터 작업 (일부 완료, 남은 항목만 표시):**
- [x] `GE_ExperienceReward_Flat`, `GE_GoldReward_Kill`(+`MMC_GoldKillBounty` BP), `GE_GoldReward_Flat` 생성 완료
- [x] `CT_ChampionKillXP`/`CT_ChampionAssistXP`(챔피언 킬/어시스트 경험치, Row=피해자 레벨 1~18) CurveTable 임포트 및 BP_Greystone의 `ChampionKillXPTable`/`ChampionAssistXPTable`에 지정 — PIE로 검증됨
- [ ] `CT_XPToNextLevel`(레벨업 요구 경험치)을 BP_P1PlayerState의 `XPToNextLevelTable`에 지정 — 안 해도 XP는 정상 누적되고 레벨업만 안 일어남(안전 폴백)
- [ ] `GE_ManaRestore_Instant`(Duration=Instant, Mana Modifier=Additive, Magnitude=SetByCaller `Data.Mana.Flat`) 생성 — `HealEffectClass`(`GE_Heal`, 기존 존재)는 이미 있지만 마나 버전은 아직 없음
- [ ] BP_Greystone에 `HealEffectClass`/`ManaRestoreEffectClass`/`GoldRewardKillEffectClass`/`GoldRewardFlatEffectClass`/`ExperienceRewardEffectClass` 전부 지정 확인
- [ ] **WBP_P1Overlay 좌하단 플레이어 정보 위젯 배치** — 위 UI 섹션 "에디터 작업 현황 (WBP)" 참고 (`LevelText`/`KDAText`/`GoldText`/`ExperienceBar`)

**로비(PreGame)/백엔드 에디터 작업 (C++/백엔드 코드는 완료·검증됨, 에디터 자산이 전부 대기 중):**
- [x] 백엔드 curl 엔드투엔드 검증 완료(Docker/앱 실행은 사용자가 직접) — signup×2(201) → login×2(200, JWT 발급) → queue×2(1번째 WAITING, 2번째 MATCHED+`serverAddress`) → 이미 대기 중이던 1번째 유저도 `/status` 재조회 시 MATCHED로 확인됨. 에러 경로(중복 아이디 409, 오답 401, 토큰 없음 401)도 확인
- [x] **레벨/위젯/Default Map 전부 완료(체크리스트가 여기까지 stale하게 남아있었음, 2026-08-12에 정정)** — 실제 맵은 `Content/Maps/PreGame.umap`(경로 `/Game/Maps/PreGame`, "L_" 접두사 없음 — `Scripts/Package_Client.bat`도 이 경로를 그대로 씀)로 이미 존재. `DefaultEngine.ini`에 `GameDefaultMap=/Game/Maps/PreGame.PreGame`+`EditorStartupMap=/Game/Maps/PreGame.PreGame`도 이미 설정됨. `BP_P1LobbyGameMode`/`BP_P1LobbyPlayerController`/`WBP_Login`/`WBP_Signup`/`WBP_MatchQueue`/`WBP_PreGameHUD` 전부 만들어져 있고 로그인→매칭→Arena 접속까지 이 세션에서 실제로 여러 번 PIE 검증됨(위 "매치 결과 에디터 작업" 섹션에서 매치 종료→로비 복귀 테스트도 이 레벨로 확인)
- [ ] `DefaultGame.ini`에 `[/Script/P1.P1BackendSubsystem]` 섹션 추가해 `BackendBaseUrl` 확인/조정(기본값은 C++에 `http://127.0.0.1:8080`으로 하드코딩돼 있어 로컬 테스트는 설정 없이도 동작)
- [ ] PIE 2클라이언트로 로그인→매칭→`ClientTravel`까지 엔드투엔드 검증 (Arena 맵을 `127.0.0.1:7777`에서 데디케이티드/리슨 서버로 미리 띄워둬야 함 — 백엔드 `match.server-address`와 일치시킬 것)

**그레이스톤/덱커 스킬(E Sacred Oath, Q Make Way, R Stone Forged Soul, 패시브 Stoicism, Dekker RMB/Q/E/R/패시브) 에디터 작업 — 완료(2026-08-17):** GE/BP 생성, `DefaultAbilities` 등록, 입력 매핑, 파티클/머티리얼 지정까지 전부 끝남(Stone Forged Soul의 카메라 연출만 여전히 별도 작업으로 보류). **남은 건 그레이스톤 스킬 아이콘 텍스처를 가져와서 각 `GA_*` BP의 `AbilityIcon`에 등록하는 것뿐** — 덱커는 아이콘까지 이미 등록됨.
- [ ] 그레이스톤 RMB/Q/E/R/패시브/좌클릭 스킬 아이콘 텍스처 준비 후 각 어빌리티 BP의 `AbilityIcon`에 지정 → PIE에서 `WBP_SkillIcon`에 정상 표시되는지 확인

**전투 피드백 UI 에디터 작업:**
- [x] 데미지 팝업(`WBP_DamageNumber`+`BP_P1DamageNumberActor`+`DamageNumberActorClass` 지정) — **완료(2026-08-17)**
- [ ] `WBP_FloatingStatus`(기존 HP/MP 바 위젯): **`StunLabelText`(TextBlock)+`StunProgressBar`(ProgressBar)** 배치 — 이름 정확히 일치해야 바인딩됨(둘 다 BindWidgetOptional). **주의사항**(전부 실제로 겪은 함정): (1) 둘 다 HP/MP 바와 같은 Vertical Box 안 등 렌더 정상인 자리에 두고 RenderOpacity=1, Visibility=Visible/SelfHitTestInvisible로 둘 것 — Collapsed/RenderOpacity 0이 조상에 하나라도 있으면 안 뜸. (2) `StunProgressBar`의 Fill Image에 실제 보이는 색을 넣고 Background는 투명(알파 0)으로 — 안 스턴 시 Percent=0으로 비우는 방식이라. (3) **전체 내용(Stun+Level+HP/MP)이 `FloatingStatusComponent`의 Draw Size(기본 150×40) 안에 다 들어가야** 함 — 넘치면 Stun 부분이 렌더타겟 밖으로 잘려 안 보인다. 폰트/바 높이를 작게 하거나 캐릭터 BP의 Draw Size Y를 키울 것. C++이 이름으로 자동 바인딩하니 별도 코드 작업은 없음 — **완료 여부 미확인, 다음에 PIE로 스턴 걸어서 확인할 것**

**점수판 에디터 작업 — 완료(2026-08-17):** `WBP_ScoreboardPlayerRow`/`WBP_ScoreboardTeam`/`WBP_Scoreboard` 전부 배치 완료, PIE 2클라이언트로 Tab 홀드 시 양쪽 팀 닉네임/캐릭터/KDA 표시, 사망~리스폰 중 칸 비었다 채워지는 것까지 확인됨.

**상점 에디터 작업 필요:**
- `IA_Shop`(또는 원하는 이름) 입력 액션 생성 → IMC에 원하는 키(예: B) 매핑 → `BP_P1PlayerController`의 `ShopAction`에 지정(토글 입력이라 `Trigger`는 기본 Down 그대로 — `Started`에서만 반응해 열림/닫힘을 토글함, 스코어보드처럼 Started/Completed 둘 다 쓰지 않음)
- 9개 WBP를 **반드시 이 순서로**(자식이 부모보다 먼저 있어야 다음 단계에서 클래스로 선택 가능) 생성:
  1. `WBP_ShopItemSlot`(parent=`UP1ShopItemSlotWidget`, 카탈로그 목록 칸): `NameText`(TextBlock, 필수)+`IconImage`(Image, 선택)+`SelectButton`(Button, 필수 — 칸 전체를 덮는 투명 버튼으로 만들면 자연스러움)+`OwnedBadge`(임의 위젯 — 체크 아이콘 등, 선택, 보유 중일 때만 보임) 배치. **더블클릭하면 구매됨** — `SelectButton`의 `OnClicked`가 0.35초 안에 두 번 들어오는지로 판정(`NativeOnMouseButtonDoubleClick`은 `SelectButton`이 입력을 먼저 가로채서 못 씀, 위 상점 UI 항목 참고). 루트 위젯 Visibility 요건 없음(클릭은 전부 `SelectButton`을 통해서만 들어옴)
  2. `WBP_ShopInventorySlot`(parent=`UP1ShopInventorySlotWidget`, 인벤토리 칸): `IconImage`(Image, 선택) 하나만 있으면 됨 — 이름/가격 텍스트 없음. **루트 위젯의 Visibility를 반드시 `Visible`로 둘 것**(`SelfHitTestInvisible`이면 우클릭 이벤트 자체가 안 들어옴 — `NativeOnMouseButtonUp` 오버라이드가 마우스 이벤트를 받으려면 히트테스트가 필요)
  3. `WBP_ShopStatChip`(parent=`UP1ShopStatChipWidget`, 상세 패널의 스탯 칩 하나): `ValueText`(TextBlock, 필수) 하나만 있으면 됨 — "+45 물리 공격력" 식으로 C++이 포맷팅해서 채워줌. **한 줄에 2개씩 정확히 배치하려면 루트를 `SizeBox`로 감싸고 고정 너비를 줄 것**(예: 상세 패널 폭의 절반 안팎) — 안 그러면 텍스트 길이에 따라 WrapBox 줄바꿈이 들쭉날쭉해짐
  4. `WBP_ShopUniqueAbility`(parent=`UP1ShopUniqueAbilityWidget`, 상세 패널의 고유 능력 한 블록): `AbilityNameText`+`AbilityDescriptionText`(TextBlock, 둘 다 필수) 배치 — 이름은 좀 더 크게/굵게, 설명은 `Auto Wrap Text` 켜서 여러 줄 허용하는 걸 추천
  5. `WBP_ShopItemList`(parent=`UP1ShopItemListWidget`, 상점 중앙 카탈로그): `CatalogContainer`(임의 Panel 계열, VerticalBox/WrapBox 등, 필수) 배치. Class Defaults에서 `ItemSlotWidgetClass`=`WBP_ShopItemSlot` 지정
  6. `WBP_ShopItemDetail`(parent=`UP1ShopItemDetailWidget`, 상점 우측 상세): `NameText`/`PriceText`(TextBlock, 선택)+`IconImage`(Image, 선택)+`StatsContainer`(WrapBox 등 Panel, 선택 — 스탯 칩들이 여기 채워짐)+`UniqueAbilitiesContainer`(VerticalBox 등 Panel, 선택 — 고유 능력 블록들이 여기 채워짐)+`DetailPanelRoot`(선택 — 패널 전체를 감싸는 최상위 위젯, 아무 아이템도 선택 안 됐을 때 이걸 Collapsed로 감춤) 배치. Class Defaults에서 `StatChipWidgetClass`=`WBP_ShopStatChip`, `UniqueAbilityWidgetClass`=`WBP_ShopUniqueAbility` 지정. **이 패널엔 클릭 인터랙션이 전혀 없다**(구매는 `WBP_ShopItemSlot` 더블클릭, 판매는 `WBP_ShopInventorySlot` 우클릭) — 순수 정보 표시용이라 루트 Visibility 요건도 없음
  7. `WBP_ShopStats`(parent=`UP1ShopStatsWidget`, 상점 좌측 스탯): `PhysicalPowerText`/`AttackSpeedText`/`MagicalPowerText`/`PhysicalPenetrationText`/`MagicalPenetrationText`/`PhysicalArmorText`/`MagicalArmorText`/`MaxHealthText`/`HealthRegenText`/`LifeStealText`/`MaxManaText`/`ManaRegenText`/`AbilityHasteText`/`UltimateHasteText`/`UltimateDamagePercentText`/`TenacityText`/`DamageReductionText`/`CriticalChanceText`/`CriticalDamageText`(전부 TextBlock, 전부 선택 — 원하는 것만 배치해도 됨) 배치
  8. `WBP_ShopInventory`(parent=`UP1ShopInventoryWidget`, 상점 중앙하단 보유 아이템 6칸): `InventorySlotsContainer`(HorizontalBox 등 임의 Panel, 필수) 배치. Class Defaults에서 `InventorySlotWidgetClass`=`WBP_ShopInventorySlot` 지정
  9. `WBP_Shop`(parent=`UP1ShopWidget`, 최상위): 좌측에 `WBP_ShopStats` 인스턴스(`StatsWidget`), 중앙에 `WBP_ShopItemList`(`ItemListWidget`) 인스턴스 + 그 아래 `WBP_ShopInventory`(`InventoryWidget`) 인스턴스, 우측에 `WBP_ShopItemDetail`(`ItemDetailWidget`) 인스턴스를 배치(전부 이름으로 바인딩 — Scoreboard의 미리 배치한 팀 위젯과 달리 각각 인스턴스가 딱 하나씩이라 `DesignatedTeamId` 같은 식별자는 필요 없음). 그 위에 `CarryTabButton`/`MageTabButton`/`AssassinTabButton`/`TankTabButton`/`FighterTabButton`/`SupportTabButton`(Button, 전부 필수) 6개, `GoldText`(TextBlock, 선택), `CloseButton`(Button, 선택) 배치. **시작 Visibility를 Collapsed로 둘 것** — `AP1HUD::InitShop()`이 생성 직후 곧바로 Collapsed로 설정하긴 하지만 디자이너 프리뷰 기본값도 안전하게
- `BP_P1HUD`(기존 `OverlayWidgetClass`/`OverlayWidgetControllerClass`가 지정된 그 블루프린트)에 신규 프로퍼티 `ShopWidgetClass`=`WBP_Shop`, `ShopWidgetControllerClass`=`UP1ShopWidgetController`(또는 이를 parent로 하는 BP) 지정
- `Data/DT_ShopItems.json`을 Content Browser로 드래그&드롭 임포트 → `DT_ShopItems`(Row Struct=`FP1ShopItemData`) DataTable 생성 — 5개 예시 아이템에 `Categories`/`DisplayStats`/`UniqueAbilities`(이름/설명만)는 이미 채워져 있음(`Item_PowerBlade`에 "절단" 예시 하나 들어있음). 임포트 후 각 행마다 `Icon`(텍스처)과 `StatEffectClass`+`UniqueAbilities[].EffectClass`(둘 다 GameplayEffect) 등 자산 참조는 JSON에 못 담았으므로 DataTable 에디터에서 직접 채워야 함 — 최소 1개 아이템(예: `Item_PowerBlade`)은 끝까지 테스트하려면 `StatEffectClass`(Infinite Duration, Modifier 예: PhysicalPower +45)와 "절단" 항목의 `EffectClass`(예: 기본 공격 시 대상 최대 체력 1% 피해 GE — 아직 없으면 대충 아무 Infinite GE로 대체해서 적용/제거만 확인해도 됨) 둘 다 만들어 지정해둘 것
- `BP_P1PlayerState`(또는 해당 클래스)의 `ShopItemTable`에 `DT_ShopItems` 지정
- **패시브 골드 수입 GE**(예: `GE_PassiveGoldIncome`) 생성: `Duration Policy=Infinite`, `Period`=원하는 틱 간격(초), `Modifiers`에 `Gold` 속성 하나 — `Modifier Op=Additive`, `Magnitude=Scalable Float`로 틱당 지급량 고정값(예: 2.0) 지정. SetByCaller 필요 없음(영웅마다 다르게 줄 게 아니면). 만든 뒤 `BP_P1PlayerState`의 `PassiveGoldEffectClass`에 지정
- PIE 검증: 토글 키로 상점 열림/닫힘(마우스 커서 나타남/사라짐, WASD는 계속 먹는지) 확인 → 좌측 스탯 패널에 현재 스탯이 표시되는지 → 역할군 탭 전환 시 중앙 목록이 해당 역할군 아이템으로 바뀌는지(예: `Item_SwiftBoots`/`Item_HealthPotion`은 전 역할군에 걸쳐 있어 어느 탭에서도 보여야 함) → 목록에서 아이템 클릭 시 우측 상세 패널에 이름/가격/스탯목록/고유능력 설명이 채워지는지 → **목록 칸을 더블클릭하면 구매**되어 골드가 차감되고 좌측 스탯이 즉시 반영되며 중앙하단 인벤토리 칸에 아이콘이 채워지는지 → **같은 아이템을 다시 더블클릭해도 중복 구매가 안 되는지**(RPC 자체가 안 나가는지 로그로 확인) → **인벤토리 칸을 우클릭하면 판매**되어 골드가 환급되고 좌측 스탯이 원래대로 돌아오며 그 칸이 비는지(고유 능력 GE도 같이 제거됐는지, 예: `Item_PowerBlade`를 팔면 "절단" 효과도 사라지는지) → 골드 부족/인벤토리 만석(6칸) 상황에서 서버가 거부하고 클라이언트 상태가 깨지지 않는지 확인 → **패시브 골드 수입**: 가만히 있어도 골드가 주기적으로 오르는지, 죽였다가 죽어있는 동안에도(리스폰 대기 중) 계속 오르는지(멈추면 버그), 리스폰 후에도 중복 적용 없이 같은 속도로 계속 오르는지 확인

**HUD 우측하단 읽기전용 인벤토리 에디터 작업 필요(2026-08-08):**
- 2개 WBP를 이 순서로 생성:
  1. `WBP_HUDInventorySlot`(parent=`UP1HUDInventorySlotWidget`): `IconImage`(Image, 선택) 하나만 있으면 됨 — 클릭 인터랙션이 아예 없어서 `WBP_ShopInventorySlot`과 달리 루트 Visibility 요건도 없음
  2. `WBP_HUDInventory`(parent=`UP1HUDInventoryWidget`): `InventorySlotsContainer`(HorizontalBox 등 임의 Panel, 필수) 배치. Class Defaults에서 `InventorySlotWidgetClass`=`WBP_HUDInventorySlot` 지정
- `WBP_HUD`(기존 HealthBar/ManaBar/SkillIcon 등이 배치된 그 위젯)를 열어서 화면 우측 하단에 `WBP_HUDInventory` 인스턴스를 배치하고 이름을 `InventoryWidget`으로(BindWidgetOptional이라 이름 안 맞으면 그냥 안 뜨기만 함, 에러 없음)
- PIE 검증: 상점에서 아이템 구매/판매 시 상점 안 인벤토리 칸이랑 HUD 우측하단 칸이 **동시에** 갱신되는지(같은 데이터를 별도 컨트롤러 경로로 보여주는 거라 어긋나면 버그) → HUD 쪽 칸을 우클릭해도 **아무 일도 안 일어나는지**(읽기전용 확인 — 실수로 팔리면 안 됨) → 상점을 닫은 상태에서도 HUD 칸은 계속 보이는지

**애쉬브링어(아이템, 한글명 "잿불인도자") 에디터 작업 필요(2026-08-17, 원작 툴팁 기준 스탯 재조정 + 고유 능력 "절단" 유지):**
- `GE_Ashbringer_Stat`: Duration=Infinite, Modifier 4개 — `AbilityHaste` Additive **+10**, `AttackSpeed` Additive **+25**(100기준 퍼센트, +0.25 아님), `CriticalChance` Additive **+0.20**(0~1 분수 스케일, +20 아님), `PhysicalPower` Additive +40. `Item_Ashbringer`의 `StatEffectClass`에 지정.
- **신규** `GA_Item_ChronoStrike`(BP, parent=`UP1GameplayAbility_Item_ChronoStrike`, 고유 능력 표시명은 "절단"): 필요하면 `HeroCDRPercent`(기본 0.08)/`MonsterCDRPercent`(기본 0.04) 값 조정 — 기본값 그대로 써도 됨. **GE가 아니라 어빌리티**이므로 `Item_Ashbringer`의 `UniqueAbilities[0].TriggeredAbilityClass`에 지정 — `UniqueAbilities[0].EffectClass`는 비워둘 것.
- **`AbilityHaste`가 실제로 쿨다운을 줄이려면 `GE_GenericCooldownReduction`(범용, 딱 하나만)이 먼저 있어야 한다**: Duration Policy=Has Duration, Duration Magnitude=**Set by Caller**(`Data.CooldownDuration`), **Granted Tags는 비워둘 것**(런타임에 `DynamicGrantedTags`로 대상 어빌리티의 쿨다운 태그가 실림) — `BP_P1AbilitySystemComponent`의 `GenericCooldownReductionEffectClass`에 지정. 이 GE 하나가 Q/E/RMB/R 전체를 커버하므로(위 "AbilityHaste/UltimateHaste 쿨다운 감소 시스템" 설명 참고) 각 어빌리티의 기존 쿨다운 GE는 전혀 손댈 필요 없음 — `UP1AbilitySystemComponent::ReduceCooldownByInputTag()`가 "절단"과 헤이스트 시스템 양쪽에서 공유하는 인프라라 한 번만 설정하면 됨.
- PIE 검증: 애쉬브링어 구매 후 스탯 패널에 AbilityHaste/AttackSpeed/CriticalChance/PhysicalPower가 정확히 반영되는지, `AbilityHaste 10`으로 아무 스킬 쿨다운이나 약 9% 감소하는지(10/(10+100)), 기본 공격으로 적 영웅을 맞히면 "절단" 효과로 Q/E/RMB 쿨다운이 추가로 8%(정글 몬스터는 4%) 줄어드는지, 판매 시(`GA_Item_ChronoStrike`가 `ClearAbility`로 회수됨) "절단" 효과가 사라지는지 확인.

**더스트 데빌(아이템) 에디터 작업 필요(2026-08-17, C++ 완료 — 온-히트 아이템 어빌리티 리팩터링 반영):**
- `GE_DustDevil_Stat`: Duration=Infinite, Modifier 4개 — `AttackSpeed` Additive **+25**(100기준 스케일, +0.25 아님), `CriticalChance` Additive **+0.20**(0~1 분수 스케일, +20 아님 — 두 스탯이 정반대 스케일 컨벤션이라 헷갈리기 가장 쉬운 지점), `MovementSpeed` **Multiply ×1.04**(4% 증가 — 이동속도는 이 아이템만 유일하게 퍼센트 배율로 적용, `0.04`를 넣으면 -96%가 되니 주의), `PhysicalPower` Additive +30. `Item_DustDevil`의 `StatEffectClass`에 지정. **매너스(6스택) 이동속도 보너스도 같은 MovementSpeed 어트리뷰트에 Multiply로 걸리므로, 둘 다 활성화된 상태면 두 배율이 곱연산으로 누적된다(정상 동작 — 순서 신경 쓸 필요 없음).**
- **신규** `MMC_DustDevil_AttackSpeedPerStack`(BP, parent=`UP1MMC_StackCountMagnitude`): `MagnitudePerStack=3.0`(공격속도 스택당 3%, AttackSpeed 100기준 스케일).
- **신규** `MMC_DustDevil_MoveSpeedAt6Stacks`(BP, parent=`UP1MMC_StackThresholdMagnitude`): `RequiredStackCount=6`, `BonusMagnitude=1.10`, **`BelowThresholdMagnitude=1.0`(반드시! 이 GE의 MovementSpeed 모디파이어를 Multiply로 쓸 것이므로 0으로 두면 6스택 미만일 때 이동속도가 0으로 곱해져 사라진다)**.
- **신규** `GE_DustDevil_MenaceBuff`(실제 스택 버프, 이름 자유): Duration=Has Duration **3초**, **Stacking Type=AggregateByTarget, Stack Limit=6**, **Stack Duration Refresh Policy=RefreshOnSuccessfulApplication**(매 히트마다 3초로 갱신), Stack Expiration Policy는 ClearEntireStack 권장(전체 스택이 한꺼번에 만료). Modifier 2개: (1) `AttackSpeed`, Modifier Op=Additive, Magnitude=Custom Calculation Class=`MMC_DustDevil_AttackSpeedPerStack`. (2) `MovementSpeed`, Modifier Op=**Multiply**, Magnitude=Custom Calculation Class=`MMC_DustDevil_MoveSpeedAt6Stacks`.
- **신규** `GA_Item_Menace`(BP, parent=`UP1GameplayAbility_Item_Menace`): `MenaceBuffEffectClass`에 위 `GE_DustDevil_MenaceBuff` 지정. **GE가 아니라 어빌리티**이므로 `Item_DustDevil`의 `UniqueAbilities[0].TriggeredAbilityClass`에 지정 — `UniqueAbilities[0].EffectClass`는 비워둘 것.
- PIE 검증: 더스트 데빌 구매 후 기본 공격으로 적 영웅을 계속 맞히면 공격속도가 스택당(3%씩) 오르는지, 6스택에서 이동속도가 추가로 10% 붙는지, 크리티컬 적중 시 한 번에 2스택 오르는지, 3초간 안 때리면 스택이 전부 사라지고 공격속도/이동속도가 원래대로 돌아오는지, 정글 몬스터를 때렸을 때는 스택이 전혀 안 오르는지(영웅 전용) 확인, 판매 시(`GA_Item_Menace`가 `ClearAbility`로 회수됨) 더 이상 스택이 안 붙는지 확인.

**괴사(아이템) 에디터 작업 필요(2026-08-17, C++ 완료):** 애쉬브링어/더스트 데빌과 달리 두 고유 능력 다 온-히트 반응형이 아니라 조건 없는 상시 패시브라 **새 `UGameplayAbility` 서브클래스가 필요 없다** — 순수 GE 3개만 만들면 끝.
- `GE_Necrosis_Stat`: Duration=Infinite, Modifier 3개 — `AttackSpeed` Additive **+30**(100기준 스케일), `CriticalChance` Additive **+0.20**(0~1 분수 스케일), `PhysicalPower` Additive +35. `Item_Necrosis`의 `StatEffectClass`에 지정.
- **신규** `GE_Necrosis_NecroEdge`(이름 자유, "네크로의 칼날"): Duration=Infinite, `UltimateHaste` Modifier=Additive **+25**. `Item_Necrosis`의 `UniqueAbilities[0].EffectClass`에 지정 — `TriggeredAbilityClass`는 비워둘 것(반응형 아님). 이 값은 `UP1GameplayAbility::ApplyCooldown()`이 R 쿨다운 계산 시 `AbilityHaste`에 자동으로 더해 소비하므로 이 GE 외엔 아무것도 만들 필요가 없다.
- **신규** `GE_Necrosis_MatterDisruptor`(이름 자유, "물질분열기"): Duration=Infinite, `UltimateDamagePercent` Modifier=Additive **+0.15**(15%, +15 아님 — 0~1 분수 스케일). `Item_Necrosis`의 `UniqueAbilities[1].EffectClass`에 지정 — `TriggeredAbilityClass`는 비워둘 것. `UP1GameplayAbility_StoneForgedSoul`/`UP1GameplayAbility_IonStrike`가 R 데미지 판정마다 이 값을 직접 읽으므로 이 GE 외엔 아무것도 만들 필요가 없다.
- PIE 검증: 괴사 구매 후 R 쿨다운이(다른 스킬 사용 후 쿨다운 중일 때) 20%(Haste 25 기준) 줄어드는지 — Q/E/RMB는 영향 없어야 함(UltimateHaste는 R 전용). R로 적을 맞혔을 때 데미지가 15% 증가하는지(스톤포지드소울/이온스트라이크 둘 다 확인). 판매 시 두 효과 다 사라지는지(GE 제거라 `ActiveItemEffects` 경로로 자동 처리됨 — 특별히 다를 것 없음).

**임페라토르(아이템) 에디터 작업 필요(2026-08-17, C++ 완료):** 고유 능력 "정밀"도 상시 패시브라 **새 어빌리티 클래스는커녕 새 어트리뷰트도 필요 없다** — 이미 존재하는 `UP1AttributeSet::CriticalDamage`(치명타 데미지 배율, `P1ExecCalc_Damage`가 이미 소비 중)에 얹기만 하면 됨. GE 2개만 만들면 끝.
- `GE_Imperator_Stat`: Duration=Infinite, Modifier 2개 — `CriticalChance` Additive **+0.20**(0~1 분수 스케일), `PhysicalPower` Additive +55. `Item_Imperator`의 `StatEffectClass`에 지정.
- **신규** `GE_Imperator_Precision`(이름 자유, "정밀"): Duration=Infinite, `CriticalDamage` Modifier=Additive **+0.30**(1.5→1.8, 즉 크리티컬 데미지 150%→180% — +30 아님, 0~1 분수 스케일이 아니라 배율 자체에 더하는 값이니 주의). `Item_Imperator`의 `UniqueAbilities[0].EffectClass`에 지정 — `TriggeredAbilityClass`는 비워둘 것.
- PIE 검증: 임페라토르 구매 후 상점 스탯 패널의 `CriticalDamage`가 150%→180%로 바뀌는지, 실제 치명타 적중 시 데미지 배율이 커진 값(1.8배)으로 적용되는지(`[ExecCalc_Damage]` 로그의 `Crit=1(...배율x)` 값으로 확인), 판매 시 원래 배율(1.5)로 돌아오는지 확인.

**사면(아이템) 에디터 작업 필요(2026-08-17, C++ 완료 — 새 CC 반응 시스템 도입):**
- **⚠️ 가장 먼저**: 기존 스턴 GE(`GE_Debuff_Stun` 등, 스턴을 부여하는 모든 GE)를 열어 **Granted Tags에 `State.Immobilized`를 추가**한다(`State.Stunned`는 그대로 유지, 추가만). 이걸 안 하면 `Event.Character.Immobilized`가 아예 발신되지 않아 "용기"가 영원히 발동하지 않는다.
- `GE_Absolution_Stat`: Duration=Infinite, Modifier 4개 — `AttackSpeed` Additive **+25**(100기준 퍼센트), `MagicalArmor` Additive +35, `PhysicalPower` Additive +40, `Tenacity` Additive **+0.20**(0~1 분수 스케일). `Item_Absolution`의 `StatEffectClass`에 지정.
- **신규** `GE_Absolution_BraveryBuff`(이름 자유, 3초짜리 반응 버프): Duration=Has Duration **3초**, Modifier 2개 — `DamageReduction` Additive **+0.25**(0~1 분수 스케일), `MovementSpeed` **Multiply ×1.40**(40% 증가 — 애쉬브링어/더스트 데빌의 이동속도와 동일하게 배율, `+0.40`이 아니라 `1.40`).
- **신규** `GE_Absolution_Cooldown`: Duration=Has Duration **45초**(고정값, SetByCaller 불필요 — Scalable Float에 그냥 45 입력), **Granted Tags에 `Cooldown.Ability.Bravery`를 반드시 추가**(신규 네이티브 태그, 다른 어빌리티들의 `Cooldown.Ability.X`와 동일 컨벤션 — 엔진이 `CooldownGameplayEffectClass`의 이 태그로 "지금 쿨다운 중인지"를 자동 판별하므로 빠뜨리면 쿨다운 자체가 전혀 안 걸림).
- **신규** `GA_Item_Bravery`(BP, parent=`UP1GameplayAbility_Item_Bravery`): `BuffEffectClass`에 `GE_Absolution_BraveryBuff` 지정, Class Defaults의 `CooldownGameplayEffectClass`(베이스 `UGameplayAbility` 프로퍼티, Ability 탭에 있음)에 `GE_Absolution_Cooldown` 지정. `Item_Absolution`의 `UniqueAbilities[0].TriggeredAbilityClass`에 이 BP를 지정 — `UniqueAbilities[0].EffectClass`는 비워둘 것. **선택**: 보호막류 나이아가라 이펙트가 있다면 `ShieldEffect`(+필요시 `ShieldEffectSocketName`)에 지정 — 미지정 시 그냥 이펙트 없이 버프만 걸림.
- PIE 검증: 사면 구매 후 스턴에 걸리면(정글 몬스터 공격이나 StasisBomb 등으로) 3초간 스탯 패널의 `DamageReduction`이 오르고 이동속도가 빨라지는지, 스턴에 걸린 "동안"에도 이 버프가 정상적으로 걸리는지(스턴이 원래 대부분의 어빌리티 발동을 막는데 이건 예외적으로 뚫려야 함 — `GA_Item_Bravery` 생성자의 `ActivationBlockedTags.RemoveTag(State.Stunned)`가 핵심), 45초 안에 다시 스턴에 걸려도 재발동 안 하는지(쿨다운 중), 45초 지난 후 다시 스턴에 걸리면 재발동하는지, 판매 시 더 이상 반응 안 하는지(`ClearAbility`로 회수) 확인.

**증강(아이템) 에디터 작업 필요(2026-08-17, C++ 완료 — 이 세션에서 가장 복잡한 아이템):**
- `GE_Augmentation_Stat`: Duration=Infinite, Modifier 4개 — `AbilityHaste` Additive **+20**, `AttackSpeed` Additive **+25**(100기준 퍼센트), `MaxHealth` Additive +225, `PhysicalPower` Additive +30. `Item_Augmentation`의 `StatEffectClass`에 지정.
- **신규** `GE_Item_TrueStrike_EmpowerBuff`(이름 자유, "다음 공격 강화" 마커): Duration=Has Duration **4초**, Modifier 없이 **Granted Tags에 `Buff.TrueStrike.Empowered`만 추가**(실제 데미지는 GE가 아니라 어빌리티 코드가 계산해서 별도로 넣음 — 이 GE는 순수 "지금 강화 대기 중" 신호).
- **신규** `GE_Item_TrueStrike_Cooldown`: Duration=Has Duration **1.5초**, **Granted Tags에 `Cooldown.Ability.TrueStrike` 추가**(엔진이 이 태그로 쿨다운 여부를 판별 — 다른 아이템들의 `Cooldown.Ability.X`와 동일 컨벤션).
- **신규** `GA_Item_TrueStrike`(BP, parent=`UP1GameplayAbility_Item_TrueStrike`): `EmpowerBuffEffectClass`에 `GE_Item_TrueStrike_EmpowerBuff` 지정, Class Defaults의 `CooldownGameplayEffectClass`(베이스 `UGameplayAbility` 프로퍼티)에 `GE_Item_TrueStrike_Cooldown` 지정. `PhysicalPowerCoefficient`(기본 0.90)/`bIsTrueDamage`(기본 true)는 C++에서 이미 세팅해뒀으니 손댈 필요 없음(값을 바꾸고 싶을 때만 조정). **`DamageEffectClass`(베이스 `UP1DamageGameplayAbility` 프로퍼티)를 반드시 지정해야 함** — 다른 데미지 어빌리티들과 마찬가지로 물리 데미지 공용 GE(`GE_Damage`)를 그대로 재사용. `Item_Augmentation`의 `UniqueAbilities[0].TriggeredAbilityClass`에 이 BP를 지정 — `UniqueAbilities[0].EffectClass`는 비워둘 것. **선택**: 나이아가라 이펙트가 있다면 `EmpowerEffect`("부여" 순간, 스킬 사용 감지)/`ConsumeEffect`("소모" 순간, 강화된 공격 적중)에 지정 — 서로 독립이라 하나만 넣어도 되고 둘 다 생략해도 됨. **`EmpowerEffect`는 양손 소켓 2개**(`EmpowerEffectSocketNameLeftHand`/`EmpowerEffectSocketNameRightHand`, 주문검류 특유의 "양손이 빛나는" 연출용 — 소켓 이름이 실제로 채워진 쪽만 재생, 스켈레톤의 실제 손 소켓 이름을 확인해서 지정할 것)이고, `ConsumeEffect`는 소켓 1개(`ConsumeEffectSocketName`)뿐이라 구조가 다름.
- PIE 검증: 증강 구매 후 Q/E/RMB/R 아무거나 사용하면 즉시 발동하는지(스킬 다시 쓰면 재발동 안 함 — 1.5초 쿨다운 중), 4초 안에 기본공격을 적중시키면 물리 공격력의 90%만큼 추가 고정 피해가 들어가는지(`[ExecCalc_Damage]` 로그의 `True=1`, `PassThrough=1.00` 확인 — 대상 방어력이 높아도 추가 피해량이 안 줄어드는지가 핵심), 4초가 지나도록 기본공격을 안 하면 강화가 소멸돼 다음 기본공격엔 추가 피해가 없는지, 강화된 공격이 적중해서 소모된 후 같은 4초 창 안에 또 때려도 추가 피해가 안 들어가는지(1회 소모 확인), 기본공격(InputTag.Ability.BasicAttack) 자체를 써도 강화가 발동 안 하는지("스킬 사용 시"에만 반응, 기본공격은 트리거 아님) 확인.

**매치 결과 에디터 작업 필요(2026-08-12, C++ 완료):**
- `WBP_Scoreboard`의 각 `WBP_ScoreboardTeam` 인스턴스에 `TeamScoreText`(TextBlock, 선택) 배치 — 매치 진행 중 Tab 스코어보드에도 팀 킬스코어가 실시간(정확히는 Tab을 누른 시점 스냅샷)으로 뜨게 됨. 없어도 크래시는 안 나고 그 텍스트만 안 뜸
- `WBP_MatchResult`(parent=`UP1MatchResultWidget`) 생성 — `WinningTeamText`(TextBlock, 선택) 배치하고, **`WBP_Scoreboard`에서 이미 만든 `WBP_ScoreboardTeam` 인스턴스들을 그대로 복붙**해 팀 수만큼 배치 + `Designated Team Id` 지정(스코어보드와 완전히 같은 방식 — 이 결과창 전용으로 새로 만들 팀/행 위젯이 없음). 캔버스 시작 Visibility는 Collapsed 권장(스코어보드와 같은 이유)
- `BP_P1PlayerController`에 `MatchResultWidgetClass=WBP_MatchResult` 지정
- `BP_P1ArenaGameMode`의 `KillScoreToWin`(기본 30)/`ResultScreenDurationSeconds`(기본 6초)/`LobbyMapPath`(기본 `/Game/Maps/PreGame` — 실제 레벨 경로와 이미 일치, `Scripts/Package_Client.bat`이 쿡하는 경로와도 동일) 값 확인 — 테스트 중엔 `KillScoreToWin`을 2~3 정도로 낮춰서 빠르게 매치 종료를 재현하는 걸 추천
- PIE 2클라이언트로 검증: 한 팀이 `KillScoreToWin`을 채울 때까지 반복 처치 → 서버 로그(`[ArenaGameMode] Team N 킬스코어 X/Y`, `[ArenaGameMode] 매치 종료`)로 임계치 판정 확인 → 양쪽 클라이언트 모두 결과창이 뜨고 승리 팀 표시가 맞는지, 팀별 KDA 목록이 스코어보드와 동일하게 채워지는지 확인 → `ResultScreenDurationSeconds` 경과 후 각 클라이언트가 자동으로 로비로 개별 복귀하는지, 로비 로그인/매칭 화면이 정상적으로 뜨는지 확인 → 매치 종료 이후 잔여 데미지(막타 이후 스킬 잔탄 등)로 `OnTeamKillScored`가 또 불려도 `MatchState!=InProgress` 가드로 무시되고 크래시/중복 `EndMatch` 호출이 없는지 확인

**미니맵 에디터 작업 필요(2026-08-12, C++ 완료):**
- **배경 이미지 캡처**: 에디터에서 `InGame` 레벨 위에 임시 카메라를 아레나 정중앙 높은 고도에 배치해 정확히 수직(무회전)으로 내려다보는 스크린샷을 찍어 텍스처로 임포트 — 카메라가 조금이라도 회전/기울어지면 좌표 변환이 어긋난다
- **월드 범위 실측**: 캡처 이미지의 좌상단/우하단에 대응하는 실제 월드 X/Y 좌표를 에디터에서 확인(예: 아레나 바닥 반대편 모서리 두 지점의 좌표)
- `WBP_MinimapIcon`(parent=`UP1MinimapIconWidget`): `IconImage`(작은 원형 텍스처 추천, Image) 배치
- `WBP_Minimap`(parent=`UP1MinimapWidget`): `MinimapBackground`(Image, 위에서 캡처한 텍스처를 Brush로)+`MinimapIconContainer`(CanvasPanel, 배경 위에 겹쳐서) 배치. Class Defaults에서 `IconWidgetClass=WBP_MinimapIcon`, `WorldBoundsMin`/`WorldBoundsMax`(실측값), `TeamColors`(팀 수만큼, 예: 파랑/빨강/초록), `CampIconColor`(예: 노랑), 필요시 `VisionRadius`/`CampDetectionRadius`/`RefreshIntervalSeconds` 조정
- `WBP_HUD`에 `WBP_Minimap` 인스턴스를 화면 구석(보통 우측상단)에 배치하고 이름을 `MinimapWidget`으로
- PIE 2클라이언트로 검증: 양쪽 다 자기 아군 아이콘은 항상 보이는지, 상대팀 아이콘은 가까이 접근했을 때만 나타나고 멀어지면 사라지는지 → 정글 캠프 위치에 아이콘이 뜨는지, 무리를 전멸시키면 사라졌다가 `RespawnDelay` 후 다시 뜨는지 → 아군이 죽어있는 동안엔 시야를 제공 안 하는지(그 아군 근처의 적이 다른 아군 시야가 없으면 미니맵에서 사라지는지) → 캐릭터가 아레나 구석으로 이동했을 때 미니맵 아이콘도 배경 이미지의 대응 구석으로 움직이는지(어긋나면 `WorldBoundsMin`/`Max` 재보정) 확인
